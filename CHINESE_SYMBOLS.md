# 言律语言中文符号扩展规范

**版本：** 1.2  
**日期：** 2026-05-21  
**目标：** 增加更多中文符号，让代码更接近自然中文表达

---

## 一、完整符号表

### 1.1 基础标点符号

| 符号 | 名称 | 用途 | 示例 |
|------|------|------|------|
| `。` | 句号 | 语句结束 | `印x。` |
| `，` | 逗号 | 因果分隔/管道 | `温度升高，风扇开启。` |
| `；` | 分号 | 并列分隔 | `动作1；动作2。` |
| `：` | 冒号 | 块开始 | `对于i从1到10：` |
| `、` | 顿号 | 参数分隔 | `参数1、参数2，函数。` |

---

### 1.2 引号符号

| 符号 | 名称 | 用途 | 示例 |
|------|------|------|------|
| `'` `'` | 单引号 | 字符串字面量 | `'你好，世界'` |
| `"` `"` | 双引号 | 字符串（支持转义） | `"第一行\n第二行"` |

**转义字符：**
```yan
\n      # 换行
\t      # 制表符
\\      # 反斜杠
\"      # 双引号
\'      # 单引号
```

---

### 1.3 表达符号

| 符号 | 名称 | 用途 | 示例 |
|------|------|------|------|
| `！` | 叹号 | 断言/强调 | `x大0！` |
| `？` | 问号 | 条件判断 | `x大0？` |

---

### 1.4 书写符号

| 符号 | 名称 | 用途 | 示例 |
|------|------|------|------|
| `《》` | 书名号 | 模块/文件名 | `导入《数学库》` |
| `……` | 省略号 | 可变参数/范围 | `1……10` |
| `——` | 破折号 | 标签/标记 | `开始——：` |
| `～` | 波浪号 | 范围/约数 | `1～10` |
| `·` | 间隔号 | 属性访问 | `用户·姓名` |
| `【】` | 方括号 | 索引/标注 | `列表【0】` |
| `①②③` | 圆圈号 | 步骤标记 | `① 初始化。` |

---

## 二、符号详细用法

### 2.1 分号（；）- 并列分隔符

**用途：** 分隔并列的动作、条件、语句

```yan
# 并列动作
温度超过30度，风扇开启；空调开启；发送警报。

# 多条件判断
用户为管理员，允许所有操作；
用户为经理，允许部分操作；
用户为普通用户，只读操作。

# 多语句并列
定x是10；定y是20；定z是x加y。
```

---

### 2.2 叹号（！）- 断言与强调

**用途：** 表示断言、必须条件、错误提示

```yan
# 断言（运行时检查）
x大0！                    # 如果x不大于0，抛出错误
列表非空！                # 断言列表不为空

# 必须条件（编译时检查）
参数必填！
类型必须匹配！

# 错误提示
错误！文件不存在。
警告！内存不足。
```

---

### 2.3 问号（？）- 条件判断

**用途：** 表示疑问、条件表达式、可空类型

```yan
# 条件表达式（返回布尔值）
x大0？                    # 返回 真 或 假
列表为空？                # 返回 真 或 假

# 三元运算符
x大0？'正数'：'非正数'

# 可空类型
定名字是字符串？          # 名字可以是字符串或空
用户年龄是整数？          # 年龄可以是整数或空

# 安全访问
用户？年龄                # 如果用户存在，返回年龄；否则返回空
```

---

### 2.4 书名号（《》）- 模块与文件

**用途：** 表示模块名、文件名、库名

```yan
# 导入模块
导入《数学库》。
导入《文件系统》。

# 从模块导入
从《网络库》导入《HTTP请求》、《WebSocket》。

# 文件路径
定数据是读取《data.json》。
写入《output.txt》，结果。

# 引用文档
参见《语言规范》第三章。
```

---

### 2.5 省略号（……）- 可变参数与范围

**用途：** 表示可变参数、范围、未完成

```yan
# 可变参数
定求和是函数字……：
  数字……，归加0。

# 范围
对于i在1……10：           # 从1到10
  印i。

# 切片
列表[0……5]               # 前5个元素
字符串[开始……结束]        # 子字符串

# 未完成（待实现）
定复杂功能是函：
  # ……待实现
```

---

### 2.6 破折号（——）- 标签与标记

**用途：** 表示代码标签、跳转标记、代码段分隔

**说明：** 破折号在中文中主要用于补充说明，不适合作为注释符号。言律语言将其用于标签/标记，与注释区分开。

```yan
# 标签（用于跳转）
开始——：
  初始化。
  若条件满足就跳转到开始——。

# 代码段分隔
# 初始化部分
定x是10。
定y是20。

——

# 主逻辑部分
x加y，印。

# 错误处理标签
错误处理——：
  记录错误日志。
  发送告警通知。
```

---

### 2.7 波浪号（～）- 范围与约数

**用途：** 表示范围、约数、模糊匹配

```yan
# 范围
对于i在1～10：           # 从1到10（包含）
  印i。

年龄在18～60：           # 年龄范围
  允许注册。

# 约数
温度是25～30度。         # 大约25到30度
耗时是100～毫秒。        # 大约100毫秒

# 模糊匹配
名字～'张'               # 匹配以"张"开头的名字
```

---

### 2.8 间隔号（·）- 属性访问

**用途：** 表示属性访问、命名空间

```yan
# 属性访问
用户·姓名
订单·金额
列表·长度

# 命名空间
数学库·圆周率
文件库·读取
网络库·HTTP·请求

# 链式调用
用户·订单·金额
```

---

### 2.9 方括号（【】）- 索引与标注

**用途：** 表示索引、标注、特殊标记

```yan
# 索引访问
列表【0】                # 第一个元素
字典【'姓名'】           # 字典键访问

# 标注
【重要】这个函数需要优化。
【待办】实现错误处理。

# 特殊标记
【调试】印x。
【测试】断言结果等预期。
```

---

### 2.10 圆圈号（①②③）- 步骤标记

**用途：** 表示步骤、序号、枚举

```yan
# 步骤标记
① 初始化变量。
② 读取数据。
③ 处理数据。
④ 输出结果。

# 枚举选项
颜色选项：
  ① 红色
  ② 绿色
  ③ 蓝色
```

---

## 三、完整示例

### 3.1 冒泡排序（完整版）

```yan
# 冒泡排序
定冒泡排序是函列表：
  定长度是列表·长度。
  
  对于i在0～长度减1：
    对于j在0～长度减i减2：
      定当前是列表【j】。
      定下一个是列表【j加1】。
      
      若当前大下一个就：
        列表【j】、列表【j加1】，交换。

  返回列表。

# 测试
定数据是列5 3 8 4 2 1 9 6。
数据，冒泡排序，印。
```

---

### 3.2 游戏系统（完整版）

```yan
# ============================================
# 游戏状态机
# ============================================

定义游戏：
  初始状态：菜单。
  初始分数：0。
  初始生命：3。
  
  状态为菜单：
    按下开始键，状态变为游戏中。
    按下设置键，状态变为设置。
  
  状态为游戏中：
    玩家被击中，生命减1；检查生命。
    分数达到1000，等级加1；显示升级提示。
    按下暂停键，状态变为暂停。
  
  状态为游戏结束：
    显示最终分数！
    按下重新开始，分数重置；生命重置；状态变为游戏中。

# ============================================
# 玩家控制
# ============================================

定义玩家：
  初始位置：屏幕中央。
  初始速度：5。
  
  按下左键，位置左移速度。
  按下右键，位置右移速度。
  按下空格键，发射子弹！
  
  位置超出边界？位置等于边界。

# ============================================
# 碰撞检测
# ============================================

定检测碰撞是函对象1、对象2：
  定距离是算对象1·位置减对象2·位置，绝对。
  距离小于碰撞半径？返回真。不然返回假。

# ============================================
# 主循环
# ============================================

定主循环是函：
  定游戏实例是创建游戏。
  定玩家实例是创建玩家。
  
  ① 初始化游戏。
  ② 加载资源。
  ③ 开始游戏循环。
  
  一直：
    处理输入；更新状态；渲染画面。
    等待16毫秒。

主循环。
```

---

### 3.3 文件处理（完整版）

```yan
# ============================================
# 文件处理示例
# ============================================

# 导入模块
导入《文件系统》。
导入《数据处理》。

# 读取配置
定配置是读取《config.json》。
配置有效？继续。不然抛出错误！

# 处理数据
定数据是读取《data.txt》。
定结果是数据处理·转换数据。

# 写入结果
写入《output.txt》，结果。
成功？印'处理完成'。不然印'写入失败'！

# 日志
追加《log.txt》，'处理时间：'、当前时间。
```

---

## 四、符号优先级

### 4.1 解析优先级

| 优先级 | 符号类型 | 说明 |
|--------|----------|------|
| 1 | `「」` `『』` `'` `"` | 字符串优先识别 |
| 2 | `《》` | 模块/文件名 |
| 3 | `【】` | 索引/标注 |
| 4 | `！` `？` | 表达符号 |
| 5 | `……` `～` | 范围符号 |
| 6 | `——` | 注释符号 |
| 7 | `·` | 属性访问 |
| 8 | `；` `、` | 分隔符号 |
| 9 | `，` `：` | 结构符号 |
| 10 | `。` | 语句结束 |

---

## 五、符号使用建议

### 5.1 推荐用法

**字符串：**
- 简单字符串：使用单引号 `'你好'`
- 包含转义：使用双引号 `"第一行\n第二行"`

**并列动作：**
- 3个以内：使用顿号 `动作1、动作2、动作3`
- 3个以上：使用分号 `动作1；动作2；动作3；动作4`

**范围表达：**
- 精确范围：使用波浪号 `1～10`
- 模糊范围：使用省略号 `1……10`
- 可变参数：使用省略号 `参数……`

**注释：**
- 行内注释：使用破折号 `—— 注释`
- 块注释：使用 `#` 注释

---

### 5.2 避免用法

**不要混用引号：**
```yan
# 错误
'你好"

# 正确
'你好'
"你好"
```

**不要在表达式中使用书名号：**
```yan
# 错误
定x是《变量》

# 正确
定x是变量
```

**不要在索引中使用圆圈号：**
```yan
# 错误
列表①

# 正确
列表【0】
```

---

## 六、符号扩展路线图

### 6.1 已实现

- ✅ 基础标点符号（`。` `，` `；` `：` `、`）
- ✅ 引号符号（`'` `"`）
- ✅ 表达符号（`！` `？`）
- ✅ 书写符号（`《》` `……` `——` `～` `·` `【】`）

### 6.2 待实现

- ⬜ 圆圈号（`①②③`）- 步骤标记
- ⬜ 更多中文符号（根据实际需求）

---

## 七、总结

### 7.1 核心优势

1. **更接近自然中文** - 使用中文符号，符合中文习惯
2. **语义更清晰** - 每个符号有明确的语义
3. **视觉层次分明** - 不同符号表示不同层次
4. **易于理解** - 降低学习门槛

### 7.2 符号数量

- 基础标点：5个
- 引号符号：2个
- 表达符号：2个
- 书写符号：7个
- **总计：16个中文符号**

---

**版本历史：**

- v1.2 (2026-05-21) - 改用单引号和双引号，增加中文符号扩展
- v1.1 (2026-05-20) - 缩进语法优化
- v1.0 (2026-05-20) - 初始版本

---

**版权声明：** 本文档遵循 CC 4.0 BY-SA 版权协议
