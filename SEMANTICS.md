# 言律语义规范 v1.0

**版本：** 1.0  
**状态：** 草案  
**日期：** 2026-05-20

---

## 1. 语义分析概述

### 1.1 目标

语义分析阶段负责：

1. **类型检查** - 确保类型一致性
2. **作用域解析** - 确定标识符的可见性
3. **上下文补全** - 利用语境推断省略的元素
4. **语义验证** - 检查语义正确性

### 1.2 分析流程

```
AST → 语义分析器 → 增强AST
         ↓
    上下文管理器
         ↓
    符号表 + 类型表 + 作用域链
```

---

## 2. 类型系统

### 2.1 类型层次

```
Any
├─ 数值
│  ├─ 整数
│  └─ 浮点数
├─ 文本
│  └─ 字符串
├─ 布尔
├─ 空
├─ 集合
│  ├─ 列表
│  ├─ 字典
│  └─ 元组
└─ 函数
```

### 2.2 类型推断规则

#### 2.2.1 字面量推断

```yan
42          → 整数
3.14        → 浮点数
「你好」     → 字符串
真          → 布尔
空          → 空
[1, 2, 3]   → 列表<整数>
```

#### 2.2.2 算术表达式推断

```
整数 + 整数     → 整数
整数 + 浮点数   → 浮点数
浮点数 + 浮点数 → 浮点数
字符串 + 字符串 → 字符串
```

#### 2.2.3 比较表达式推断

```
任何类型 = 任何类型   → 布尔
数值 < 数值          → 布尔
字符串 = 字符串      → 布尔
```

#### 2.2.4 函数调用推断

```
函数<参数类型 → 返回类型> + 参数类型匹配 → 返回类型
```

### 2.3 类型兼容性

#### 2.3.1 隐式转换

| 源类型 | 目标类型 | 条件 |
|--------|----------|------|
| 整数 | 浮点数 | 总是 |
| 空 | 任何类型 | 总是 |

#### 2.3.2 显式转换

```yan
字符串转整数(字符串)     → 整数
整数转字符串(整数)       → 字符串
浮点数转整数(浮点数)     → 整数
```

---

## 3. 作用域系统

### 3.1 作用域类型

| 作用域类型 | 创建时机 | 可见性 |
|-----------|---------|--------|
| 全局作用域 | 程序启动 | 整个程序 |
| 模块作用域 | 模块导入 | 模块内部 |
| 函数作用域 | 函数调用 | 函数内部 |
| 块作用域 | 作用域语法 | 块内部 |

### 3.2 作用域链

**示例：**

```yan
定 全局变量 = 10。

定义 模块：
  定 模块变量 = 20。
  
  定 函数 = 函 参数：
    定 局部变量 = 30。
    
    # 作用域链：局部 → 模块 → 全局
    定 结果 = 局部变量 + 模块变量 + 全局变量。
    返回 结果。
  。
```

### 3.3 标识符查找规则

1. 在当前作用域查找
2. 如果未找到，在父作用域查找
3. 重复直到全局作用域
4. 如果仍未找到，报错

---

## 4. 上下文补全

### 4.1 主题链跟踪

**定义：主题链是上下文中持续有效的讨论对象**

**补全策略：**

| 当前话题 | 后续省略句 | 自动补全结果 |
|----------|-----------|-------------|
| 上一句提到"灯" | "调暗一点" | 灯.调暗(30%) |
| 上一句是"温度太高" | "就报警" | if temp > threshold: alert() |
| 在"做饭的时候："作用域内 | "火开中" | stove.设置火力('中') |
| 最近操作对象是"门" | "锁上它" | door.锁定() |

### 4.2 省略补全规则

#### 4.2.1 主语省略

**规则：** 当主语在上下文中明确时，可以省略

**示例：**

```yan
用户登录。
验证权限。    # 省略主语"系统"
发送通知。    # 省略主语"系统"
```

**补全后：**

```python
user.login()
system.verify_permission(user)
system.send_notification(user)
```

#### 4.2.2 宾语省略

**规则：** 当宾语在上下文中明确时，可以省略

**示例：**

```yan
获取用户信息。
显示。        # 省略宾语"用户信息"
```

**补全后：**

```python
user_info = get_user_info()
display(user_info)
```

#### 4.2.3 条件省略

**规则：** 在作用域内，条件可以继承

**示例：**

```yan
用户为管理员：
  显示管理面板。
  允许所有操作。    # 省略条件"用户为管理员"
```

**补全后：**

```python
if user.is_admin():
    show_admin_panel()
    allow_all_operations()
```

### 4.3 语境推断

#### 4.3.1 类型推断

**规则：** 根据使用上下文推断类型

**示例：**

```yan
定 数据 = 读取文件。
定 结果 = 数据 + 10。    # 推断"数据"为数值类型
```

#### 4.3.2 函数重载

**规则：** 根据参数类型选择函数实现

**示例：**

```yan
定义 处理：
  定 处理 = 函 数据为字符串：
    返回 数据，字符串长度。
  。
  
  定 处理 = 函 数据为列表：
    返回 数据，列表长度。
  。
```

---

## 5. 因果链语义

### 5.1 因果关系识别

**识别规则：**

1. **时间顺序** - 前句先于后句
2. **逻辑关联** - 前句是条件，后句是结果
3. **语义连贯** - 两句语义相关

**示例：**

```yan
温度升高。风扇开启。
```

**识别过程：**

1. "温度升高"是状态变化（条件）
2. "风扇开启"是动作（结果）
3. 两者语义相关（温度控制）
4. 建立因果关系：温度升高 → 风扇开启

### 5.2 条件累积

**规则：** 多个条件可以累积

**示例：**

```yan
温度升高。
湿度升高。
空调开启。
```

**解析为：**

```python
if temperature_increased() and humidity_increased():
    air_conditioner.turn_on()
```

### 5.3 因果链长度限制

**规则：** 最多支持5个连续流水句

**原因：**
- 避免过度复杂的条件组合
- 保持代码可读性
- 减少解析歧义

---

## 6. 状态流语义

### 6.1 状态定义

**语法：**

```yan
实体：
  初始状态：值。
```

**语义：**

1. 创建实体实例
2. 初始化状态变量
3. 注册状态监听器

### 6.2 状态转换

**语法：**

```yan
触发条件，状态变为新值。
```

**语义：**

1. 监听触发条件
2. 当条件满足时，更新状态
3. 触发状态变化事件

**示例：**

```yan
交通信号灯：
  初始状态：红灯。
  
  计时超过30秒，状态变为绿灯。
```

**编译为：**

```python
class TrafficLight:
    def __init__(self):
        self.state = 'red'
        self.timer = 0
    
    def update(self):
        if self.state == 'red' and self.timer > 30:
            self.state = 'green'
            self.timer = 0
            self.on_state_change('green')
```

### 6.3 状态翻转

**语法：**

```yan
触发条件，状态翻转。
```

**语义：**

仅适用于布尔类型状态，将真变假、假变真。

### 6.4 状态检查

**语法：**

```yan
状态为某值，执行动作。
```

**语义：**

当状态等于指定值时，执行动作。

---

## 7. 意合式调用语义

### 7.1 参数匹配

#### 7.1.1 位置参数

**语法：**

```yan
参数1、参数2，函数名。
```

**语义：**

按位置顺序匹配函数参数。

**示例：**

```yan
用户、订单，计算折扣。
```

**编译为：**

```python
calculate_discount(user, order)
```

#### 7.1.2 命名参数

**语法：**

```yan
参数名1为值1、参数名2为值2，函数名。
```

**语义：**

按参数名匹配函数参数。

**示例：**

```yan
用户ID为123、订单金额为500，创建订单。
```

**编译为：**

```python
create_order(user_id=123, order_amount=500)
```

#### 7.1.3 混合参数

**语法：**

```yan
位置参数、命名参数，函数名。
```

**语义：**

先匹配位置参数，再匹配命名参数。

**示例：**

```yan
用户、订单金额为500，创建订单。
```

**编译为：**

```python
create_order(user, order_amount=500)
```

### 7.2 参数推断

**规则：** 根据上下文推断缺失参数

**示例：**

```yan
当前用户、当前订单，计算折扣。
```

**推断过程：**

1. 查找上下文中的"当前用户"
2. 查找上下文中的"当前订单"
3. 将找到的值作为参数传递

### 7.3 链式调用

**语法：**

```yan
参数，函数1，函数2，函数3。
```

**语义：**

将前一个函数的返回值作为后一个函数的参数。

**示例：**

```yan
用户、订单，计算折扣，应用到订单，保存订单。
```

**编译为：**

```python
save_order(apply_to_order(calculate_discount(user, order)))
```

---

## 8. 多轨制语义

### 8.1 数学轨语义

**语法：**

```yan
$(数学公式)
```

**语义：**

1. 解析数学公式
2. 转换为对应的计算表达式
3. 在当前上下文中求值

**示例：**

```yan
定 圆面积 = $(π * r²)。
```

**编译为：**

```python
import math
circle_area = math.pi * r ** 2
```

### 8.2 Python轨语义

**语法：**

```yan
{{python:
Python代码
}}。
```

**语义：**

1. 提取Python代码块
2. 在隔离环境中执行
3. 捕获返回值
4. 将返回值注入当前作用域

**示例：**

```yan
定 数据 = {{python:
import pandas as pd
df = pd.read_csv('data.csv')
return df.head()
}}。
```

**编译为：**

```python
def _python_block_1():
    import pandas as pd
    df = pd.read_csv('data.csv')
    return df.head()

data = _python_block_1()
```

### 8.3 JavaScript轨语义

**语法：**

```yan
{{javascript:
JavaScript代码
}}。
```

**语义：**

1. 提取JavaScript代码块
2. 通过Node.js执行
3. 捕获返回值
4. 将返回值注入当前作用域

### 8.4 SQL轨语义

**语法：**

```yan
{{sql:
SQL查询
}}。
```

**语义：**

1. 提取SQL查询
2. 连接数据库
3. 执行查询
4. 返回结果集

---

## 9. 语义错误处理

### 9.1 类型错误

**错误代码：** E301

**示例：**

```yan
定 文本 = 「123」。
定 结果 = 文本 + 10。    # 类型错误
```

**错误消息：**

```
错误 [E301] 在 示例.yan:2:15
  类型不匹配：无法将「字符串」和「整数」相加
  
  定 结果 = 文本 + 10。
           ^^^^^^^^ 指示位置
  
  建议：请使用「文本转整数」函数将字符串转换为整数
```

### 9.2 未定义符号

**错误代码：** E302

**示例：**

```yan
定 结果 = 未定义变量 + 10。
```

**错误消息：**

```
错误 [E302] 在 示例.yan:1:11
  未定义的符号「未定义变量」
  
  定 结果 = 未定义变量 + 10。
           ^^^^^^^^^^ 指示位置
  
  建议：请先定义变量「未定义变量」
```

### 9.3 作用域错误

**错误代码：** E303

**示例：**

```yan
定 函数 = 函 参数：
  定 局部变量 = 10。
  返回 局部变量。
。

定 结果 = 局部变量。    # 作用域错误
```

**错误消息：**

```
错误 [E303] 在 示例.yan:5:11
  符号「局部变量」在当前作用域不可见
  
  定 结果 = 局部变量。
           ^^^^^^^^ 指示位置
  
  建议：「局部变量」定义在函数内部，请在函数内部使用它
```

---

## 10. 语义优化

### 10.1 常量折叠

**优化：** 编译时计算常量表达式

**示例：**

```yan
定 圆周率 = 3.14159。
定 半径 = 10。
定 面积 = 圆周率 * 半径 * 半径。
```

**优化后：**

```python
PI = 3.14159
RADIUS = 10
AREA = 314.159  # 编译时计算
```

### 10.2 死代码消除

**优化：** 移除永远不会执行的代码

**示例：**

```yan
要是 假：
  这段代码永远不会执行。
```

**优化后：**

```python
# 死代码已移除
```

### 10.3 内联优化

**优化：** 将简单函数内联展开

**示例：**

```yan
定 平方 = 函 x：
  返回 x * x。
。

定 结果 = 5，平方。
```

**优化后：**

```python
result = 5 * 5  # 函数已内联
```

---

## 11. 附录

### 11.1 语义分析器接口

```python
class SemanticAnalyzer:
    def analyze(self, ast, context):
        """分析AST并返回增强AST"""
        pass
    
    def check_types(self, node):
        """类型检查"""
        pass
    
    def resolve_scope(self, identifier):
        """作用域解析"""
        pass
    
    def infer_type(self, expression):
        """类型推断"""
        pass
    
    def complete_context(self, node):
        """上下文补全"""
        pass
```

### 11.2 符号表结构

```python
class SymbolTable:
    def __init__(self):
        self.symbols = {}  # 符号名 -> 符号信息
        self.parent = None  # 父作用域
    
    def define(self, name, symbol):
        """定义符号"""
        pass
    
    def lookup(self, name):
        """查找符号"""
        pass
```

---

**版本历史：**

- v1.0 (2026-05-20) - 初始版本

---

**版权声明：** 本规范遵循 CC 4.0 BY-SA 版权协议
