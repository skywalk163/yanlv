# 言律语言语法优化建议

**版本：** 1.1  
**日期：** 2026-05-20  
**基于：** zhixing + newlisp/yan 双项目对比分析

---

## 一、两个项目的核心优势

### 1.1 zhixing 的优势

**1. 友好语法设计**
- **块结束标记**：`结束` / `完毕` 比 `。。` 更符合中文习惯
- **条件表达式**：`就` / `不然` 比 `则` / `否则` 更口语化
- **变量赋值**：`是` 比 `=` 更自然
- **数学表达式**：`算` 比 `$()` 更简洁
- **循环语法**：`对于` 比 `遍历` 更易理解

**示例对比：**
```yan
# zhixing 友好语法
定fact是函n：
  若n等1就1不然n乘fact算n减1。
结束

# 原始语法
定fact=函n：
  若n等1则1否则n乘fact $(n-1)。
。
```

**2. 百家姓变量命名**
- 张三、李四、王小明等,符合中文人名习惯
- 自动识别用户自定义变量
- 避免与关键字冲突

**3. 隐式定义/赋值**
- 首次出现 `=` 或 `等于` → 定义
- 再次出现 → 赋值
- 减少关键字使用

**4. 中文数字支持**
- `十` → 10
- `二十五` → 25
- `十万` → 100000

**5. Markdown 块语法**
- `#` 标题定义作用域
- 子块继承父块变量
- 文学编程风格

---

### 1.2 newlisp/yan 的优势

**1. 元数驱动解析**
- 动词声明参数数量
- 自动收集参数
- 支持无空格分词

**示例：**
```yan
印斐波那契15。

解析过程：
1. "印" 是动词(元数=1)
2. 收集1个参数 → "斐波那契15"
3. 递归解析 → 斐波那契(15)
4. 生成调用 → 印(斐波那契(15))
```

**2. 无空格分词**
- 汉字连续书写,符合中文习惯
- 贪心最长匹配关键字
- 类型切换自动分词

**3. 两遍解析机制**
- 第一遍：收集用户定义函数名
- 第二遍：正常解析
- 支持递归函数

**4. 自举成功**
- 1908行纯言语言代码
- Python代码块占比 < 5%
- 证明语言设计可行性

**5. 管道操作**
- `，` 作为管道符
- 数据流动清晰
- 支持链式调用

**6. 双轨制语法**
- `$()` 数学表达式
- `{{}}` Python代码块
- 中文+数学+Python融合

---

## 二、言律语言优化方案

### 2.1 核心语法优化

#### 优化1：采用元数驱动解析

**原因：** 这是实现无空格分词的关键技术

**实现：**
```python
# 动词元数表
VERB_ARITY = {
    # 算术
    '加': 2, '减': 2, '乘': 2, '除': 2, '模': 2, '幂': 2,
    '负': 1, '绝对': 1,
    
    # 比较
    '大': 2, '小': 2, '等': 2, '不等': 2,
    
    # 逻辑
    '且': 2, '或': 2, '非': 1,
    
    # 列表
    '列': -1,  # 可变参数
    '首': 1, '余': 1, '入': 2, '长': 1, '添': 2, '连': 2, '含': 2,
    
    # 高阶函数
    '皆': 2, '只': 2, '归': 3,
    
    # I/O
    '印': 1, '读': 1, '写': 2, '行': 1,
}
```

**示例：**
```yan
# 无空格书写
印斐波那契15。
列1 2 3皆乘2。

# 解析过程
印(斐波那契(15))
皆(乘(2), 列(1, 2, 3))
```

---

#### 优化2：引入友好语法

**原因：** 降低学习门槛,符合中文习惯

**语法对照表：**

| 功能 | 原语法 | 友好语法 | 说明 |
|------|--------|----------|------|
| 块结束 | `。` | `结束` / `完毕` | 更符合中文习惯 |
| 条件-则 | `则` | `就` | 口语化表达 |
| 条件-否则 | `否则` | `不然` | 口语化表达 |
| 变量赋值 | `=` | `是` / `等于` | 更自然 |
| 数学表达式 | `$(...)` | `算...` | 更简洁 |
| 循环 | `遍历` | `对于` | 更易理解 |

**示例：**
```yan
# 友好语法
定fact是函n：
  若n等1就1不然n乘fact算n减1。
结束

# 原语法（兼容）
定fact=函n：
  若n等1则1否则n乘fact $(n-1)。
。
```

---

#### 优化3：百家姓变量命名

**原因：** 符合中文人名习惯,自动识别用户变量

**实现：**
```python
# 百家姓表格（前20个）
SURNAMES = ['赵', '钱', '孙', '李', '周', '吴', '郑', '王', 
            '冯', '陈', '褚', '卫', '蒋', '沈', '韩', '杨',
            '朱', '秦', '尤', '许']

# 冲突姓氏（不能作为变量名）
CONFLICTING = {
    '空': '空值关键字',
    '言': '语言名称',
    '印': '内置动词',
}

def is_variable(name):
    """判断是否为用户变量"""
    if name[0] in CONFLICTING:
        return False
    return name[0] in SURNAMES
```

**示例：**
```yan
定张三=10。
定李四=20。
张三加李四。        -- 30

定王小明=5。
王小明乘2。        -- 10
```

---

#### 优化4：隐式定义/赋值

**原因：** 减少关键字使用,更符合中文习惯

**规则：**
1. 首次出现 `=` 或 `等于` → 定义（等价于 `定`）
2. 再次出现 → 赋值（等价于 `设`）
3. 显式指定 `定` / `设` 可消除歧义

**示例：**
```yan
# 隐式定义
张三等于10。        -- 等价于 "定张三=10"
李四等于20。        -- 等价于 "定李四=20"

# 隐式赋值
张三等于张三加1。   -- 等价于 "设张三=张三加1"

# 显式指定（消除歧义）
定王五=20。         -- 明确定义
设王五=王五加1。    -- 明确赋值
```

---

#### 优化5：中文数字支持

**原因：** 支持中文数字,更符合中文习惯

**实现：**
```python
CHINESE_NUMBERS = {
    '零': 0, '一': 1, '二': 2, '三': 3, '四': 4,
    '五': 5, '六': 6, '七': 7, '八': 8, '九': 9,
    '十': 10, '百': 100, '千': 1000, '万': 10000,
}

def parse_chinese_number(text):
    """解析中文数字"""
    # 单个数字
    if text in CHINESE_NUMBERS:
        return CHINESE_NUMBERS[text]
    
    # 组合数字（如"十三"、"二十五"）
    # ... 实现略
```

**示例：**
```yan
张三等于十。        -- 等价于 "张三=10"
李四等于二十五。    -- 等价于 "李四=25"
结果等于五加三。    -- 等价于 "结果=5加3"
```

---

### 2.2 因果链语法优化

#### 优化6：因果链 + 元数驱动

**原因：** 结合因果链的自然表达和元数驱动的精确解析

**语法：**
```yan
条件，动作。
条件1、条件2，动作1、动作2。
```

**解析规则：**
1. `，` 左侧为条件表达式
2. `，` 右侧为动作列表（用 `、` 分隔）
3. `。` 结束语句

**示例：**
```yan
# 单条件单动作
温度升高，风扇开启。

# 单条件多动作
温度超过30度，风扇开启、空调开启。

# 多条件单动作
温度升高、湿度升高，空调开启。

# 多条件多动作
温度超过30度、湿度超过60%，风扇开启、空调开启、发送警报。
```

**编译为Python：**
```python
# 温度升高，风扇开启。
if temperature_increased():
    fan.turn_on()

# 温度超过30度，风扇开启、空调开启。
if temperature > 30:
    fan.turn_on()
    air_conditioner.turn_on()

# 温度升高、湿度升高，空调开启。
if temperature_increased() and humidity_increased():
    air_conditioner.turn_on()
```

---

### 2.3 状态流语法优化

#### 优化7：状态流 + 隐式定义

**原因：** 结合状态流的自然表达和隐式定义的简洁性

**语法：**
```yan
实体：
  初始状态：值。
  
  触发条件，状态变为新值。
  触发条件，状态翻转。
  
  状态为某值，执行动作。
```

**示例：**
```yan
交通信号灯：
  
  初始状态：红灯。
  初始计时：0。
  
  状态为红灯：
    计时超过30秒，状态变为绿灯、计时重置。
  
  状态为绿灯：
    计时超过25秒，状态变为黄灯、计时重置。
  
  状态为黄灯：
    计时超过5秒，状态变为红灯、计时重置。
```

**编译为Python：**
```python
class TrafficLight:
    def __init__(self):
        self.state = 'red'
        self.timer = 0
    
    def update(self):
        if self.state == 'red' and self.timer > 30:
            self.state = 'green'
            self.timer = 0
            self.on_state_change('green')
        
        elif self.state == 'green' and self.timer > 25:
            self.state = 'yellow'
            self.timer = 0
            self.on_state_change('yellow')
        
        elif self.state == 'yellow' and self.timer > 5:
            self.state = 'red'
            self.timer = 0
            self.on_state_change('red')
```

---

### 2.4 意合式函数调用优化

#### 优化8：意合式调用 + 管道操作

**原因：** 结合意合式调用的自然表达和管道操作的数据流

**语法：**
```yan
参数1、参数2，函数名。
参数，函数1，函数2，函数3。
```

**解析规则：**
1. `、` 分隔参数
2. `，` 分隔函数调用
3. 管道注入：前一个函数的返回值作为后一个函数的第一个参数

**示例：**
```yan
# 单函数调用
用户、订单，计算折扣。

# 链式调用
用户、订单，计算折扣，应用到订单，保存订单。

# 管道操作
列1 2 3，皆乘2，只大3，归加0。
```

**编译为Python：**
```python
# 用户、订单，计算折扣。
calculate_discount(user, order)

# 用户、订单，计算折扣，应用到订单，保存订单。
save_order(apply_to_order(calculate_discount(user, order)))

# 列1 2 3，皆乘2，只大3，归加0。
reduce(add, 0, filter(lambda x: x > 3, map(lambda x: x * 2, [1, 2, 3])))
```

---

### 2.5 多轨制语法优化

#### 优化9：多轨制 + 友好语法

**原因：** 结合多轨制的灵活性和友好语法的简洁性

**语法：**
```yan
# 数学轨（友好语法）
算1+2*3。
算π*r²。

# 数学轨（原语法，兼容）
$(1+2*3)。

# Python轨
{{
import pandas as pd
df = pd.read_csv('data.csv')
return df.head()
}}。

# JavaScript轨
{{
javascript:
class Component {
  constructor(props) {
    this.props = props;
  }
  render() {
    return `<div>${this.props.title}</div>`;
  }
}
return new Component({title: '你好'});
}}。
```

---

## 三、实现优先级

### 3.1 高优先级（核心功能）

1. **元数驱动解析** - 实现无空格分词的基础
2. **友好语法** - 降低学习门槛
3. **百家姓变量命名** - 符合中文习惯
4. **因果链语法** - 核心创新特性

### 3.2 中优先级（增强功能）

5. **隐式定义/赋值** - 减少关键字使用
6. **中文数字支持** - 符合中文习惯
7. **状态流语法** - 自然语言描述状态变化
8. **意合式函数调用** - 自然语言调用函数

### 3.3 低优先级（扩展功能）

9. **多轨制语法** - 支持多语言嵌入
10. **Markdown块语法** - 文学编程支持

---

## 四、语法对照表

### 4.1 完整对照表

| 功能 | zhixing | newlisp/yan | 言律优化方案 | 说明 |
|------|---------|-------------|--------------|------|
| **分词机制** | 贪心匹配 | 元数驱动 | **元数驱动** | 精确解析 |
| **空格要求** | 可选 | 无需 | **无需** | 符合中文习惯 |
| **块结束** | `结束` | `。` | **`结束`** | 更自然 |
| **条件-则** | `就` | `则` | **`就`** | 口语化 |
| **条件-否则** | `不然` | `否则` | **`不然`** | 口语化 |
| **变量赋值** | `是` | `=` | **`是` / `=`** | 双语法支持 |
| **数学表达式** | `算` | `$()` | **`算` / `$()`** | 双语法支持 |
| **循环** | `对于` | `遍历` | **`对于`** | 更易理解 |
| **变量命名** | 百家姓 | 任意 | **百家姓** | 符合中文习惯 |
| **隐式定义** | 支持 | 不支持 | **支持** | 减少关键字 |
| **中文数字** | 支持 | 不支持 | **支持** | 符合中文习惯 |
| **管道操作** | `，` | `，` | **`，`** | 数据流动 |
| **因果链** | 无 | 无 | **创新特性** | 核心创新 |
| **状态流** | 无 | 无 | **创新特性** | 核心创新 |
| **意合式调用** | 无 | 无 | **创新特性** | 核心创新 |
| **多轨制** | 双轨 | 双轨 | **多轨** | 更灵活 |
| **自举** | 失败 | 成功 | **待实现** | 验证设计 |

---

## 五、示例对比

### 5.1 阶乘函数

**zhixing：**
```yan
定fact是函n：
  若n等1就1不然n乘fact算n减1。
结束
```

**newlisp/yan：**
```yan
定阶乘=函n若n等1则1否则n乘阶乘n减1。
```

**言律优化方案：**
```yan
# 方案1：友好语法 + 块结构
定阶乘是函n：
  若n等1就1不然n乘阶乘算n减1。
结束

# 方案2：简洁语法（兼容）
定阶乘=函n若n等1则1否则n乘阶乘n减1。

# 方案3：隐式定义
阶乘等于函n：
  若n等1就1不然n乘阶乘算n减1。
结束
```

---

### 5.2 循环求和

**zhixing：**
```yan
定s是0。
对于i从1到5：
  设s是算s+i。
结束
s，印。
```

**newlisp/yan：**
```yan
定s=0。
遍历x于范围5：
  定s=加s x。
。
s，印。
```

**言律优化方案：**
```yan
# 方案1：友好语法
定总和是0。
对于i从1到5：
  设总和是算总和加i。
结束
印总和。

# 方案2：高阶函数
范围5，归加0，印。

# 方案3：隐式定义
总和等于0。
对于i从1到5：
  总和等于算总和加i。
结束
印总和。
```

---

### 5.3 因果链示例

**言律独有特性：**
```yan
# 智能家居控制
温度升高，风扇开启。
温度超过30度，风扇开启、空调开启。
温度升高、湿度升高，空调开启。

# 权限检查
用户为管理员，允许所有操作。
用户为经理：
  操作为读取、写入或删除：
    资源属于本部门，允许。
    资源不属于本部门，拒绝。
  其他操作，拒绝。
```

---

## 六、实现建议

### 6.1 编译器架构

```python
class YanLuCompiler:
    """言律编译器"""
    
    def __init__(self):
        # 预分词器（贪心匹配）
        self.pre_tokenizer = PreTokenizer()
        
        # 词法分析器（元数驱动）
        self.lexer = Lexer(VERB_ARITY)
        
        # 语法分析器（两遍解析）
        self.parser = Parser()
        
        # 语义分析器（上下文补全）
        self.semantic_analyzer = SemanticAnalyzer()
        
        # 代码生成器（多目标）
        self.code_generator = CodeGenerator()
    
    def compile(self, source_code):
        # 预分词
        tokens = self.pre_tokenizer.tokenize(source_code)
        
        # 词法分析
        tokens = self.lexer.analyze(tokens)
        
        # 第一遍：收集用户定义函数名
        user_functions = self.parser.collect_functions(tokens)
        
        # 第二遍：正常解析
        ast = self.parser.parse(tokens, user_functions)
        
        # 语义分析
        ast = self.semantic_analyzer.analyze(ast)
        
        # 代码生成
        target_code = self.code_generator.generate(ast)
        
        return target_code
```

---

### 6.2 关键技术点

**1. 元数驱动解析**
```python
def parse_call(tokens, pos):
    """解析函数调用"""
    verb = tokens[pos]
    arity = VERB_ARITY.get(verb.name, 0)
    
    if arity == 0:
        return Call(verb, []), pos + 1
    
    args = []
    current_pos = pos + 1
    
    if arity == -1:  # 可变参数
        while current_pos < len(tokens) and not is_verb(tokens[current_pos]):
            arg, current_pos = parse_term(tokens, current_pos)
            args.append(arg)
    else:  # 固定参数
        for _ in range(arity):
            arg, current_pos = parse_term(tokens, current_pos)
            args.append(arg)
    
    return Call(verb, args), current_pos
```

**2. 贪心最长匹配**
```python
def greedy_match(text, pos, keywords):
    """贪心最长匹配关键字"""
    best_match = None
    best_length = 0
    
    for keyword in keywords:
        if text[pos:pos+len(keyword)] == keyword:
            if len(keyword) > best_length:
                best_match = keyword
                best_length = len(keyword)
    
    return best_match
```

**3. 两遍解析**
```python
def parse_program(tokens):
    """两遍解析"""
    # 第一遍：收集用户定义函数名
    user_functions = set()
    for i, token in enumerate(tokens):
        if token.name == '定' and i + 2 < len(tokens):
            if tokens[i + 2].name == '函':
                user_functions.add(tokens[i + 1].name)
    
    # 第二遍：正常解析
    parser = Parser(user_functions)
    return parser.parse(tokens)
```

---

## 七、总结

### 7.1 核心优化点

1. **采用元数驱动解析** - 实现无空格分词
2. **引入友好语法** - 降低学习门槛
3. **百家姓变量命名** - 符合中文习惯
4. **隐式定义/赋值** - 减少关键字使用
5. **中文数字支持** - 符合中文习惯
6. **因果链语法** - 核心创新特性
7. **状态流语法** - 自然语言描述状态变化
8. **意合式函数调用** - 自然语言调用函数
9. **多轨制语法** - 支持多语言嵌入

### 7.2 实现路线

**阶段1（1个月）：**
- 元数驱动解析
- 友好语法
- 百家姓变量命名

**阶段2（1个月）：**
- 因果链语法
- 状态流语法
- 意合式函数调用

**阶段3（1个月）：**
- 隐式定义/赋值
- 中文数字支持
- 多轨制语法

**阶段4（持续）：**
- 自举验证
- 标准库开发
- 工具链完善

---

**版本历史：**

- v1.1 (2026-05-20) - 基于 zhixing + newlisp/yan 双项目对比的优化建议
- v1.0 (2026-05-20) - 初始版本

---

**版权声明：** 本文档遵循 CC 4.0 BY-SA 版权协议
