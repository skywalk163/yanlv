# 言律术语表

**版本：** 1.0  
**日期：** 2026-05-20

---

## A

### AST (Abstract Syntax Tree)

抽象语法树。编译器在语法分析阶段生成的树形结构，表示源代码的语法结构。

---

## C

### Causal Chain Grammar (因果链语法)

直接映射"事件-响应"的因果关系，无需if-else语法标记的语法结构。

**示例：**

```yan
温度升高，风扇开启。
```

### Context Manager (上下文管理器)

管理程序上下文的组件，负责跟踪主题链、补全省略元素。

### Context Omission Grammar (语境省略语法)

利用上下文语境，省略重复的语法元素的语法结构。

**示例：**

```yan
用户为管理员：
  显示管理面板。
  允许所有操作。    # 省略条件"用户为管理员"
```

---

## I

### Identifier (标识符)

用于命名变量、函数、类等程序元素的名称。

**命名规则：**

```
标识符 = 中文字符+ | 英文字母 (中文字符 | 英文字母 | 数字 | _)*
```

### Implicit Function Call (意合式函数调用)

通过语义关联传递参数，无需显式的参数列表和括号的函数调用方式。

**示例：**

```yan
用户、订单，计算折扣。
```

---

## L

### Lexer (词法分析器)

将源代码字符流转换为Token序列的编译器组件。

### Literal (字面量)

在源代码中直接表示固定值的符号。

**类型：**

- 数字字面量：`42`, `3.14`
- 字符串字面量：`「你好」`
- 布尔字面量：`真`, `假`
- 空值字面量：`空`

---

## M

### Multi-Track System (多轨制)

支持在中文代码中嵌入其他语言代码块的机制。

**支持的轨道：**

- 数学轨：`$(数学公式)`
- Python轨：`{{python: ... }}`
- JavaScript轨：`{{javascript: ... }}`
- SQL轨：`{{sql: ... }}`

---

## P

### Parser (语法分析器)

将Token序列转换为AST的编译器组件。

---

## S

### Semantic Analyzer (语义分析器)

检查AST语义正确性、进行类型检查和作用域解析的编译器组件。

### State Flow Grammar (状态流语法)

用自然语言描述状态变化，无需显式声明变量和赋值的语法结构。

**示例：**

```yan
交通信号灯：
  初始状态：红灯。
  
  计时超过30秒，状态变为绿灯。
```

### Symbol Table (符号表)

存储程序中所有符号（变量、函数、类等）信息的编译器数据结构。

---

## T

### Theme Chain (主题链)

上下文中持续有效的讨论对象，用于语境省略补全。

**示例：**

```yan
用户登录。          # 主题：用户
验证权限。          # 省略主语"系统对用户"
发送通知。          # 省略主语"系统向用户"
```

### Token

词法分析的基本单位，表示源代码中的一个语法元素。

**类型：**

- 关键词Token：`当`, `若`, `定义`
- 标识符Token：`温度`, `用户列表`
- 字面量Token：`42`, `「你好」`
- 标点符号Token：`。`, `，`, `：`
- 运算符Token：`+`, `-`, `*`

### Type Inference (类型推断)

编译器根据上下文自动推断表达式类型的机制。

**示例：**

```yan
定 温度 = 25。          # 推断为整数
定 价格 = 19.99。       # 推断为浮点数
定 名称 = 「张三」。     # 推断为字符串
```

---

## Y

### Yan Lü (言律)

本语言的名称，意为"语言的规律"或"言说的法则"。

### Yan Lü Zheng Yin Ti (言律正音体)

机器可解析的标准中文句式集合，确保代码的唯一映射和精确解析。

**语句类型：**

| 类型 | 结构模板 | 示例 |
|------|----------|------|
| 事件触发句 | 当 A，就 B。 | 当门开了，就开灯。 |
| 条件分支句 | 要是 A，就 B；否则 C。 | 要是太热，就开风扇；否则关机。 |
| 循环执行句 | 每隔 N [单位]，做 X。 | 每隔五分钟，查一次温度。 |
| 顺序执行句 | A 了。B 开了。 | 天黑了。灯亮了。 |
| 状态监听句 | 看到 A，立刻做 B。 | 看到陌生人，拍照上传。 |
| 延时动作句 | A 以后，做 B。 | 报警三秒后，拨打急救电话。 |

### Yi He Shi Programming (意合式编程)

不依赖显式语法标记，通过语义关联和语境推断的编程范式。

**核心原则：**

1. 无时态编程：状态变化由语境推断
2. 无类型转换：类型由上下文决定
3. 因果链表达：直接映射事件-响应
4. 语境省略：利用上下文简化表达

---

## 四

### 四字格模式 (Four-Character Pattern)

成语式编程单元，用四字成语表达常见的编程模式。

**模式库：**

| 模式名 | 句式 | 含义 |
|--------|------|------|
| 温升火急 | 温升火急，启扇降温 | 温度升高立即散热 |
| 数据到位 | 数据到位，立刻入库 | 数据到达即存储 |
| 缓存待发 | 网络断连，缓存待发 | 断网时暂存数据 |
| 降级保底 | 服务异常，降级保底 | 故障时启用备用方案 |
| 削峰填谷 | 高峰限流，闲时补工 | 负载均衡策略 |
| 守株待兔 | 守株待兔，人来灯亮 | 静态等待触发 |

---

## 元

### 元数驱动解析 (Arity-Driven Parsing)

动词声明参数数量，解析器根据元数自动收集参数的解析机制。

**示例：**

```yan
印斐波那契15。
```

**解析过程：**

1. 识别"印"是动词（元数=1）
2. 收集1个参数 → "斐波那契15"
3. 递归解析参数 → 斐波那契(15)
4. 生成调用 → 印(斐波那契(15))

---

## 作

### 作用域 (Scope)

标识符在程序中可见的区域。

**类型：**

| 作用域类型 | 创建时机 | 可见性 |
|-----------|---------|--------|
| 全局作用域 | 程序启动 | 整个程序 |
| 模块作用域 | 模块导入 | 模块内部 |
| 函数作用域 | 函数调用 | 函数内部 |
| 块作用域 | 作用域语法 | 块内部 |

### 作用域语法 (Scope Syntax)

使用"主题的时候："格式定义局部作用域的语法结构。

**示例：**

```yan
回家的时候：
  门锁开。
  灯自动亮。
  播放欢迎语。
```

---

## 流

### 流水句因果链 (Flowing Sentence Causal Chain)

相邻句子之间存在潜在因果关系，可跨句累积条件的语法结构。

**示例：**

```yan
天黑了。
没人在家。
窗帘关闭。
夜灯微亮。
```

**解析为：**

```python
if is_night() and not someone_at_home():
    curtain.close()
    night_light.turn_on(dim=20%)
```

---

## 认

### 认知科学基础 (Cognitive Science Foundation)

言律语言设计的理论基础，基于语言相对论和中文深层认知特性。

**核心发现：**

- 中文处理减少了3个认知步骤
- 认知延迟降低约40%
- 工作记忆负担减轻

---

## 参

### 参考资料 (References)

1. Feng XY, Fu RX, Shi L et al. An Overview of Cangjie Programming Language. JCST, 2026.
2. wenyan-lang学术论文：文言编程语言的理论基础和研究价值. CSDN博客, 2026.
3. 中文编程语言深度研究报告：全景回顾与未来展望. CSDN博客, 2026.
4. 言律（Yán Lǜ）中文原生编程语言开发计划书（v1.0）

---

**版本历史：**

- v1.0 (2026-05-20) - 初始版本

---

**版权声明：** 本文档遵循 CC 4.0 BY-SA 版权协议
