# 言律语言规范 v1.2

**版本：** 1.2  
**状态：** 草案  
**日期：** 2026-05-21  
**基于：** zhixing + newlisp/yan 双项目对比优化

---

## 1. 引言

### 1.1 设计目标

言律（Yán Lǜ）是一门基于中文深层认知特性的编程语言，融合了两个成熟项目的优势：

**来自 zhixing 的优势：**
- 友好语法设计（`结束`、`就`、`不然`、`是`、`算`）
- 百家姓变量命名（张三、李四、王小明）
- 隐式定义/赋值（减少关键字使用）
- 中文数字支持（十、二十五、十万）

**来自 newlisp/yan 的优势：**
- 元数驱动解析（实现无空格分词）
- 两遍解析机制（支持递归函数）
- 管道操作（`，` 数据流动）
- 自举成功验证（1908行纯言语言代码）

**言律的核心目标：**
1. **降低认知负担** - 利用母语优势，减少语言转换开销
2. **提高表达效率** - 通过意合式语法，简化代码编写
3. **支持多范式** - 融合函数式、面向对象、响应式编程
4. **实现多轨制** - 中文+数学+多语言融合
5. **无空格分词** - 符合中文书写习惯

### 1.2 核心原则

**原则1：元数驱动解析**

动词声明参数数量，解析器自动收集参数，实现无空格分词。

**原则2：意合式编程**

不依赖显式语法标记，通过语义关联和语境推断。

**原则3：因果链表达**

直接映射"事件-响应"的因果关系，无需if-else语法标记。

**原则4：语境省略**

利用上下文语境，省略重复的语法元素。

**原则5：状态流**

用自然语言描述状态变化，无需显式声明变量和赋值。

**原则6：友好语法优先**

优先使用符合中文习惯的语法（`结束`、`就`、`不然`、`是`、`算`）。

---

## 2. 词法规范

### 2.1 字符集

言律使用以下字符集：

**基本字符：**
- 中文字符（U+4E00 - U+9FFF）
- 英文字母（A-Z, a-z）
- 数字（0-9）
- 下划线（_）

**标点符号：**
- `。` - 句号（语句终止符）
- `，` - 逗号（逻辑块内停顿 / 管道操作符）
- `；` - 分号（并列分隔符）
- `：` - 冒号（作用域入口标记）
- `、` - 顿号（参数分隔符）
- `'` `'` - 单引号（字符串字面量）
- `"` `"` - 双引号（字符串字面量，支持转义）
- `！` - 叹号（断言/强调）
- `？` - 问号（条件判断）
- `《》` - 书名号（模块/文件名）
- `……` - 省略号（可变参数/范围）
- `——` - 破折号（标签/标记）
- `～` - 波浪号（范围/约数）
- `·` - 间隔号（属性访问）
- `【】` - 方括号（索引/标注）
- `①②③` - 圆圈号（步骤标记）

**数学符号：**
- `+` `-` `*` `/` `%` - 算术运算符
- `=` `≠` `<` `>` `≤` `≥` - 比较运算符
- `∧` `∨` `¬` - 逻辑运算符
- `(` `)` `[` `]` `{` `}` - 分组符号

**代码块标记：**
- `{{` `}}` - 多语言代码块

### 2.2 元数驱动解析

**核心机制：** 动词声明自己的元数（arity），解析器根据元数自动收集参数，实现无空格分词。

**元数类型：**
- `arity = 0`：零参数动词（如 `日期`）
- `arity = 1`：单参数动词（如 `首` `余` `长` `印`）
- `arity = 2`：双参数动词（如 `加` `减` `乘` `除`）
- `arity = 3`：三参数动词（如 `归`）
- `arity = -1`：可变参数（如 `列` `典`）

**动词元数表：**

```python
VERB_ARITY = {
    # 算术运算
    '加': 2, '减': 2, '乘': 2, '除': 2, '模': 2, '幂': 2,
    '负': 1, '绝对': 1,
    
    # 比较运算
    '大': 2, '小': 2, '等': 2, '不等': 2,
    
    # 逻辑运算
    '且': 2, '或': 2, '非': 1,
    
    # 列表操作
    '列': -1,  # 可变参数
    '首': 1, '余': 1, '入': 2, '长': 1, '添': 2, '连': 2, '含': 2,
    
    # 高阶函数
    '皆': 2, '只': 2, '归': 3,
    
    # I/O操作
    '印': 1, '读': 1, '写': 2, '行': 1,
}
```

**解析示例：**

```yan
# 无空格书写
印斐波那契15。
列1 2 3皆乘2。

# 解析过程
印(斐波那契(15))
皆(乘(2), 列(1, 2, 3))
```

**解析规则：**
1. 遇到动词 → 创建 Call 节点，开始收集参数
2. 根据元数决定何时停止收集：
   - `arity > 0`：收集够指定数量的参数后停止
   - `arity = -1`：持续收集直到遇到下一个动词或结构符
3. 遇到新动词 → 当前 Call 完成，开始新的 Call

### 2.3 缩进规则

**核心原则：** 使用缩进定义代码块，无需显式的"结束"标记。

**缩进规则：**
1. **缩进增加** → 进入新的代码块
2. **缩进减少** → 结束当前代码块
3. **相同缩进** → 同一层次的语句

**示例：**
```yan
# 单层块
对于i从1到5：
  印i。           # 缩进增加，进入循环块
                   # 缩进减少，自动结束循环块

# 多层嵌套
对于i从1到3：
  对于j从1到2：
    印i、j。       # 两层缩进，在两个循环内
                   # 缩进减少一次，结束内层循环
                   # 缩进再减少一次，结束外层循环
```

**单行语句：**
```yan
# 单行条件
若x大0就印「正数」。

# 单行循环
对于i从1到5：印i。

# 单行函数
定平方是函x：返回x乘x。
```

### 2.4 关键词

#### 2.4.1 控制关键词

| 关键词 | 类型 | 功能 | 友好语法 |
|--------|------|------|----------|
| 当、若、只要、一旦 | 条件触发 | 引入事件监听 | - |
| 就 | 动作启动 | 触发后续动作 | ✅ 推荐 |
| 要是、如果 | 条件判断 | 开启 if 分支 | - |
| 否则、不然 | 分支切换 | else 分支 | ✅ `不然` 推荐 |
| 每隔、每 | 时间循环 | 定时重复 | - |
| 以后、之后 | 延迟执行 | after 操作 | - |
| 一直、持续 | 循环监测 | while 监听 | - |

#### 2.4.2 感知关键词

| 关键词 | 类型 | 功能 |
|--------|------|------|
| 看到、发现、检测到 | 感知输入 | sensor 输入 |
| 立刻、马上、立即 | 实时响应 | 强调低延迟 |

#### 2.4.3 定义关键词

| 关键词 | 类型 | 功能 | 友好语法 |
|--------|------|------|----------|
| 定 | 变量定义 | 定义变量 | - |
| 设 | 变量赋值 | 修改变量 | - |
| 是 | 变量赋值 | 定义/赋值 | ✅ 推荐 |
| 等于 | 变量赋值 | 定义/赋值 | ✅ 推荐 |
| 定义 | 实体定义 | 定义类或模块 | - |
| 初始状态 | 状态初始化 | 设置初始值 | - |
| 状态变为 | 状态转换 | 改变状态 | - |
| 状态翻转 | 状态切换 | 布尔翻转 | - |
| 状态为 | 状态检查 | 条件判断 | - |

#### 2.4.4 函数关键词

| 关键词 | 类型 | 功能 |
|--------|------|------|
| 函 | 函数定义 | 定义函数 |
| 返回 | 返回值 | 函数返回 |
| 导入 | 模块导入 | 导入模块 |
| 导出 | 模块导出 | 导出符号 |

#### 2.4.5 数学关键词

| 关键词 | 类型 | 功能 | 友好语法 |
|--------|------|------|----------|
| 算 | 数学表达式 | 计算数学公式 | ✅ 推荐 |
| $() | 数学表达式 | 计算数学公式 | 兼容 |

### 2.4 标识符

#### 2.4.1 百家姓变量命名

**命名规则：** 变量名以百家姓中的姓氏开头，支持1-4字中文名字。

**百家姓表格（前20个）：**
赵、钱、孙、李、周、吴、郑、王、冯、陈、褚、卫、蒋、沈、韩、杨、朱、秦、尤、许

**冲突姓氏列表：**
| 姓氏 | 冲突关键字 | 说明 |
|------|------------|------|
| 空 | `空` | 空值关键字 |
| 言 | `言` | 语言名称 |
| 印 | `印` | 内置动词 |

**示例：**
```yan
# 百家姓变量
定张三=10。
定李四=20。
张三加李四。        -- 30

# 复姓支持
定欧阳修=100。
欧阳修加50。        -- 150

# 冲突姓氏（不能作为变量名）
定空五=10。         -- 错误：以冲突姓氏"空"开头
```

#### 2.4.2 传统标识符

**命名规则：**

```
标识符 = 中文字符+ | 英文字母 (中文字符 | 英文字母 | 数字 | _)*
```

**示例：**

```yan
温度          # 纯中文标识符
user_name     # 英文标识符
用户ID        # 中英混合标识符
```

**命名约定：**

1. **变量**：使用名词，如 `温度`、`用户列表`
2. **函数**：使用动词，如 `计算折扣`、`发送消息`
3. **类**：使用名词短语，如 `用户管理器`、`订单处理器`
4. **常量**：使用全大写或特定前缀，如 `最大温度`、`默认超时`

### 2.5 字面量

#### 2.5.1 数字字面量

```yan
42              # 整数
3.14            # 浮点数
1_000_000       # 下划线分隔的大数
0xFF            # 十六进制
0b1010          # 二进制
```

#### 2.5.2 中文数字字面量

言律支持中文数字与阿拉伯数字混用：

```yan
十              # 10
二十五          # 25
一百二十三      # 123
十万            # 100000
```

**中文数字映射表：**
```python
CHINESE_NUMBERS = {
    '零': 0, '一': 1, '二': 2, '三': 3, '四': 4,
    '五': 5, '六': 6, '七': 7, '八': 8, '九': 9,
    '十': 10, '百': 100, '千': 1000, '万': 10000,
}
```

**示例：**
```yan
张三等于十。        # 等价于 "张三=10"
李四等于二十五。    # 等价于 "李四=25"
结果等于五加三。    # 等价于 "结果=5加3"
```

#### 2.5.3 字符串字面量

**单引号字符串：**
```yan
'你好，世界'          # 单行字符串
'Hello World'        # 英文字符串
```

**双引号字符串（支持转义）：**
```yan
"你好，世界"          # 单行字符串
"第一行\n第二行"      # 支持转义字符
"引号：\"嵌套\""      # 支持引号转义
```

**多行字符串：**
```yan
"这是
多行
字符串"
```

**转义字符：**
```yan
\n      # 换行
\t      # 制表符
\\      # 反斜杠
\"      # 双引号
\'      # 单引号
```

#### 2.5.4 布尔字面量

```yan
真
假
```

#### 2.5.5 空值

```yan
空
```

### 2.6 注释

```yan
# 这是单行注释

##
这是
多行注释
##
```

### 2.7 隐式定义/赋值

**规则：**
1. **隐式定义**：变量首次出现且以 `=` 或 `等于` 赋值时，视为定义（等价于 `定`）
2. **隐式赋值**：变量已存在时，再次赋值视为修改（等价于 `设`）
3. **显式指定**：`定` 和 `设` 可用于明确指定操作类型，消除歧义

**示例：**
```yan
# 隐式定义
张三等于10。        # 等价于 "定张三=10"
李四等于20。        # 等价于 "定李四=20"

# 隐式赋值
张三等于张三加1。   # 等价于 "设张三=张三加1"

# 显式指定（消除歧义）
定王五=20。         # 明确定义
设王五=王五加1。    # 明确赋值

# 使用"是"（友好语法）
张三是10。          # 等价于 "定张三=10"
李四是"你好"。      # 等价于 "定李四="你好""
```

---

## 3. 语法规范

### 3.1 语句类型

#### 3.1.1 因果链语句

**语法：**

```
因果链语句 = 条件表达式 '，' 动作列表 '。'
动作列表 = 动作 ('、' 动作)*
```

**示例：**

```yan
温度升高，风扇开启。
温度超过30度，风扇开启、空调开启。
```

**语义：**

当条件表达式为真时，执行动作列表中的所有动作。

#### 3.1.2 条件分支语句

**语法：**

```
条件分支语句 = '要是' 条件表达式 '，' 动作列表 ('；' '否则' 动作列表)? '。'
```

**示例：**

```yan
要是温度超过30度，开启空调；否则关闭空调。
要是用户已登录，显示欢迎信息；否则跳转到登录页。
```

**语义：**

如果条件表达式为真，执行第一个动作列表；否则执行第二个动作列表。

#### 3.1.3 循环语句

**语法：**

```
循环语句 = '每隔' 时间表达式 '，' 动作列表 '。'
时间表达式 = 数字 时间单位
时间单位 = '秒' | '分钟' | '小时' | '天'
```

**示例：**

```yan
每隔5秒，检查温度。
每隔10分钟，同步数据。
```

**语义：**

按照指定的时间间隔，重复执行动作列表。

#### 3.1.4 状态转换语句

**语法：**

```
状态转换语句 = 触发条件 '，' '状态变为' 新状态值 '。'
状态翻转语句 = 触发条件 '，' '状态翻转' '。'
```

**示例：**

```yan
温度超过30度，状态变为过热。
开关按下，状态翻转。
```

**语义：**

当触发条件满足时，改变实体的状态。

#### 3.1.5 意合式函数调用

**语法：**

```
意合式调用 = 参数列表 '，' 函数名 '。'
参数列表 = 参数 ('、' 参数)*
参数 = 表达式 | 命名参数
命名参数 = 参数名 '为' 参数值
```

**示例：**

```yan
用户、订单，计算折扣。
用户ID为123、订单金额为500，创建订单。
```

**语义：**

将参数列表传递给指定函数并执行。

### 3.2 表达式

#### 3.2.1 算术表达式

**语法：**

```
算术表达式 = 表达式 (('+' | '-' | '*' | '/' | '%') 表达式)*
```

**示例：**

```yan
温度 + 5
总价 * 0.9
(基础价格 + 运费) * 折扣率
```

#### 3.2.2 比较表达式

**语法：**

```
比较表达式 = 表达式 (('=' | '≠' | '<' | '>' | '≤' | '≥') 表达式)*
```

**示例：**

```yan
温度 > 30
用户年龄 ≥ 18
输入密码 = 存储密码
```

#### 3.2.3 逻辑表达式

**语法：**

```
逻辑表达式 = 表达式 (('且' | '或' | '¬') 表达式)*
```

**示例：**

```yan
温度 > 30 且 湿度 > 60
用户已登录 或 拥有访客权限
¬ 系统维护中
```

#### 3.2.4 数学表达式（数学轨）

**语法：**

```
数学表达式 = '$(' 数学公式 ')'
```

**示例：**

```yan
定 面积 = $(π * r²)。
定 距离 = $(√((x₂-x₁)² + (y₂-y₁)²))。
```

**语义：**

数学表达式直接嵌入数学公式，编译器负责解析和计算。

### 3.3 作用域

#### 3.3.1 作用域语法

**语法：**

```
作用域块 = 主题 '的时候：' 语句列表
```

**示例：**

```yan
回家的时候：
  门锁开。
  灯自动亮。
  播放欢迎语。
```

**语义：**

当主题事件发生时，执行语句列表中的所有语句。

#### 3.3.2 作用域嵌套

**示例：**

```yan
用户登录的时候：
  验证权限：
    权限为管理员，显示管理面板。
    权限为普通用户，显示用户面板。
  发送登录通知。
```

### 3.4 流水句因果链

**语法规则：**

1. 相邻句子之间存在潜在因果关系
2. 前句为感知/状态变化，后句为响应动作
3. 可跨句累积条件
4. 支持最多5个连续流水句组成一个逻辑单元

**示例：**

```yan
天黑了。
没人在家。
窗帘关闭。
夜灯微亮。
```

**解析逻辑：**

```
"天黑了" → 触发环境判断
"没人在家" → 追加条件过滤
"窗帘关闭""夜灯微亮" → 执行动作组
```

**编译结果：**

```python
if is_night() and not someone_at_home():
    curtain.close()
    night_light.turn_on(dim=20%)
```

---

## 4. 类型系统

### 4.1 基本类型

| 类型名 | 描述 | 示例 |
|--------|------|------|
| 整数 | 整数类型 | 42, -17, 0xFF |
| 浮点数 | 浮点数类型 | 3.14, -0.001 |
| 字符串 | 文本类型 | 「你好」 |
| 布尔 | 布尔类型 | 真, 假 |
| 空 | 空值类型 | 空 |

### 4.2 复合类型

#### 4.2.1 列表

**语法：**

```yan
定 数字列表 = [1, 2, 3, 4, 5]。
定 字符串列表 = [「苹果」, 「香蕉」, 「橙子」]。
```

#### 4.2.2 字典

**语法：**

```yan
定 用户信息 = {
  「姓名」: 「张三」,
  「年龄」: 25,
  「邮箱」: 「zhangsan@example.com」
}。
```

#### 4.2.3 元组

**语法：**

```yan
定 坐标 = (10, 20)。
定 RGB颜色 = (255, 128, 0)。
```

### 4.3 类型推断

言律支持类型推断，无需显式声明类型：

```yan
定 温度 = 25。          # 推断为整数
定 价格 = 19.99。       # 推断为浮点数
定 名称 = 「张三」。     # 推断为字符串
定 已登录 = 真。        # 推断为布尔
```

### 4.4 类型转换

**显式转换：**

```yan
定 字符串数字 = 「123」。
定 数字 = 字符串数字转整数。

定 浮点数 = 3.14。
定 整数 = 浮点数转整数。
```

---

## 5. 函数

### 5.1 函数定义

**语法：**

```yan
定 函数名 = 函 参数列表：
  函数体。
  返回 返回值。
```

**示例：**

```yan
定 计算折扣 = 函 用户、订单：
  定 基础折扣 = 0.1。
  定 会员折扣 = 用户会员等级 * 0.05。
  定 总折扣 = 基础折扣 + 会员折扣。
  返回 订单金额 * 总折扣。
```

### 5.2 函数调用

**意合式调用：**

```yan
用户、订单，计算折扣。
```

**传统调用（可选）：**

```yan
计算折扣(用户, 订单)。
```

### 5.3 高阶函数

**语法：**

```yan
定 高阶函数名 = 函 函数参数、其他参数：
  函数体。
```

**示例：**

```yan
定 应用到每个元素 = 函 列表、转换函数：
  定 结果 = []。
  遍历 元素 于 列表：
    定 新元素 = 元素，转换函数。
    新元素，结果入。
  。
  返回 结果。
```

---

## 6. 模块系统

### 6.1 模块定义

**语法：**

```yan
定义 模块名：
  导出 符号1。
  导出 符号2。
  
  # 模块内容
  定 符号1 = ...。
  定 符号2 = ...。
```

### 6.2 模块导入

**语法：**

```yan
导入 模块名。
从 模块名 导入 符号1、符号2。
```

**示例：**

```yan
导入 数学库。
从 文件库 导入 读文件、写文件。

定 圆面积 = 数学库圆周率 * 半径²。
定 内容 = 「data.txt」，读文件。
```

---

## 7. 多轨制

### 7.1 数学轨

**语法：**

```yan
定 表达式 = $(数学公式)。
```

**示例：**

```yan
定 圆面积 = $(π * r²)。
定 勾股定理 = $(√(a² + b²))。
定 二次方程根 = $((-b ± √(b²-4ac)) / 2a)。
```

### 7.2 Python轨

**语法：**

```yan
定 变量名 = {{python:
Python代码
}}。
```

**示例：**

```yan
定 数据分析 = {{python:
import pandas as pd
import numpy as np

df = pd.read_csv('data.csv')
result = df.groupby('category').agg({
    'value': ['mean', 'std', 'count']
})

return result.to_dict()
}}。
```

### 7.3 JavaScript轨

**语法：**

```yan
定 变量名 = {{javascript:
JavaScript代码
}}。
```

**示例：**

```yan
定 前端组件 = {{javascript:
class Component {
  constructor(props) {
    this.props = props;
  }
  
  render() {
    return `<div>${this.props.title}</div>`;
  }
}

return new Component({title: '你好'});
}}。
```

### 7.4 SQL轨

**语法：**

```yan
定 变量名 = {{sql:
SQL查询语句
}}。
```

**示例：**

```yan
定 用户订单 = {{sql:
SELECT 
  u.user_id,
  u.username,
  COUNT(o.order_id) as order_count,
  SUM(o.amount) as total_amount
FROM users u
LEFT JOIN orders o ON u.user_id = o.user_id
WHERE u.created_at > '2024-01-01'
GROUP BY u.user_id, u.username
HAVING total_amount > 1000
ORDER BY total_amount DESC
LIMIT 10;
}}。
```

---

## 8. 错误处理

### 8.1 错误类型

| 错误类型 | 描述 | 示例 |
|---------|------|------|
| 词法错误 | 非法字符或标识符 | 未闭合的字符串 |
| 语法错误 | 语法结构不正确 | 缺少句号 |
| 语义错误 | 类型不匹配或未定义符号 | 未定义的变量 |
| 运行时错误 | 执行时错误 | 除零错误 |

### 8.2 错误消息格式

```
错误 [错误代码] 在 文件名:行号:列号
  错误描述
  
  源代码片段
  ^^^^^ 指示位置
  
  建议：修复建议
```

**示例：**

```
错误 [E201] 在 示例.yan:15:12
  未定义的变量「用户ID」
  
  用户ID、订单，计算折扣。
  ^^^^^^ 指示位置
  
  建议：您是否想使用「用户ID」？如果是，请先定义它。
```

---

## 9. 标准库

### 9.1 核心模块

| 模块名 | 功能 | 主要函数 |
|--------|------|----------|
| 数学库 | 数学运算 | 正弦、余弦、开方、幂 |
| 字符串库 | 字符串操作 | 长度、连接、分割、替换 |
| 列表库 | 列表操作 | 排序、过滤、映射、归约 |
| 文件库 | 文件操作 | 读文件、写文件、列目录 |
| 网络库 | 网络请求 | HTTP请求、下载、上传 |
| 时间库 | 时间处理 | 当前时间、格式化、解析 |

### 9.2 示例

```yan
导入 数学库。
导入 字符串库。

定 圆周率 = 数学库圆周率。
定 文本长度 = 「你好，世界」，字符串库长度。
```

---

## 10. 附录

### 10.1 完整语法参考

参见 `GRAMMAR.md`。

### 10.2 编译器实现

参见 `COMPILER.md`。

### 10.3 示例代码

参见 `examples/` 目录。

---

**版本历史：**

- v1.0 (2026-05-20) - 初始版本

---

**版权声明：** 本规范遵循 CC 4.0 BY-SA 版权协议
