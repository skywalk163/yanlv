# 言律语言模块系统设计

## 概述

为言律语言添加模块系统，支持模块导入/导出、命名空间管理和标准库模块。

---

## 语法设计

### 1. 模块定义

#### 基本语法

```yan
定义模块 模块名
    模块内容
结束模块
```

#### 示例

```yan
定义模块 数学工具
    定义函数 平方 参数 x
        返回 x 乘以 x
    结束
    
    定义函数 立方 参数 x
        返回 x 乘以 x 乘以 x
    结束
结束模块
```

---

### 2. 模块导出

#### 导出特定函数

```yan
导出 平方
导出 立方
```

#### 导出所有

```yan
导出 所有
```

#### 导出为别名

```yan
导出 平方 为 求平方
```

---

### 3. 模块导入

#### 导入整个模块

```yan
导入 "数学工具"
```

#### 导入特定函数

```yan
从 "数学工具" 导入 平方
从 "数学工具" 导入 平方, 立方
```

#### 导入为别名

```yan
从 "数学工具" 导入 平方 为 求平方
```

#### 使用模块前缀

```yan
导入 "数学工具" 为 数学
调用 数学.平方 参数 5
```

---

### 4. 标准库模块

言律语言提供以下标准库模块：

#### 4.1 数学模块 (math)
```yan
从 "标准库/数学" 导入 正弦, 余弦, 正切
从 "标准库/数学" 导入 平方根, 幂
```

#### 4.2 字符串模块 (string)
```yan
从 "标准库/字符串" 导入 连接, 分割, 替换
从 "标准库/字符串" 导入 大写, 小写, 去空格
```

#### 4.3 数组模块 (array)
```yan
从 "标准库/数组" 导入 排序, 反转, 过滤
从 "标准库/数组" 导入 映射, 归约
```

#### 4.4 文件模块 (file)
```yan
从 "标准库/文件" 导入 读取, 写入, 追加
从 "标准库/文件" 导入 存在, 大小
```

#### 4.5 时间模块 (time)
```yan
从 "标准库/时间" 导入 当前时间, 时间戳
从 "标准库/时间" 导入 格式化时间
```

#### 4.6 随机模块 (random)
```yan
从 "标准库/随机" 导入 随机数, 随机整数
从 "标准库/随机" 导入 随机选择, 打乱
```

---

## TokenType设计

```python
# 模块系统
MODULE = "MODULE"          # 模块
IMPORT = "IMPORT"          # 导入
EXPORT = "EXPORT"          # 导出
FROM = "FROM"              # 从
AS = "AS"                  # 为
NAMESPACE = "NAMESPACE"    # 命名空间
```

---

## 关键词映射

```python
'定义模块': TokenType.MODULE,
'导入': TokenType.IMPORT,
'导出': TokenType.EXPORT,
'从': TokenType.FROM,
'为': TokenType.AS,
'命名空间': TokenType.NAMESPACE,
'结束模块': TokenType.END_MODULE,
```

---

## 实现架构

### 1. 模块管理器 (ModuleManager)

```python
class ModuleManager:
    def __init__(self):
        self.modules = {}          # 已加载模块
        self.namespaces = {}       # 命名空间
        self.stdlib_path = None    # 标准库路径
    
    def load_module(self, module_name):
        """加载模块"""
        pass
    
    def import_from(self, module_name, items):
        """从模块导入"""
        pass
    
    def export(self, items):
        """导出项目"""
        pass
```

### 2. 模块对象 (Module)

```python
class Module:
    def __init__(self, name):
        self.name = name           # 模块名
        self.functions = {}        # 函数
        self.variables = {}        # 变量
        self.exports = []          # 导出列表
```

### 3. 命名空间 (Namespace)

```python
class Namespace:
    def __init__(self, name):
        self.name = name           # 命名空间名
        self.symbols = {}          # 符号表
        self.parent = None         # 父命名空间
```

---

## 标准库结构

```
stdlib/
├── 数学.yan          # math module
├── 字符串.yan        # string module
├── 数组.yan          # array module
├── 文件.yan          # file module
├── 时间.yan          # time module
├── 随机.yan          # random module
└── 系统.yan          # system module
```

---

## 使用示例

### 示例1：创建和使用模块

**模块文件：数学工具.yan**
```yan
定义模块 数学工具
    定义函数 平方 参数 x
        返回 x 乘以 x
    结束
    
    定义函数 立方 参数 x
        返回 x 乘以 x 乘以 x
    结束
    
    导出 平方
    导出 立方
结束模块
```

**主程序：**
```yan
从 "数学工具" 导入 平方, 立方

定义变量a为 5
定义变量结果1为 调用 平方 参数 a
定义变量结果2为 调用 立方 参数 a

输出 结果1
输出 结果2
```

---

### 示例2：使用标准库

```yan
从 "标准库/数学" 导入 正弦, 余弦

定义变量角度为 3.14159 除以 2
定义变量结果为 调用 正弦 参数 角度

输出 结果
```

---

### 示例3：命名空间

```yan
导入 "数学工具" 为 数学

定义变量x为 10
定义变量结果为 调用 数学.平方 参数 x

输出 结果
```

---

## 实现要点

### 1. 模块加载
- 支持相对路径和绝对路径
- 支持标准库路径
- 模块缓存机制
- 循环导入检测

### 2. 命名空间管理
- 符号查找规则
- 作用域链
- 命名冲突处理

### 3. 导入导出
- 按需导入
- 别名支持
- 批量导入
- 默认导出

---

## 版本信息

- **设计版本**：v1.0
- **设计日期**：2026-05-25
- **状态**：设计完成，待实现
