# 言律语言混合模式语句结束策略说明

## 概述

为了提高代码的可读性和编写效率，言律语言实现了**混合模式的语句结束策略**，允许用户选择性地省略句号，同时保持代码结构清晰。

---

## 策略规则

### 1. 句号（。）- 强制语句结束

**规则：** 当一行以句号结尾时，强制结束当前语句。

**示例：**
```yan
定义变量x为10。
输出x。
```

**说明：** 每个语句都以句号明确结束，这是最传统和清晰的方式。

---

### 2. 换行（\\n）- 智能处理

换行符的处理根据缩进关系智能判断：

#### 2.1 进入子块

**规则：** 如果下一行缩进 > 当前行，进入子块。

**示例：**
```yan
如果条件成立则
    输出"条件为真"
    输出"执行操作"
结束
```

**说明：** 缩进增加表示进入子块（如if语句的执行体），当前行自然结束。

---

#### 2.2 退出当前块

**规则：** 如果下一行缩进 < 当前行，退出当前块。

**示例：**
```yan
如果条件成立则
    输出"条件为真"
结束
输出"程序继续"
```

**说明：** 缩进减少表示退出当前块，当前行自然结束。

---

#### 2.3 续行拼接

**规则：** 如果下一行缩进 == 当前行，且当前行不以句号结尾，则视为续行。

**示例：**
```yan
定义变量x为10
输出x
```

**处理结果：**
```yan
定义变量x为10 输出x
```

**说明：** 两行被拼接成一行，中间用空格连接。这允许长语句分行书写，提高可读性。

---

#### 2.4 新语句开始

**规则：** 如果下一行缩进 == 当前行，且当前行以句号结尾，则开始新语句。

**示例：**
```yan
定义变量x为10。
定义变量y为20。
```

**说明：** 句号明确表示语句结束，下一行开始新语句。

---

## 使用示例

### 示例1：基本使用

```yan
# 使用句号明确结束
定义变量x为10。
定义变量y为20。
输出x。
输出y。
```

### 示例2：省略句号

```yan
# 省略句号，自动拼接
定义变量x为10
定义变量y为20
输出x
输出y
```

**处理结果：**
```yan
定义变量x为10 定义变量y为20 输出x 输出y
```

### 示例3：混合使用

```yan
# 混合使用句号和省略
定义变量x为10。
定义变量y为20
输出x
输出y。
```

**处理结果：**
```yan
定义变量x为10。
定义变量y为20 输出x
输出y。
```

### 示例4：条件语句

```yan
如果x大于10则
    输出"x大于10"
    输出x。
结束
输出"程序结束"。
```

**说明：** 缩进块内的语句可以省略句号，块结构通过缩进清晰表达。

### 示例5：嵌套块

```yan
如果条件1成立则
    如果条件2成立则
        输出"嵌套条件"
        输出"执行操作"
    结束
结束
```

**说明：** 多层嵌套通过缩进清晰表达，无需句号。

---

## 实现细节

### 处理流程

1. **读取源代码**：将源代码按行分割
2. **逐行处理**：
   - 计算当前行缩进
   - 检查是否以句号结尾
   - 比较下一行缩进
   - 根据规则决定：结束语句、进入子块、退出块、或续行拼接
3. **输出处理结果**：返回处理后的源代码

### 核心算法

```python
def process_source(source_code: str) -> str:
    lines = source_code.split('\n')
    processed_lines = []

    i = 0
    while i < len(lines):
        current_line = lines[i]
        current_indent = get_indent(current_line)
        ends_with_period = current_line.strip().endswith('。')

        if i + 1 < len(lines):
            next_line = lines[i + 1]
            next_indent = get_indent(next_line)

            if len(next_indent) > len(current_indent):
                # 进入子块
                processed_lines.append(current_line)
                i += 1
            elif len(next_indent) < len(current_indent):
                # 退出块
                processed_lines.append(current_line)
                i += 1
            else:
                # 缩进相同
                if ends_with_period:
                    # 新语句
                    processed_lines.append(current_line)
                    i += 1
                else:
                    # 续行拼接
                    merged_line = current_line + ' ' + next_line.strip()
                    lines[i] = merged_line
                    continue
        else:
            processed_lines.append(current_line)
            i += 1

    return '\n'.join(processed_lines)
```

---

## 优势

### 1. 灵活性
- 可以选择使用句号明确结束
- 也可以省略句号，让系统自动处理
- 两种方式可以混合使用

### 2. 可读性
- 缩进清晰表达代码结构
- 长语句可以分行书写
- 减少不必要的标点符号

### 3. 兼容性
- 完全兼容传统句号结束方式
- 不影响现有代码
- 渐进式采用新特性

---

## 注意事项

### 1. 缩进一致性
- 使用统一的缩进（建议4个空格）
- 不要混用空格和制表符
- 保持缩进层次清晰

### 2. 句号使用
- 需要明确结束语句时使用句号
- 避免歧义时使用句号
- 长语句分行时可以省略句号

### 3. 块结构
- 块的开始和结束要缩进一致
- 嵌套块要逐层增加缩进
- 避免过深的嵌套

---

## 版本信息

- **版本**：v2.2.0
- **实现日期**：2026-05-25
- **状态**：已实现并可用

---

## 后续改进

计划中的改进：
1. 支持更多语句结束标记（如分号）
2. 智能检测语句完整性
3. 提供代码格式化工具
4. 集成到IDE中提供实时提示
