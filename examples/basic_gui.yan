# 基础GUI示例
# 展示如何用言律语言创建图形用户界面

# ===== 导入GUI模块 =====

导入《图形界面》。

# ===== 窗口创建 =====

# 创建主窗口
定创建主窗口是函标题、宽度、高度：
  定窗口是图形界面，创建窗口：
    标题是标题
    宽度是宽度
    高度是高度
    背景色是"#FFFFFF"
    可调整大小是真。
  
  印"已创建窗口："加标题。
  回窗口。

# 设置窗口属性
定设置窗口属性是函窗口、属性字典：
  对于键于属性字典，键列表：
    定值是属性字典算键。
    窗口，设置属性(键、值)。
  
  印"已设置窗口属性"。

# 显示窗口
定显示窗口是函窗口：
  窗口，显示。
  印"窗口已显示"。

# 关闭窗口
定关闭窗口是函窗口：
  窗口，关闭。
  印"窗口已关闭"。

# ===== 控件创建 =====

# 创建标签
定创建标签是函父容器、文本、样式：
  定标签是图形界面，创建标签：
    父容器是父容器
    文本是文本
    字体是样式·字体
    字号是样式·字号
    颜色是样式·颜色
    对齐是样式·对齐。
  
  回标签。

# 创建按钮
定创建按钮是函父容器、文本、点击事件、样式：
  定按钮是图形界面，创建按钮：
    父容器是父容器
    文本是文本
    字体是样式·字体
    字号是样式·字号
    背景色是样式·背景色
    文字颜色是样式·文字颜色
    点击事件是点击事件。
  
  回按钮。

# 创建输入框
定创建输入框是函父容器、占位符、样式：
  定输入框是图形界面，创建输入框：
    父容器是父容器
    占位符是占位符
    字体是样式·字体
    字号是样式·字号
    边框色是样式·边框色
    背景色是样式·背景色。
  
  回输入框。

# 创建文本域
定创建文本域是函父容器、行数、样式：
  定文本域是图形界面，创建文本域：
    父容器是父容器
    行数是行数
    字体是样式·字体
    字号是样式·字号
    可滚动是真。
  
  回文本域。

# 创建下拉框
定创建下拉框是函父容器、选项列表、样式：
  定下拉框是图形界面，创建下拉框：
    父容器是父容器
    选项是选项列表
    字体是样式·字体
    字号是样式·字号。
  
  回下拉框。

# 创建复选框
定创建复选框是函父容器、文本、样式：
  定复选框是图形界面，创建复选框：
    父容器是父容器
    文本是文本
    字体是样式·字体
    字号是样式·字号。
  
  回复选框。

# 创建单选按钮
定创建单选按钮是函父容器、文本列表、样式：
  定单选组是图形界面，创建单选组：
    父容器是父容器
    选项是文本列表
    字体是样式·字体
    字号是样式·字号。
  
  回单选组。

# 创建滑块
定创建滑块是函父容器、最小值、最大值、初始值、样式：
  定滑块是图形界面，创建滑块：
    父容器是父容器
    最小值是最小值
    最大值是最大值
    初始值是初始值
    方向是样式·方向。
  
  回滑块。

# 创建进度条
定创建进度条是函父容器、最小值、最大值、初始值、样式：
  定进度条是图形界面，创建进度条：
    父容器是父容器
    最小值是最小值
    最大值是最大值
    初始值是初始值
    背景色是样式·背景色
    进度色是样式·进度色。
  
  回进度条。

# ===== 布局管理 =====

# 创建垂直布局
定创建垂直布局是函父容器、间距、边距：
  定布局是图形界面，创建垂直布局：
    父容器是父容器
    间距是间距
    左边距是边距
    右边距是边距
    上边距是边距
    下边距是边距。
  
  回布局。

# 创建水平布局
定创建水平布局是函父容器、间距、边距：
  定布局是图形界面，创建水平布局：
    父容器是父容器
    间距是间距
    左边距是边距
    右边距是边距
    上边距是边距
    下边距是边距。
  
  回布局。

# 创建网格布局
定创建网格布局是函父容器、行数、列数、间距、边距：
  定布局是图形界面，创建网格布局：
    父容器是父容器
    行数是行数
    列数是列数
    行间距是间距
    列间距是间距
    边距是边距。
  
  回布局。

# 添加控件到布局
定添加控件是函布局、控件、位置：
  布局，添加控件(控件、位置)。

# ===== 事件处理 =====

# 绑定点击事件
定绑定点击事件是函控件、事件处理函数：
  控件，绑定事件：
    事件类型是"点击"
    处理函数是事件处理函数。
  
  印"已绑定点击事件"。

# 绑定输入事件
定绑定输入事件是函控件、事件处理函数：
  控件，绑定事件：
    事件类型是"输入"
    处理函数是事件处理函数。
  
  印"已绑定输入事件"。

# 绑定键盘事件
定绑定键盘事件是函窗口、按键、事件处理函数：
  窗口，绑定事件：
    事件类型是"键盘"
    按键是按键
    处理函数是事件处理函数。
  
  印"已绑定键盘事件："加按键。

# 绑定鼠标事件
定绑定鼠标事件是函控件、事件类型、事件处理函数：
  控件，绑定事件：
    事件类型是事件类型
    处理函数是事件处理函数。
  
  印"已绑定鼠标事件："加事件类型。

# ===== 对话框 =====

# 显示消息对话框
定显示消息是函窗口、标题、消息、类型：
  若类型等于空就：
    类型是"信息"。
  
  图形界面，显示对话框：
    父窗口是窗口
    类型是"消息"
    标题是标题
    消息是消息
    图标是类型。

# 显示确认对话框
定显示确认是函窗口、标题、消息：
  定结果是图形界面，显示对话框：
    父窗口是窗口
    类型是"确认"
    标题是标题
    消息是消息。
  
  回结果是"确定"。

# 显示输入对话框
定显示输入是函窗口、标题、提示、默认值：
  定结果是图形界面，显示对话框：
    父窗口是窗口
    类型是"输入"
    标题是标题
    提示是提示
    默认值是默认值。
  
  回结果。

# 显示文件选择对话框
定选择文件是函窗口、标题、文件类型：
  定文件路径是图形界面，显示对话框：
    父窗口是窗口
    类型是"打开文件"
    标题是标题
    文件类型是文件类型。
  
  回文件路径。

# 显示保存文件对话框
定选择保存路径是函窗口、标题、文件类型：
  定文件路径是图形界面，显示对话框：
    父窗口是窗口
    类型是"保存文件"
    标题是标题
    文件类型是文件类型。
  
  回文件路径。

# 显示目录选择对话框
定选择目录是函窗口、标题：
  定目录路径是图形界面，显示对话框：
    父窗口是窗口
    类型是"选择目录"
    标题是标题。
  
  回目录路径。

# ===== 菜单和工具栏 =====

# 创建菜单栏
定创建菜单栏是函窗口：
  定菜单栏是图形界面，创建菜单栏：
    父窗口是窗口。
  
  回菜单栏。

# 添加菜单
定添加菜单是函菜单栏、菜单名、菜单项列表：
  定菜单是图形界面，创建菜单：
    父容器是菜单栏
    标题是菜单名。
  
  对于项于菜单项列表：
    若项·类型是"分隔符"就：
      菜单，添加分隔符。
    不然：
      菜单，添加菜单项：
        文本是项·文本
        快捷键是项·快捷键
        点击事件是项·事件。
  
  回菜单。

# 创建工具栏
定创建工具栏是函窗口、按钮列表：
  定工具栏是图形界面，创建工具栏：
    父窗口是窗口。
  
  对于按钮于按钮列表：
    工具栏，添加按钮：
      图标是按钮·图标
      提示是按钮·提示
      点击事件是按钮·事件。
  
  回工具栏。

# ===== 状态栏 =====

# 创建状态栏
定创建状态栏是函窗口：
  定状态栏是图形界面，创建状态栏：
    父窗口是窗口。
  
  回状态栏。

# 更新状态栏
定更新状态是函状态栏、消息：
  状态栏，设置消息(消息)。

# ===== 样式定义 =====

# 默认样式
定默认样式是典：
  字体是"微软雅黑"
  字号是12
  颜色是"#333333"
  背景色是"#FFFFFF"
  边框色是"#CCCCCC"
  对齐是"左"。

# 按钮样式
定按钮样式是典：
  字体是"微软雅黑"
  字号是12
  背景色是"#0078D7"
  文字颜色是"#FFFFFF"
  悬停色是"#1084D9"
  点击色是"#006CBD"。

# 输入框样式
定输入框样式是典：
  字体是"微软雅黑"
  字号是12
  边框色是"#CCCCCC"
  背景色是"#FFFFFF"
  焦点色是"#0078D7"。

# ===== 示例应用：简单计算器 =====

# 创建计算器窗口
定创建计算器是函：
  定窗口是"计算器"、300、400，创建主窗口。
  
  # 创建布局
  定主布局是窗口、10、10，创建垂直布局。
  
  # 创建显示框
  定显示框是窗口、""、输入框样式，创建输入框。
  显示框，设置属性("只读"、真)。
  显示框，设置属性("对齐"、"右")。
  显示框，设置属性("字号"、20)。
  主布局、显示框、典行是0，添加控件。
  
  # 创建按钮布局
  定按钮布局是窗口、5、0，创建网格布局。
  主布局、按钮布局、典行是1，添加控件。
  
  # 按钮文本
  定按钮文本是列：
    列"7"、"8"、"9"、"÷"。
    列"4"、"5"、"6"、"×"。
    列"1"、"2"、"3"、"-"。
    列"0"、"."、"="、"+"。
  
  # 创建按钮
  定当前值是""。
  定运算符是""。
  定前一个值是""。
  
  定按钮点击是函按钮文本：
    若按钮文本是"C"就：
      当前值是""。
      运算符是""。
      前一个值是""。
      显示框，设置属性("文本"、"")。
    
    不然若按钮文本是"="就：
      若前一个值不等于""且运算符不等于""且当前值不等于""就：
        定结果是空。
        
        若运算符是"+"就：
          结果是前一个值加当前值。
        不然若运算符是"-"就：
          结果是前一个值减当前值。
        不然若运算符是"×"就：
          结果是前一个值乘以当前值。
        不然若运算符是"÷"就：
          若当前值不等于0就：
            结果是前一个值除以当前值。
          不然：
            显示框，设置属性("文本"、"错误")。
            回。
        
        显示框，设置属性("文本"、结果转字符串)。
        当前值是结果转字符串。
        前一个值是""。
        运算符是""。
    
    不然若按钮文本是"+"或按钮文本是"-"或按钮文本是"×"或按钮文本是"÷"就：
      若当前值不等于""就：
        前一个值是当前值转数字。
        运算符是按钮文本。
        当前值是""。
    
    不然：
      当前值加等于按钮文本。
      显示框，设置属性("文本"、当前值)。
  。
  
  # 创建数字按钮
  对于行于按钮文本：
    对于文本于行：
      定按钮是窗口、文本、函：按钮点击(文本)、按钮样式，创建按钮。
      按钮布局、按钮、典，添加控件。
  
  # 显示窗口
  窗口，显示。
  
  回窗口。

# ===== 示例应用：文本编辑器 =====

# 创建文本编辑器
定创建文本编辑器是函：
  定窗口是"文本编辑器"、800、600，创建主窗口。
  
  # 创建菜单栏
  定菜单栏是窗口，创建菜单栏。
  
  # 文件菜单
  定文件菜单项是列：
    典文本是"新建"、快捷键是"Ctrl+N"、事件是函：印"新建文件"。
    典文本是"打开"、快捷键是"Ctrl+O"、事件是函：印"打开文件"。
    典类型是"分隔符"。
    典文本是"保存"、快捷键是"Ctrl+S"、事件是函：印"保存文件"。
    典文本是"另存为"、快捷键是"Ctrl+Shift+S"、事件是函：印"另存为"。
    典类型是"分隔符"。
    典文本是"退出"、快捷键是"Alt+F4"、事件是函：窗口，关闭。
  。
  
  菜单栏、"文件"、文件菜单项，添加菜单。
  
  # 编辑菜单
  定编辑菜单项是列：
    典文本是"撤销"、快捷键是"Ctrl+Z"、事件是函：印"撤销"。
    典文本是"重做"、快捷键是"Ctrl+Y"、事件是函：印"重做"。
    典类型是"分隔符"。
    典文本是"剪切"、快捷键是"Ctrl+X"、事件是函：印"剪切"。
    典文本是"复制"、快捷键是"Ctrl+C"、事件是函：印"复制"。
    典文本是"粘贴"、快捷键是"Ctrl+V"、事件是函：印"粘贴"。
    典类型是"分隔符"。
    典文本是"全选"、快捷键是"Ctrl+A"、事件是函：印"全选"。
  。
  
  菜单栏、"编辑"、编辑菜单项，添加菜单。
  
  # 创建工具栏
  定工具栏按钮是列：
    典图标是"新建"、提示是"新建文件"、事件是函：印"新建"。
    典图标是"打开"、提示是"打开文件"、事件是函：印"打开"。
    典图标是"保存"、提示是"保存文件"、事件是函：印"保存"。
  。
  
  定工具栏是窗口、工具栏按钮，创建工具栏。
  
  # 创建状态栏
  定状态栏是窗口，创建状态栏。
  状态栏，更新状态("就绪")。
  
  # 创建主布局
  定主布局是窗口、0、0，创建垂直布局。
  
  # 创建文本编辑区
  定文本区是窗口、20、默认样式，创建文本域。
  主布局、文本区、典行是0，添加控件。
  
  # 显示窗口
  窗口，显示。
  
  回窗口。

# ===== 示例应用：登录窗口 =====

# 创建登录窗口
定创建登录窗口是函：
  定窗口是"用户登录"、400、300，创建主窗口。
  窗口，设置属性("可调整大小"、假)。
  
  # 创建主布局
  定主布局是窗口、20、40，创建垂直布局。
  
  # 标题
  定标题标签是窗口、"用户登录"、典字体是"微软雅黑"、字号是18、对齐是"中"，创建标签。
  主布局、标题标签、典行是0，添加控件。
  
  # 用户名输入
  定用户名布局是窗口、10、0，创建水平布局。
  主布局、用户名布局、典行是1，添加控件。
  
  定用户名标签是窗口、"用户名："、默认样式，创建标签。
  用户名布局、用户名标签、典列是0，添加控件。
  
  定用户名输入是窗口、"请输入用户名"、输入框样式，创建输入框。
  用户名布局、用户名输入、典列是1，添加控件。
  
  # 密码输入
  定密码布局是窗口、10、0，创建水平布局。
  主布局、密码布局、典行是2，添加控件。
  
  定密码标签是窗口、"密码："、默认样式，创建标签。
  密码布局、密码标签、典列是0，添加控件。
  
  定密码输入是窗口、"请输入密码"、输入框样式，创建输入框。
  密码输入，设置属性("密码模式"、真)。
  密码布局、密码输入、典列是1，添加控件。
  
  # 记住密码
  定记住密码是窗口、"记住密码"、默认样式，创建复选框。
  主布局、记住密码、典行是3，添加控件。
  
  # 按钮布局
  定按钮布局是窗口、10、0，创建水平布局。
  主布局、按钮布局、典行是4，添加控件。
  
  # 登录按钮
  定登录事件是函：
    定用户名是用户名输入，获取文本。
    定密码是密码输入，获取文本。
    
    若用户名等于"admin"且密码等于"123456"就：
      窗口，显示消息("成功"、"登录成功！"、"信息")。
      印"登录成功"。
    不然：
      窗口，显示消息("错误"、"用户名或密码错误！"、"错误")。
      印"登录失败"。
  。
  
  定登录按钮是窗口、"登录"、登录事件、按钮样式，创建按钮。
  按钮布局、登录按钮、典列是0，添加控件。
  
  # 取消按钮
  定取消事件是函：
    窗口，关闭。
  。
  
  定取消按钮是窗口、"取消"、取消事件、按钮样式，创建按钮。
  按钮布局、取消按钮、典列是1，添加控件。
  
  # 显示窗口
  窗口，显示。
  
  回窗口。

# ===== 主程序 =====

印"========================================"。
印"基础GUI示例"。
印"========================================"。
印""。

印"【示例1：简单计算器】"。
印"运行计算器示例..."。
定计算器窗口是创建计算器。

印""。
印"【示例2：文本编辑器】"。
印"运行文本编辑器示例..."。
定编辑器窗口是创建文本编辑器。

印""。
印"【示例3：登录窗口】"。
印"运行登录窗口示例..."。
定登录窗口是创建登录窗口。

印""。
印"========================================"。
印"GUI示例运行中..."。
印"========================================"。

# 启动主循环
图形界面，启动主循环。
