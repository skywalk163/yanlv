# 冒泡排序

定冒泡排序是函列表：
  定长度是列表，长。
  
  对于i从0到长度减1：
    对于j从0到长度减i减2：
      定当前是列表算j。
      定下一个是列表算j加1。
      
      若当前大下一个就：
        列表、j、j加1，交换。

# 交换列表中的两个元素
定交换是函列表、索引1、索引2：
  定临时是列表算索引1。
  列表算索引1等于列表算索引2。
  列表算索引2等于临时。

# 测试
定数据是列5 3 8 4 2 1 9 6。
数据，冒泡排序，印。
