# 因果链语法示例
# 展示言律语言的因果链式编程特性

# ===== 基础因果链 =====

# 简单因果：条件 → 动作
下雨了，带伞。
天黑了，开灯。
饿了，吃饭。

# 带参数的因果链
温度大于30，开空调。
温度小于18，开暖气。
温度在18到30之间，关闭空调和暖气。

# ===== 多条件因果链 =====

# 多个条件触发同一动作
下雨了，带伞。
下雪了，带伞。
刮大风，带伞。

# 简化写法（条件累积）
下雨了、下雪了、刮大风，带伞。

# 条件组合
下雨了且温度小于10，带伞并穿厚外套。
天晴了且温度大于25，带太阳镜并涂防晒霜。

# ===== 因果链与状态 =====

# 状态变化触发动作
订单状态变为已付款，准备发货。
订单状态变为已发货，通知用户。
订单状态变为已签收，更新库存。

# 状态条件判断
用户状态为在线，发送即时消息。
用户状态为离线，发送邮件通知。
用户状态为忙碌，延迟发送。

# ===== 因果链与事件 =====

# 事件监听
当收到消息时，显示通知。
当收到好友请求时，弹出确认框。
当收到文件传输请求时，询问保存位置。

# 事件处理链
当用户点击按钮时：
  验证表单数据。
  若数据有效就：
    提交表单。
    显示成功提示。
  不然：
    显示错误提示。

# ===== 实际应用示例 =====

# 智能家居控制
定智能家居是典：
  温度是25
  湿度是60
  光线是"明亮"
  是否有人是真

# 温度控制因果链
温度大于28，开启空调制冷。
温度小于20，开启空调制热。
温度在20到28之间，关闭空调。

# 湿度控制因果链
湿度大于70，开启除湿机。
湿度小于40，开启加湿器。
湿度在40到70之间，关闭除湿机和加湿器。

# 光线控制因果链
光线为"明亮"且有人，关闭灯光。
光线为"昏暗"且有人，开启灯光。
光线为"明亮"且无人，关闭灯光节能。
光线为"昏暗"且无人，关闭灯光节能。

# ===== 业务流程因果链 =====

# 订单处理流程
定订单处理是函订单：
  # 因果链1：验证
  订单信息不完整，返回错误提示。
  订单信息完整，继续处理。
  
  # 因果链2：支付
  支付失败，取消订单并通知用户。
  支付成功，更新订单状态为已付款。
  
  # 因果链3：发货
  库存不足，通知用户并延迟发货。
  库存充足，准备发货并更新状态。
  
  # 因果链4：完成
  发货成功，通知用户物流信息。
  用户签收，更新订单状态为已完成。

# 用户注册流程
定用户注册是函用户信息：
  # 因果链1：验证
  用户名已存在，提示更换用户名。
  用户名可用，继续注册。
  
  # 因果链2：邮箱验证
  邮箱格式错误，提示正确格式。
  邮箱格式正确，发送验证邮件。
  
  # 因果链3：完成注册
  邮箱验证成功，创建用户账户。
  用户账户创建成功，发送欢迎邮件。

# ===== 异常处理因果链 =====

# 文件操作异常处理
定安全读取文件是函文件路径：
  文件不存在，返回空并记录日志。
  文件存在但无权限，提示权限不足。
  文件存在且有权限，读取文件内容。

# 网络请求异常处理
定安全请求是函网址：
  网络连接失败，返回错误并重试。
  网络连接成功但超时，返回超时提示。
  网络连接成功且响应正常，返回响应内容。

# 数据库操作异常处理
定安全查询是函SQL语句：
  数据库连接失败，返回连接错误。
  SQL语法错误，返回语法错误提示。
  查询成功，返回查询结果。

# ===== 条件优先级因果链 =====

# 优先级判断
定判断用户等级是函用户：
  用户消费金额大于10000，设为VIP用户。
  用户消费金额大于5000，设为金牌用户。
  用户消费金额大于1000，设为银牌用户。
  用户消费金额小于等于1000，设为普通用户。

# 折扣计算
定计算折扣是函用户等级、订单金额：
  用户等级为VIP，订单金额打8折。
  用户等级为金牌，订单金额打85折。
  用户等级为银牌，订单金额打9折。
  用户等级为普通，订单金额不打折。

# ===== 链式因果 =====

# 数据处理流水线
定数据处理流水线是函原始数据：
  # 链式因果：每一步的输出是下一步的输入
  原始数据，验证格式，结果1。
  结果1，清洗数据，结果2。
  结果2，转换格式，结果3。
  结果3，分析统计，结果4。
  结果4，生成报告，最终结果。
  
  回最终结果。

# 订单处理流水线
定订单处理流水线是函订单：
  订单，验证信息，有效订单。
  有效订单，检查库存，可发货订单。
  可发货订单，计算价格，定价订单。
  定价订单，处理支付，已支付订单。
  已支付订单，安排发货，已发货订单。
  已发货订单，跟踪物流，已完成订单。
  
  回已完成订单。

# ===== 因果链与循环 =====

# 循环中的因果链
定批量处理订单是函订单列表：
  对于订单于订单列表：
    订单状态为新建，处理订单。
    订单状态为已付款，安排发货。
    订单状态为已发货，跟踪物流。
    订单状态为异常，记录问题。

# 监控循环
定监控系统是函：
  当系统运行时：
    CPU使用率大于80%，发送警报。
    内存使用率大于90%，发送警报。
    磁盘空间小于10%，发送警报。
    网络延迟大于1000毫秒，发送警报。
    
    等待5秒。

# ===== 复杂因果链示例 =====

# 智能客服系统
定智能客服是函用户消息：
  # 意图识别因果链
  消息包含"价格"，查询商品价格。
  消息包含"库存"，查询商品库存。
  消息包含"订单"，查询订单状态。
  消息包含"退款"，处理退款请求。
  消息包含"投诉"，转接人工客服。
  
  # 情感分析因果链
  用户情感为愤怒，优先处理并道歉。
  用户情感为满意，感谢用户并请求评价。
  用户情感为中性，提供标准回复。

# 交通信号灯系统
定交通信号控制是函路口：
  # 时间段因果链
  当前时间为早高峰，设置高峰信号模式。
  当前时间为晚高峰，设置高峰信号模式。
  当前时间为平峰期，设置平峰信号模式。
  当前时间为深夜，设置夜间信号模式。
  
  # 特殊情况因果链
  检测到救护车，切换为紧急通行模式。
  检测到消防车，切换为紧急通行模式。
  检测到交通事故，切换为疏导模式。
  
  # 流量自适应因果链
  车流量大于阈值，延长绿灯时间。
  车流量小于阈值，缩短绿灯时间。
  行人等待时间过长，优先行人通行。

# ===== 测试因果链 =====

# 测试温度控制
定温度是30。
温度大于28，印"开启空调制冷"。

定温度是15。
温度小于20，印"开启空调制热"。

# 测试订单处理
定测试订单是典：
  编号是"001"
  状态是"新建"
  商品是"手机"
  数量是1

测试订单·状态等于"新建"，印"开始处理订单"。

# 测试用户等级
定测试用户是典：
  姓名"张三"
  消费金额是15000

测试用户，判断用户等级，印。
