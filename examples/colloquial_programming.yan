# 口语化编程示例
# 展示言律语言的自然、口语化编程风格

# ===== 询问式编程 =====

# 传统风格
定判断成绩是函分数：
  若分数大等于90就：
    印"优秀"。
  不然若分数大等于60就：
    印"及格"。
  不然：
    印"不及格"。

# 口语化风格（询问式）
定判断成绩口语是函分数：
  要是分数大等于90就印"优秀"。
  要是分数大等于60就印"及格"。
  不然就印"不及格"。

# ===== 建议式编程 =====

# 传统风格
定安全除法是函被除数、除数：
  若除数等于0就：
    印"错误：除数不能为零"。
    回0。
  回被除数除以除数。

# 口语化风格（建议式）
定安全除法口语是函被除数、除数：
  最好先检查除数是否等于0。
  若是就印"错误：除数不能为零"，回0。
  不然就回被除数除以除数。

# ===== 祈使式编程 =====

# 传统风格
定发送通知是函用户、消息：
  定邮箱是用户·邮箱。
  定主题是"系统通知"。
  邮箱、主题、消息，发送邮件。
  印"通知已发送"。

# 口语化风格（祈使式）
定发送通知口语是函用户、消息：
  请先获取用户的邮箱。
  然后设置主题为"系统通知"。
  务必发送邮件到邮箱，包含主题和消息。
  最后打印"通知已发送"。

# ===== 疑问式编程 =====

# 传统风格
定检查权限是函用户、操作：
  定角色是用户·角色。
  定权限是角色·权限。
  若权限包含操作就：
    回真。
  不然：
    回假。

# 口语化风格（疑问式）
定检查权限口语是函用户、操作：
  先获取用户的角色，好吗？
  再获取角色的权限，行吗？
  权限是否包含操作？
  若是就回真，不然就回假。

# ===== 情境式编程 =====

# 传统风格
定处理订单是函订单：
  若订单·状态等于"新建"就：
    订单·状态变为"处理中"。
    订单，发送到仓库。
  不然若订单·状态等于"处理中"就：
    订单·状态变为"已发货"。
    订单，发送通知。
  不然：
    印"订单状态异常"。

# 口语化风格（情境式）
定处理订单口语是函订单：
  当订单状态是"新建"的时候：
    把状态改为"处理中"。
    然后发送到仓库。
  
  一旦订单状态变为"处理中"：
    把状态改为"已发货"。
    然后发送通知给用户。
  
  否则就说"订单状态异常"。

# ===== 混合式编程 =====

# 结合多种口语化风格
定智能推荐是函用户、商品列表：
  # 询问式：条件判断
  要是用户是会员就：
    # 建议式：数据处理
    最好过滤出会员专享商品。
    然后按折扣排序。
  
  # 祈使式：执行操作
  请获取用户的浏览历史。
  务必根据历史调整推荐权重。
  
  # 疑问式：结果验证
  推荐列表是否为空？
  若是就回默认推荐。
  不然就回推荐列表。

# ===== 测试 =====

# 测试询问式
85，判断成绩。
85，判断成绩口语。

# 测试建议式
10、0，安全除法。
10、0，安全除法口语。

# 测试祈使式
定测试用户是典姓名是"张三"、邮箱是"zhang@example.com"。
测试用户、"您的订单已发货"，发送通知。
测试用户、"您的订单已发货"，发送通知口语。

# 测试疑问式
定测试角色是典名称是"管理员"、权限是列"读"、"写"、"删除"。
定管理员用户是典姓名是"李四"、角色是测试角色。
管理员用户、"删除"，检查权限。
管理员用户、"删除"，检查权限口语。

# 测试情境式
定测试订单是典编号是"001"、状态是"新建"。
测试订单，处理订单。
测试订单，处理订单口语。

# 测试混合式
定会员用户是典姓名是"王五"、是会员是真、浏览历史是列"手机"、"电脑"。
定商品是列手机、电脑、平板、耳机。
会员用户、商品，智能推荐。
