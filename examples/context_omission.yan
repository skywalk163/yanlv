# 语境省略语法示例
# 展示言律语言的上下文推断和省略特性

# ===== 主语省略 =====

# 传统写法
定张三是典姓名是"张三"、年龄是25、城市是"北京"。
张三·姓名，印。
张三·年龄，印。
张三·城市，印。

# 语境省略写法（主题链）
定张三是典姓名是"张三"、年龄是25、城市是"北京"。
以张三为主题：
  姓名，印。      # 省略"张三·"
  年龄，印。      # 省略"张三·"
  城市，印。      # 省略"张三·"

# 更自然的写法
定张三是典姓名是"张三"、年龄是25、城市是"北京"。
张三：
  姓名，印。
  年龄，印。
  城市，印。

# ===== 宾语省略 =====

# 传统写法
定处理订单是函订单：
  订单，验证。
  订单，处理。
  订单，发货。
  订单，通知用户。

# 语境省略写法
定处理订单是函订单：
  以订单为对象：
    验证。      # 省略宾语"订单"
    处理。      # 省略宾语"订单"
    发货。      # 省略宾语"订单"
    通知用户。  # 省略宾语"订单"

# 更自然的写法
定处理订单是函订单：
  订单：
    验证。
    处理。
    发货。
    通知用户。

# ===== 条件省略 =====

# 传统写法
定判断成绩是函分数：
  若分数大于等于90就：
    回"优秀"。
  不然若分数大于等于80就：
    回"良好"。
  不然若分数大于等于60就：
    回"及格"。
  不然：
    回"不及格"。

# 语境省略写法（条件累积）
定判断成绩是函分数：
  大于等于90，回"优秀"。
  大于等于80，回"良好"。
  大于等于60，回"及格"。
  不然，回"不及格"。

# ===== 方法链省略 =====

# 传统写法
定处理数据是函数据：
  定步骤1是数据，清洗。
  定步骤2是步骤1，转换。
  定步骤3是步骤2，分析。
  定步骤4是步骤3，输出。
  回步骤4。

# 语境省略写法（管道式）
定处理数据是函数据：
  数据：
    清洗。
    转换。
    分析。
    输出。

# 或者更简洁
定处理数据是函数据：
  数据，清洗，转换，分析，输出。

# ===== 主题链 =====

# 定义主题链
定用户操作是典：
  用户是典姓名是"张三"、年龄是25
  订单是典编号是"001"、状态是"新建"

# 传统写法
用户操作·用户·姓名，印。
用户操作·用户·年龄，印。
用户操作·订单·编号，印。
用户操作·订单·状态，印。

# 主题链写法
用户操作：
  用户：
    姓名，印。
    年龄，印。
  
  订单：
    编号，印。
    状态，印。

# ===== 上下文补全 =====

# 定义上下文
定上下文是典：
  当前用户是典姓名是"张三"、角色是"管理员"
  当前时间"2026-05-21 10:30:00"
  当前位置是"北京"

# 省略上下文引用
在上下文中：
  # 自动补全为"上下文·当前用户·姓名"
  姓名，印。
  
  # 自动补全为"上下文·当前时间"
  时间，印。
  
  # 自动补全为"上下文·当前位置"
  位置，印。

# ===== 对话式省略 =====

# 模拟对话场景
定对话处理是函：
  # 第一句：完整表达
  用户问"今天天气怎么样？"。
  
  # 第二句：省略主语（指代上一句的用户）
  回答"今天天气晴朗，温度25度。"。
  
  # 第三句：省略主语和宾语
  问"需要带伞吗？"。
  
  # 第四句：省略主语
  回答"不需要，今天不会下雨。"。

# ===== 省略与嵌套 =====

# 多层嵌套省略
定公司是典：
  名称是"科技公司"
  部门是典：
    技术部是典：
      员工是列典姓名是"张三"、职位是"工程师"，典姓名是"李四"、职位是"设计师"
      项目是列"项目A"、"项目B"

# 传统写法
公司·部门·技术部·员工，印。
公司·部门·技术部·项目，印。

# 省略写法
公司：
  部门：
    技术部：
      员工，印。
      项目，印。

# ===== 条件链省略 =====

# 多条件判断
定判断用户权限是函用户：
  # 传统写法
  若用户·角色等于"管理员"且用户·状态等于"激活"就：
    回"完全访问权限"。
  不然若用户·角色等于"编辑"且用户·状态等于"激活"就：
    回"编辑权限"。
  不然若用户·状态等于"激活"就：
    回"只读权限"。
  不然：
    回"无权限"。

# 省略写法
定判断用户权限是函用户：
  用户：
    角色等于"管理员"且状态等于"激活"，回"完全访问权限"。
    角色等于"编辑"且状态等于"激活"，回"编辑权限"。
    状态等于"激活"，回"只读权限"。
    不然，回"无权限"。

# ===== 循环中的省略 =====

# 传统写法
定批量处理是函列表：
  对于元素于列表：
    若元素大于0就：
      元素，处理正数。
    不然若元素小于0就：
      元素，处理负数。
    不然：
      元素，处理零。

# 省略写法
定批量处理是函列表：
  对于元素于列表：
    元素：
      大于0，处理正数。
      小于0，处理负数。
      不然，处理零。

# ===== 函数参数省略 =====

# 传统写法
定计算是函x、y、z：
  定结果是x加y加z。
  回结果。

# 省略写法（使用默认值）
定计算是函x、y是0、z是0：
  回x加y加z。

# 调用
10，计算，印。          # x=10, y=0, z=0
10、5，计算，印。       # x=10, y=5, z=0
10、5、3，计算，印。    # x=10, y=5, z=3

# ===== 链式调用省略 =====

# 传统写法
定处理是函数据：
  定结果是数据。
  结果，方法1。
  结果，方法2。
  结果，方法3。
  回结果。

# 链式省略写法
定处理是函数据：
  数据：
    方法1。
    方法2。
    方法3。

# 或者更简洁
定处理是函数据：
  数据，方法1，方法2，方法3。

# ===== 实际应用示例 =====

# 文件处理省略
定处理文件是函文件路径：
  文件路径：
    读取。        # 省略"文件路径"
    清洗。        # 省略"文件路径"
    分析。        # 省略"文件路径"
    保存。        # 省略"文件路径"

# 用户管理省略
定管理用户是函用户：
  用户：
    验证信息。
    更新资料。
    发送通知。
    记录日志。

# 订单处理省略
定处理订单是函订单：
  订单：
    验证。
    支付。
    发货。
    完成。

# ===== 省略与错误处理 =====

# 传统写法
定安全操作是函数据：
  试：
    定结果是数据，处理。
    回结果。
  捕错误：
    印错误。
    回空。

# 省略写法
定安全操作是函数据：
  数据：
    处理。
  不然：
    印错误。
    回空。

# ===== 省略与异步 =====

# 传统写法
定异步处理是函数据：
  定结果是等待数据，异步操作。
  回结果。

# 省略写法
定异步处理是函数据：
  数据：
    异步操作。
    等待。

# ===== 复杂省略示例 =====

# 多层省略
定复杂操作是函：
  上下文：
    用户：
      验证。
      更新。
    
    订单：
      创建。
      处理。
    
    通知：
      发送。
      确认。

# ===== 测试省略语法 =====

# 测试主题链
定测试用户是典姓名是"张三"、年龄是25、城市是"北京"。
测试用户：
  姓名，印。
  年龄，印。
  城市，印。

# 测试方法链
定测试数据是列1、2、3、4、5。
测试数据：
  过滤大于2。
  映射乘以2。
  求和。
  印。

# 测试条件省略
定测试分数是85。
测试分数：
  大于等于90，印"优秀"。
  大于等于80，印"良好"。
  大于等于60，印"及格"。
  不然，印"不及格"。
