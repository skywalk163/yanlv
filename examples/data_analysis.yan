# 数据分析示例
# 展示如何用言律语言进行统计分析、相关性分析、趋势分析、异常检测

# ===== 导入数据分析模块 =====

导入《数据分析》。
导入《数学》。

# ===== 数据结构定义 =====

# 销售数据模型
定销售数据是典：
  日期是空
  产品是空
  销量是空
  收入是空
  成本是空
  利润是空
  地区是空。

# 分析结果模型
定分析结果是典：
  指标名是空
  指标值是空
  单位是空
  说明是空。

# ===== 数据生成 =====

# 生成销售数据
定生成销售数据是函天数：
  定数据列表是列。
  定产品列表是列"产品A"、"产品B"、"产品C"、"产品D"。
  定地区列表是列"华东"、"华南"、"华北"、"西南"。
  
  定基准日期是"2024-01-01"转日期。
  
  对于i从0到天数减1：
    定当前日期是基准日期加i天。
    
    对于产品于产品列表：
      对于地区于地区列表：
        # 模拟销售数据（带有季节性和趋势）
        定季节因子是1加0.3乘以正弦(2乘以π乘以i除以365)。
        定趋势因子是1加0.001乘以i。
        定随机因子是0.8加0.4乘以随机数。
        
        定基础销量是随机选择(列50、80、100、120)。
        定销量是基础销量乘以季节因子乘以趋势因子乘以随机因子，四舍五入。
        
        定单价是随机选择(列10、20、30、40)。
        定收入是销量乘以单价。
        
        定成本率是0.6加0.1乘以随机数。
        定成本是收入乘以成本率。
        
        定利润是收入减成本。
        
        数据列表，添加典：
          日期是当前日期
          产品是产品
          销量是销量
          收入是收入
          成本是成本
          利润是利润
          地区是地区。
  
  印"已生成"加天数加"天的销售数据，共"加数据列表，长度加"条"。
  回数据列表。

# ===== 描述性统计 =====

# 计算基础统计量
定计算基础统计是函数值列表：
  定数量是数值列表，长度。
  
  # 均值
  定总和是0。
  对于值于数值列表：
    总和加等于值。
  定均值是总和除以数量。
  
  # 中位数
  定排序后是数值列表，排序。
  定中位数是空。
  若数量模2等于0就：
    中位数是(排序后算(数量除以2减1)加排序后算数量除以2)除以2。
  不然：
    中位数是排序后算(数量除以2)。
  
  # 方差和标准差
  定方差和是0。
  对于值于数值列表：
    定差值是值减均值。
    方差和加等于差值乘以差值。
  定方差是方差和除以数量。
  定标准差是方差，开平方。
  
  # 最大值和最小值
  定最大值是排序后，最后。
  定最小值是排序后，首个。
  
  # 四分位数
  定Q1索引是数量乘以0.25，取整。
  定Q3索引是数量乘以0.75，取整。
  定Q1是排序后算Q1索引。
  定Q3是排序后算Q3索引。
  定IQR是Q3减Q1。
  
  回典：
    数量是数量
    总和是总和
    均值是均值
    中位数是中位数
    方差是方差
    标准差是标准差
    最大值是最大值
    最小值是最小值
    Q1是Q1
    Q3是Q3
    IQR是IQR
    范围是最大值减最小值。

# 计算偏度和峰度
定计算偏度峰度是函数值列表、均值、标准差：
  定数量是数值列表，长度。
  
  # 偏度
  定偏度和是0。
  对于值于数值列表：
    定标准化值是(值减均值)除以标准差。
    偏度和加等于标准化值的三次方。
  定偏度是偏度和除以数量。
  
  # 峰度
  定峰度和是0。
  对于值于数值列表：
    定标准化值是(值减均值)除以标准差。
    峰度和加等于标准化值的四次方。
  定峰度是峰度和除以数量减3。  # 超额峰度
  
  回典：
    偏度是偏度
    峰度是峰度。

# 字段统计分析
定字段统计分析是函数据列表、字段名：
  定数值列表是列。
  对于数据于数据列表：
    数值列表，添加数据算字段名。
  
  定基础统计是数值列表，计算基础统计。
  定偏度峰度是数值列表、基础统计·均值、基础统计·标准差，计算偏度峰度。
  
  印"===== "加字段名加" 统计分析 ====="。
  印"数量："加基础统计·数量。
  印"总和："加基础统计·总和转字符串(保留2位小数)。
  印"均值："加基础统计·均值转字符串(保留2位小数)。
  印"中位数："加基础统计·中位数转字符串(保留2位小数)。
  印"标准差："加基础统计·标准差转字符串(保留2位小数)。
  印"范围："加基础统计·范围转字符串(保留2位小数)。
  印"Q1："加基础统计·Q1转字符串(保留2位小数)。
  印"Q3："加基础统计·Q3转字符串(保留2位小数)。
  印"偏度："加偏度峰度·偏度转字符串(保留4位小数)。
  印"峰度："加偏度峰度·峰度转字符串(保留4位小数)。
  印""。
  
  回基础统计。

# ===== 相关性分析 =====

# 计算协方差
定计算协方差是函列表X、列表Y、均值X、均值Y：
  定数量是列表X，长度。
  定协方差和是0。
  
  对于i从0到数量减1：
    定差X是列表X算i减均值X。
    定差Y是列表Y算i减均值Y。
    协方差和加等于差X乘以差Y。
  
  回协方差和除以数量。

# 计算相关系数
定计算相关系数是函列表X、列表Y：
  定统计X是列表X，计算基础统计。
  定统计Y是列表Y，计算基础统计。
  
  定协方差是列表X、列表Y、统计X·均值、统计Y·均值，计算协方差。
  定相关系数是协方差除以(统计X·标准差乘以统计Y·标准差)。
  
  回相关系数。

# 相关性矩阵
定相关性矩阵是函数据列表、字段列表：
  定矩阵是典。
  
  对于字段1于字段列表：
    矩阵算字段1是典。
    
    对于字段2于字段列表：
      定列表1是数据列表，字段1，提取字段值。
      定列表2是数据列表，字段2，提取字段值。
      
      定相关系数是列表1、列表2，计算相关系数。
      矩阵算字段1算字段2是相关系数。
  
  印"===== 相关性矩阵 ====="。
  印"字段："加字段列表，用" "连接。
  印""。
  
  对于字段1于字段列表：
    定行是字段1加" "。
    对于字段2于字段列表：
      定系数是矩阵算字段1算字段2。
      行加等于系数转字符串(保留3位小数)加" "。
    印行。
  
  印""。
  回矩阵。

# ===== 趋势分析 =====

# 移动平均
定移动平均是函数值列表、窗口大小：
  定平滑后是列。
  
  对于i从0到数值列表，长度减窗口大小：
    定窗口和是0。
    对于j从0到窗口大小减1：
      窗口和加等于数值列表算(i加j)。
    
    定平均值是窗口和除以窗口大小。
    平滑后，添加平均值。
  
  回平滑后。

# 指数平滑
定指数平滑是函数值列表、平滑系数：
  定平滑后是列。
  平滑后，添加数值列表，首个。
  
  对于i从1到数值列表，长度减1：
    定平滑值是平滑系数乘以数值列表算i加(1减平滑系数)乘以平滑后，最后。
    平滑后，添加平滑值。
  
  回平滑后。

# 线性趋势拟合
定线性趋势拟合是函数值列表：
  定数量是数值列表，长度。
  
  # 计算均值
  定X均值是(数量减1)除以2。
  定Y均值是数值列表，计算基础统计·均值。
  
  # 计算斜率和截距
  定分子是0。
  定分母是0。
  
  对于i从0到数量减1：
    定差X是i减X均值。
    定差Y是数值列表算i减Y均值。
    分子加等于差X乘以差Y。
    分母加等于差X乘以差X。
  
  定斜率是分子除以分母。
  定截距是Y均值减斜率乘以X均值。
  
  # 计算R²
  定总平方和是0。
  定残差平方和是0。
  
  对于i从0到数量减1：
    定预测值是斜率乘以i加截距。
    定残差是数值列表算i减预测值。
    残差平方和加等于残差乘以残差。
    定差值是数值列表算i减Y均值。
    总平方和加等于差值乘以差值。
  
  定R平方是1减残差平方和除以总平方和。
  
  印"===== 线性趋势拟合 ====="。
  印"斜率："加斜率转字符串(保留4位小数)。
  印"截距："加截距转字符串(保留4位小数)。
  印"R²："加R平方转字符串(保留4位小数)。
  印""。
  
  回典：
    斜率是斜率
    截距是截距
    R平方是R平方。

# 季节性分解
定季节性分解是函数值列表、周期：
  定数量是数值列表，长度。
  
  # 第一步：趋势分量（移动平均）
  定趋势分量是数值列表、周期，移动平均。
  
  # 第二步：去趋势数据
  定去趋势是列。
  对于i从0到趋势分量，长度减1：
    去趋势，添加数值列表算(i加周期除以2)除以趋势分量算i。
  
  # 第三步：季节分量
  定季节分量是列。
  对于i从0到周期减1：
    定季节值和是0。
    定季节值数是0。
    
    对于j从i到去趋势，长度减1步长周期：
      季节值和加等于去趋势算j。
      季节值数加等于1。
    
    季节分量，添加季节值和除以季节值数。
  
  # 第四步：残差分量
  定残差分量是列。
  对于i从0到数量减1：
    定趋势索引是i减周期除以2。
    若趋势索引大于等于0且趋势索引小于趋势分量，长度就：
      定残差是数值列表算i减趋势分量算趋势索引减季节分量算(i模周期)。
      残差分量，添加残差。
  
  印"===== 季节性分解 ====="。
  印"周期："加周期。
  印"趋势分量长度："加趋势分量，长度。
  印"季节分量长度："加季节分量，长度。
  印""。
  
  回典：
    趋势是趋势分量
    季节是季节分量
    残差是残差分量。

# ===== 异常检测 =====

# 基于统计的异常检测
定统计异常检测是函数值列表、阈值倍数：
  定统计是数值列表，计算基础统计。
  
  定下限是统计·Q1减阈值倍数乘以统计·IQR。
  定上限是统计·Q3加阈值倍数乘以统计·IQR。
  
  定异常值是列。
  定正常值是列。
  
  对于值于数值列表：
    若值小于下限或值大于上限就：
      异常值，添加值。
    不然：
      正常值，添加值。
  
  印"===== 统计异常检测 ====="。
  印"阈值倍数："加阈值倍数。
  印"正常范围：["加下限转字符串(保留2位小数)加", "加上限转字符串(保留2位小数)加"]"。
  印"异常值数量："加异常值，长度。
  印"异常值占比："加(异常值，长度除以数值列表，长度乘以100)转字符串(保留2位小数)加"%"。
  印""。
  
  回典：
    异常值是异常值
    正常值是正常值
    下限是下限
    上限是上限。

# 基于Z分数的异常检测
定Z分数异常检测是函数值列表、阈值：
  定统计是数值列表，计算基础统计。
  
  定异常值是列。
  定正常值是列。
  
  对于值于数值列表：
    定Z分数是(值减统计·均值)除以统计·标准差。
    
    若Z分数的绝对值大于阈值就：
      异常值，添加值。
    不然：
      正常值，添加值。
  
  印"===== Z分数异常检测 ====="。
  印"Z分数阈值："加阈值。
  印"异常值数量："加异常值，长度。
  印""。
  
  回典：
    异常值是异常值
    正常值是正常值。

# 时间序列异常检测
定时间序列异常检测是函数值列表、窗口大小、阈值倍数：
  定异常索引是列。
  
  对于i从窗口大小到数值列表，长度减窗口大小减1：
    # 计算局部统计
    定窗口值是列。
    对于j从负窗口大小到窗口大小：
      若j不等于0就：
        窗口值，添加数值列表算(i加j)。
    
    定局部统计是窗口值，计算基础统计。
    定当前值是数值列表算i。
    
    # 检测异常
    定Z分数是(当前值减局部统计·均值)除以局部统计·标准差。
    
    若Z分数的绝对值大于阈值倍数就：
      异常索引，添加i。
  
  印"===== 时间序列异常检测 ====="。
  印"窗口大小："加窗口大小。
  印"检测到"加异常索引，长度加"个异常点"。
  印""。
  
  回异常索引。

# ===== 分组分析 =====

# 按维度分组统计
定分组统计分析是函数据列表、分组字段、统计字段：
  定分组数据是数据列表，分组字段，按字段分组。
  
  定分组统计结果是列。
  
  对于组名于分组数据，键列表：
    定组数据是分组数据算组名。
    定统计是组数据，统计字段，提取字段值，计算基础统计。
    
    分组统计结果，添加典：
      组名是组名
      数量是统计·数量
      总和是统计·总和
      均值是统计·均值
      标准差是统计·标准差
      最大值是统计·最大值
      最小值是统计·最小值。
  
  印"===== 分组统计 ====="。
  印"分组字段："加分组字段。
  印"统计字段："加统计字段。
  印""。
  
  对于结果于分组统计结果：
    印结果·组名加"："。
    印"  数量："加结果·数量。
    印"  总和："加结果·总和转字符串(保留2位小数)。
    印"  均值："加结果·均值转字符串(保留2位小数)。
    印"  标准差："加结果·标准差转字符串(保留2位小数)。
    印""。
  
  回分组统计结果。

# ===== 辅助函数 =====

# 提取字段值
定提取字段值是函数据列表、字段名：
  定值列表是列。
  对于数据于数据列表：
    值列表，添加数据算字段名。
  回值列表。

# 按字段分组
定按字段分组是函数据列表、字段名：
  定分组结果是典。
  
  对于数据于数据列表：
    定键是数据算字段名转字符串。
    
    若分组结果算键等于空就：
      分组结果算键是列。
    
    分组结果算键，添加数据。
  
  回分组结果。

# ===== 主程序 =====

印"========================================"。
印"数据分析示例"。
印"========================================"。
印""。

# 生成销售数据
印"【第一步：生成销售数据】"。
定销售数据是90，生成销售数据。
印""。

# 描述性统计
印"【第二步：描述性统计】"。
定销量统计是销售数据、"销量"，字段统计分析。
定收入统计是销售数据、"收入"，字段统计分析。
定利润统计是销售数据、"利润"，字段统计分析。

# 相关性分析
印"【第三步：相关性分析】"。
定相关矩阵是销售数据、列"销量"、"收入"、"成本"、"利润"，相关性矩阵。

# 趋势分析
印"【第四步：趋势分析】"。
定销量列表是销售数据、"销量"，提取字段值。
定趋势拟合是销量列表，线性趋势拟合。
定季节分解是销量列表、7，季节性分解。

# 异常检测
印"【第五步：异常检测】"。
定利润列表是销售数据、"利润"，提取字段值。
定统计异常是利润列表、1.5，统计异常检测。
定Z分数异常是利润列表、2，Z分数异常检测。
定时间序列异常是利润列表、7、2，时间序列异常检测。

# 分组分析
印"【第六步：分组分析】"。
定产品统计是销售数据、"产品"、"收入"，分组统计分析。
定地区统计是销售数据、"地区"、"利润"，分组统计分析。

印"========================================"。
印"数据分析完成"。
印"========================================"。
