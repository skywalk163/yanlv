# 数据处理示例
# 展示如何用言律语言进行数据清洗、转换、过滤、聚合和排序

# ===== 导入数据处理模块 =====

导入《数据处理》。

# ===== 数据结构定义 =====

# 原始数据模型
定原始数据是典：
编号是空
名称是空
数值是空
类别是空
时间是空
状态是空。

# 清洗后的数据模型
定清洗数据是典：
编号是空
名称是空
数值是空
类别是空
时间是空
状态是空
是否有效是真。

# ===== 数据生成 =====

# 生成测试数据
定生成测试数据是函数量：
定数据列表是列。

对于i从1到数量：
定数据是典：
编号是i
名称是"项目"加i
数值是随机数(1, 100)
类别是随机选择(列"A类"、"B类"、"C类")
时间是随机日期("2024-01-01", "2024-12-31")
状态是随机选择(列"有效"、"无效"、"待定")。

数据列表，添加数据。

印"已生成"加数量加"条测试数据"。
回数据列表。

# 添加一些脏数据
定添加脏数据是函数据列表：
# 添加空值数据
数据列表，添加典：
编号是空
名称是空
数值是空
类别是空
时间是空
状态是空。

# 添加重复数据
数据列表，添加典：
编号是1
名称是"项目1"
数值是50
类别是"A类"
时间是"2024-06-15"
状态是"有效"。

# 添加异常值
数据列表，添加典：
编号是负1
名称是""
数值是负999
类别是"未知"
时间是"2099-01-01"
状态是"错误"。

印"已添加脏数据"。

# ===== 数据清洗 =====

# 清洗单条数据
定清洗单条数据是函原始：
第一步：处理空值。
定清洗后是典：
编号是原始·编号
名称是原始·名称
数值是原始·数值
类别是原始·类别
时间是原始·时间
状态是原始·状态
是否有效是真。

若清洗后·编号等于空就：
清洗后·是否有效是假。

若清洗后·名称等于空或清洗后·名称等于""就：
清洗后·名称是"未命名"。

若清洗后·数值等于空或清洗后·数值小于0就：
清洗后·数值是0。

若清洗后·类别等于空就：
清洗后·类别是"未分类"。

若清洗后·时间等于空就：
清洗后·时间是当前时间。

若清洗后·状态等于空就：
清洗后·状态是"待定"。

第二步：验证数据有效性。
若清洗后·编号小于0就：
清洗后·是否有效是假。

若清洗后·数值大于1000就：
清洗后·是否有效是假。

回清洗后。

# 批量清洗数据
定批量清洗数据是函数据列表：
定清洗后列表是列。
定有效数是0。
定无效数是0。

对于数据于数据列表：
定清洗后是数据，清洗单条数据。

若清洗后·是否有效就：
清洗后列表，添加清洗后。
有效数加等于1。
不然：
无效数加等于1。

印"数据清洗完成：有效"加有效数加"条，无效"加无效数加"条"。
回清洗后列表。

# 去除重复数据
定去除重复数据是函数据列表：
定唯一列表是列。
定已见编号是典。

对于数据于数据列表：
定编号键是数据·编号转字符串。

若已见编号算编号键等于空就：
唯一列表，添加数据。
已见编号算编号键是真。

定重复数是数据列表，长度减唯一列表，长度。
印"去除重复数据："加重复数加"条"。

回唯一列表。

# 处理异常值
定处理异常值是函数据列表、字段名、最小值、最大值：
定处理后列表是列。
定异常数是0。

对于数据于数据列表：
定值是数据算字段名。

若值小于最小值或值大于最大值就：
# 使用边界值替换
若值小于最小值就：
数据算字段名是最小值。
不然：
数据算字段名是最大值。

异常数加等于1。

处理后列表，添加数据。

印"处理异常值："加异常数加"条"。
回处理后列表。

# ===== 数据转换 =====

# 类型转换
定转换数据类型是函数据列表：
对于数据于数据列表：
# 编号转整数
数据·编号是数据·编号转整数。

# 数值转浮点数
数据·数值是数据·数值转浮点数。

# 时间转日期对象
数据·时间是数据·时间转日期。

印"数据类型转换完成"。

# 字段重命名
定重命名字段是函数据列表、旧名称、新名称：
对于数据于数据列表：
定值是数据算旧名称。
数据删除字段旧名称。
数据算新名称是值。

印"字段重命名："加旧名称加" → "加新名称。

# 字段计算
定计算新字段是函数据列表：
对于数据于数据列表：
# 计算数值的平方
数据·数值平方是数据·数值乘以数据·数值。

# 计算数值的分类
若数据·数值小于30就：
数据·数值分类是"低"。
不然若数据·数值小于70就：
数据·数值分类是"中"。
不然：
数据·数值分类是"高"。

# 计算季度
定月份是数据·时间取月份。
若月份小于等于3就：
数据·季度是"Q1"。
不然若月份小于等于6就：
数据·季度是"Q2"。
不然若月份小于等于9就：
数据·季度是"Q3"。
不然：
数据·季度是"Q4"。

印"新字段计算完成"。

# 数据标准化
定标准化数值字段是函数据列表、字段名：
第一步：计算均值和标准差。
定总和是0。
定数量是数据列表，长度。

对于数据于数据列表：
总和加等于数据算字段名。

定均值是总和除以数量。

定方差和是0。
对于数据于数据列表：
定差值是数据算字段名减均值。
方差和加等于差值乘以差值。

定标准差是方差和除以数量，开平方。

第二步：标准化。
对于数据于数据列表：
定标准化值是(数据算字段名减均值)除以标准差。
数据算(字段名加"_标准化")是标准化值。

印"字段标准化完成："加字段名。
印"均值："加均值加"，标准差："加标准差。

# ===== 数据过滤 =====

# 按条件过滤
定过滤数据是函数据列表、条件函数：
定过滤后列表是列。

对于数据于数据列表：
若数据，条件函数就：
过滤后列表，添加数据。

印"过滤完成：保留"加过滤后列表，长度加"条"。
回过滤后列表。

# 过滤有效数据
定过滤有效数据是函数据列表：
定条件是函数据：
回数据·是否有效。
。

回数据列表、条件，过滤数据。

# 按类别过滤
定按类别过滤是函数据列表、类别列表：
定条件是函数据：
回类别列表，包含数据·类别。
。

回数据列表、条件，过滤数据。

# 按数值范围过滤
定按数值范围过滤是函数据列表、最小值、最大值：
定条件是函数据：
回数据·数值大于等于最小值且数据·数值小于等于最大值。
。

回数据列表、条件，过滤数据。

# 按时间范围过滤
定按时间范围过滤是函数据列表、开始时间、结束时间：
定条件是函数据：
回数据·时间大于等于开始时间且数据·时间小于等于结束时间。
。

回数据列表、条件，过滤数据。

# ===== 数据聚合 =====

# 按字段分组
定按字段分组是函数据列表、字段名：
定分组结果是典。

对于数据于数据列表：
定键是数据算字段名转字符串。

若分组结果算键等于空就：
分组结果算键是列。

分组结果算键，添加数据。

印"分组完成："加分组结果，键列表，长度加"组"。
回分组结果。

# 分组统计
定分组统计是函数据列表、分组字段、统计字段、统计方法：
第一步：分组。
定分组数据是数据列表、分组字段，按字段分组。

第二步：统计。
定统计结果是列。

对于组名于分组结果，键列表：
定组数据是分组结果算组名。
定统计值是空。

若统计方法是"求和"就：
统计值是组数据，统计字段，求和。
不然若统计方法是"平均"就：
统计值是组数据，统计字段，平均。
不然若统计方法是"最大"就：
统计值是组数据，统计字段，最大。
不然若统计方法是"最小"就：
统计值是组数据，统计字段，最小。
不然若统计方法是"计数"就：
统计值是组数据，长度。

统计结果，添加典：
组名是组名
统计值是统计值
数量是组数据，长度。

回统计结果。

# 数据透视表
定数据透视表是函数据列表、行字段、列字段、值字段、聚合方法：
第一步：获取唯一值。
定行值列表是数据列表，行字段，获取唯一值。
定列值列表是数据列表，列字段，获取唯一值。

第二步：构建透视表。
定透视表是典。

对于行值于行值列表：
透视表算行值是典。

对于列值于列值列表：
# 筛选数据
定筛选数据是数据列表，过滤(函数据：
回数据算行字段等于行值且数据算列字段等于列值。
)。

# 聚合计算
定聚合值是空。
若聚合方法是"求和"就：
聚合值是筛选数据，值字段，求和。
不然若聚合方法是"平均"就：
聚合值是筛选数据，值字段，平均。
不然若聚合方法是"计数"就：
聚合值是筛选数据，长度。

透视表算行值算列值是聚合值。

回透视表。

# ===== 数据排序 =====

# 按字段排序
定按字段排序是函数据列表、字段名、是否升序：
定排序后列表是数据列表，复制。

# 使用快速排序算法
定快速排序是函列表、左、右：
若左小于右就：
定基准索引是列表、左、右，分区。
列表、左、基准索引减1，快速排序。
列表、基准索引加1、右，快速排序。
。

定分区是函列表、左、右：
定基准是列表算右算字段名。
定i是左减1。

对于j从左到右减1：
定比较结果是列表算j算字段名小于基准。

若是否升序就：
比较结果是列表算j算字段名小于基准。
不然：
比较结果是列表算j算字段名大于基准。

若比较结果就：
i加等于1。
列表、i、j，交换。

列表、i加1、right，交换。
回i加1。
。

排序后列表、0、排序后列表，长度减1，快速排序。

印"排序完成："加字段名加(若是否升序就"升序"不然"降序")。
回排序后列表。

# 多字段排序
定多字段排序是函数据列表、排序规则列表：
# 排序规则列表格式：列典字段是"名称"、升序是真……

定排序后列表是数据列表，复制。

# 从最后一个排序规则开始排序
对于i从排序规则列表，长度减1到0：
定规则是排序规则列表算i。
排序后列表是排序后列表、规则·字段、规则·升序，按字段排序。

印"多字段排序完成"。
回排序后列表。

# ===== 数据导出 =====

# 导出为CSV
定导出CSV是函数据列表、文件路径：
定CSV内容是列。

# 添加表头
定表头是"编号,名称,数值,类别,时间,状态"。
CSV内容，添加表头。

# 添加数据行
对于数据于数据列表：
定行是数据·编号转字符串加","加
数据·名称加","加
数据·数值转字符串加","加
数据·类别加","加
数据·时间转字符串加","加
数据·状态。
CSV内容，添加行。

# 写入文件
定CSV文本是CSV内容，用"\n"连接。
文件路径、CSV文本，写文件。

印"已导出CSV文件："加文件路径。

# 导出为JSON
定导出JSON是函数据列表、文件路径：
定JSON文本是数据列表，转JSON。
文件路径、JSON文本，写文件。

印"已导出JSON文件："加文件路径。

# ===== 辅助函数 =====

# 获取唯一值列表
定获取唯一值是函数据列表、字段名：
定唯一值是列。
定已见是典。

对于数据于数据列表：
定值是数据算字段名转字符串。

若已见算值等于空就：
唯一值，添加数据算字段名。
已见算值是真。

回唯一值。

# 字段求和
定字段求和是函数据列表、字段名：
定总和是0。

对于数据于数据列表：
总和加等于数据算字段名。

回总和。

# 字段平均
定字段平均是函数据列表、字段名：
定总和是数据列表、字段名，字段求和。
定数量是数据列表，长度。

回总和除以数量。

# 字段最大值
定字段最大值是函数据列表、字段名：
定最大值是负无穷。

对于数据于数据列表：
定值是数据算字段名。

若值大于最大值就：
最大值是值。

回最大值。

# 字段最小值
定字段最小值是函数据列表、字段名：
定最小值是正无穷。

对于数据于数据列表：
定值是数据算字段名。

若值小于最小值就：
最小值是值。

回最小值。

# ===== 主程序 =====

印"========================================"。
印"数据处理示例"。
印"========================================"。
印""。

# 生成测试数据
印"【第一步：生成测试数据】"。
定原始数据是100，生成测试数据。
原始数据，添加脏数据。
印"原始数据量："加原始数据，长度加"条"。
印""。

# 数据清洗
印"【第二步：数据清洗】"。
定清洗后数据是原始数据，批量清洗数据。
清洗后数据是清洗后数据，去除重复数据。
清洗后数据是清洗后数据、"数值"、0、100，处理异常值。
印"清洗后数据量："加清洗后数据，长度加"条"。
印""。

# 数据转换
印"【第三步：数据转换】"。
清洗后数据，转换数据类型。
清洗后数据，计算新字段。
清洗后数据、"数值"，标准化数值字段。
印""。

# 数据过滤
印"【第四步：数据过滤】"。
定有效数据是清洗后数据，过滤有效数据。
定A类数据是有效数据、列"A类"，按类别过滤。
定高数值数据是有效数据、70、100，按数值范围过滤。
印"有效数据："加有效数据，长度加"条"。
印"A类数据："加A类数据，长度加"条"。
印"高数值数据："加高数值数据，长度加"条"。
印""。

# 数据聚合
印"【第五步：数据聚合】"。
定类别统计是有效数据、"类别"、"数值"、"平均"，分组统计。
印"按类别统计平均值："。
对于统计于类别统计：
印"  "加统计·组名加"："加统计·统计值转字符串(保留2位小数)加"（"加统计·数量加"条）"。
印""。

定透视表是有效数据、"类别"、"数值分类"、"数值"、"平均"，数据透视表。
印"数据透视表（类别 × 数值分类）："。
印透视表。

# 数据排序
印"【第六步：数据排序】"。
定按数值排序是有效数据、"数值"、假，按字段排序。
印"按数值降序排序（前5条）："。
对于i从0到4：
定数据是按数值排序算i。
印"  "加数据·名称加"："加数据·数值。
印""。

# 数据导出
印"【第七步：数据导出】"。
有效数据、"output_data.csv"，导出CSV。
有效数据、"output_data.json"，导出JSON。
印""。

印"========================================"。
印"数据处理完成"。
印"========================================"。
