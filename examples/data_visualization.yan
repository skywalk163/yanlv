# 数据可视化示例
# 展示如何用言律语言创建图表、图形和数据可视化

# ===== 导入可视化模块 =====

导入《可视化》。
导入《数学》。

# ===== 图表创建 =====

# 创建画布
定创建画布是函宽度、高度、标题：
  定画布是可视化，创建画布：
    宽度是宽度
    高度是高度
    标题是标题
    背景色是"#FFFFFF"。
  
  印"已创建画布："加标题。
  回画布。

# 保存图表
定保存图表是函画布、文件路径：
  画布，保存(文件路径)。
  印"图表已保存："加文件路径。

# 显示图表
定显示图表是函画布：
  画布，显示。
  印"图表已显示"。

# ===== 折线图 =====

# 创建折线图
定创建折线图是函画布、数据列表、标题、X轴标签、Y轴标签：
  定图表是可视化，创建折线图：
    画布是画布
    标题是标题
    X轴标签是X轴标签
    Y轴标签是Y轴标签
    网格是真
    图例是真。
  
  对于数据于数据列表：
    图表，添加数据系列：
      名称是数据·名称
      X值是数据·X值
      Y值是数据·Y值
      线颜色是数据·颜色
      线宽度是2
      标记是数据·标记。
  
  印"已创建折线图："加标题。
  回图表。

# 示例：销售趋势折线图
定销售趋势图是函：
  定画布是800、500、"销售趋势分析"，创建画布。
  
  # 生成数据
  定月份是列"1月"、"2月"、"3月"、"4月"、"5月"、"6月"。
  定销售额是列120、150、180、160、200、220。
  定成本是列80、100、120、110、130、140。
  
  定数据列表是列：
    典名称是"销售额"、X值是月份、Y值是销售额、颜色是"#0078D7"、标记是"圆"。
    典名称是"成本"、X值是月份、Y值是成本、颜色是"#E81123"、标记是"方"。
  。
  
  定图表是画布、数据列表、"月度销售趋势"、"月份"、"金额（万元）"，创建折线图。
  
  回典画布是画布、图表是图表。

# ===== 柱状图 =====

# 创建柱状图
定创建柱状图是函画布、数据列表、标题、X轴标签、Y轴标签：
  定图表是可视化，创建柱状图：
    画布是画布
    标题是标题
    X轴标签是X轴标签
    Y轴标签是Y轴标签
    网格是真
    图例是真。
  
  对于数据于数据列表：
    图表，添加数据系列：
      名称是数据·名称
      X值是数据·X值
      Y值是数据·Y值
      颜色是数据·颜色。
  
  印"已创建柱状图："加标题。
  回图表。

# 示例：产品销量柱状图
定产品销量图是函：
  定画布是800、500、"产品销量对比"，创建画布。
  
  # 数据
  定产品是列"产品A"、"产品B"、"产品C"、"产品D"、"产品E"。
  定销量是列150、230、180、290、200。
  
  定数据列表是列：
    典名称是"销量"、X值是产品、Y值是销量、颜色是"#0078D7"。
  。
  
  定图表是画布、数据列表、"产品销量对比"、"产品"、"销量（件）"，创建柱状图。
  
  回典画布是画布、图表是图表。

# 分组柱状图
定分组柱状图是函：
  定画布是800、500、"季度销售对比"，创建画布。
  
  # 数据
  定季度是列"Q1"、"Q2"、"Q3"、"Q4"。
  定今年是列120、150、180、200。
  定去年是列100、130、160、180。
  
  定数据列表是列：
    典名称是"今年"、X值是季度、Y值是今年、颜色是"#0078D7"。
    典名称是"去年"、X值是季度、Y值是去年、颜色是"#E81123"。
  。
  
  定图表是画布、数据列表、"季度销售对比"、"季度"、"销售额（万元）"，创建柱状图。
  
  回典画布是画布、图表是图表。

# ===== 饼图 =====

# 创建饼图
定创建饼图是函画布、数据列表、标题：
  定图表是可视化，创建饼图：
    画布是画布
    标题是标题
    图例是真
    显示百分比是真。
  
  对于数据于数据列表：
    图表，添加数据：
      名称是数据·名称
      值是数据·值
      颜色是数据·颜色。
  
  印"已创建饼图："加标题。
  回图表。

# 示例：市场份额饼图
定市场份额图是函：
  定画布是800、500、"市场份额分布"，创建画布。
  
  # 数据
  定数据列表是列：
    典名称是"产品A"、值是35、颜色是"#0078D7"。
    典名称是"产品B"、值是25、颜色是"#E81123"。
    典名称是"产品C"、值是20、颜色是"#107C10"。
    典名称是"产品D"、值是15、颜色是"#FFB900"。
    典名称是"其他"、值是5、颜色是"#8A8886"。
  。
  
  定图表是画布、数据列表、"市场份额分布"，创建饼图。
  
  回典画布是画布、图表是图表。

# 环形图
定环形图是函：
  定画布是800、500、"用户年龄分布"，创建画布。
  
  # 数据
  定数据列表是列：
    典名称是"18-25岁"、值是30、颜色是"#0078D7"。
    典名称是"26-35岁"、值是35、颜色是"#E81123"。
    典名称是"36-45岁"、值是20、颜色是"#107C10"。
    典名称是"46岁以上"、值是15、颜色是"#FFB900"。
  。
  
  定图表是可视化，创建环形图：
    画布是画布
    标题是"用户年龄分布"
    数据是数据列表
    内半径是0.5
    图例是真。
  
  回典画布是画布、图表是图表。

# ===== 散点图 =====

# 创建散点图
定创建散点图是函画布、数据列表、标题、X轴标签、Y轴标签：
  定图表是可视化，创建散点图：
    画布是画布
    标题是标题
    X轴标签是X轴标签
    Y轴标签是Y轴标签
    网格是真
    图例是真。
  
  对于数据于数据列表：
    图表，添加数据系列：
      名称是数据·名称
      X值是数据·X值
      Y值是数据·Y值
      颜色是数据·颜色
      标记大小是数据·大小。
  
  印"已创建散点图："加标题。
  回图表。

# 示例：身高体重散点图
定身高体重图是函：
  定画布是800、500、"身高体重分布"，创建画布。
  
  # 生成随机数据
  定男性数据是列。
  定女性数据是列。
  
  对于i从1到50：
    男性数据，添加典X值是随机数(160, 190)、Y值是随机数(50, 90)。
    女性数据,添加典X值是随机数(150, 175)、Y值是随机数(40, 70)。
  。
  
  定数据列表是列：
    典名称是"男性"、X值是男性数据·X值、Y值是男性数据·Y值、颜色是"#0078D7"、大小是8。
    典名称是"女性"、X值是女性数据·X值、Y值是女性数据·Y值、颜色是"#E81123"、大小是8。
  。
  
  定图表是画布、数据列表、"身高体重分布"、"身高（cm）"、"体重（kg）"，创建散点图。
  
  回典画布是画布、图表是图表。

# ===== 面积图 =====

# 创建面积图
定创建面积图是函画布、数据列表、标题、X轴标签、Y轴标签：
  定图表是可视化，创建面积图：
    画布是画布
    标题是标题
    X轴标签是X轴标签
    Y轴标签是Y轴标签
    网格是真
    图例是真
    堆叠是假。
  
  对于数据于数据列表：
    图表，添加数据系列：
      名称是数据·名称
      X值是数据·X值
      Y值是数据·Y值
      颜色是数据·颜色
      透明度是0.3。
  
  印"已创建面积图："加标题。
  回图表。

# 示例：网站流量面积图
定网站流量图是函：
  定画布是800、500、"网站流量趋势"，创建画布。
  
  # 数据
  定日期是列"周一"、"周二"、"周三"、"周四"、"周五"、"周六"、"周日"。
  定访问量是列1200、1500、1800、1600、2000、2500、2200]。
  定独立访客是列800、1000、1200、1100、1400、1800、1600。
  
  定数据列表是列：
    典名称是"访问量"、X值是日期、Y值是访问量、颜色是"#0078D7"。
    典名称是"独立访客"、X值是日期、Y值是独立访客、颜色是"#107C10"。
  。
  
  定图表是画布、数据列表、"网站流量趋势"、"日期"、"数量"，创建面积图。
  
  回典画布是画布、图表是图表。

# 堆叠面积图
定堆叠面积图是函：
  定画布是800、500、"收入来源构成"，创建画布。
  
  # 数据
  定月份是列"1月"、"2月"、"3月"、"4月"、"5月"、"6月"。
  定产品收入是列50、60、70、65、80、90。
  定服务收入是列30、35、40、38、45、50。
  定其他收入是列10、12、15、14、18、20。
  
  定图表是可视化，创建面积图：
    画布是画布
    标题是"收入来源构成"
    X轴标签是"月份"
    Y轴标签是"金额（万元）"
    堆叠是真。
  
  图表，添加数据系列：典名称是"产品收入"、X值是月份、Y值是产品收入、颜色是"#0078D7"。
  图表，添加数据系列：典名称是"服务收入"、X值是月份、Y值是服务收入、颜色是"#107C10"。
  图表，添加数据系列：典名称是"其他收入"、X值是月份、Y值是其他收入、颜色是"#FFB900"。
  
  回典画布是画布、图表是图表。

# ===== 直方图 =====

# 创建直方图
定创建直方图是函画布、数据列表、标题、X轴标签、Y轴标签、分组数：
  定图表是可视化，创建直方图：
    画布是画布
    标题是标题
    X轴标签是X轴标签
    Y轴标签是Y轴标签
    分组数是分组数
    网格是真。
  
  对于数据于数据列表：
    图表，添加数据系列：
      名称是数据·名称
      值是数据·值
      颜色是数据·颜色。
  
  印"已创建直方图："加标题。
  回图表。

# 示例：成绩分布直方图
定成绩分布图是函：
  定画布是800、500、"考试成绩分布"，创建画布。
  
  # 生成随机成绩数据
  定成绩数据是列。
  对于i从1到100：
    成绩数据，添加随机数(50, 100)。
  。
  
  定数据列表是列：
    典名称是"成绩"、值是成绩数据、颜色是"#0078D7"。
  。
  
  定图表是画布、数据列表、"考试成绩分布"、"分数"、"人数"、10，创建直方图。
  
  回典画布是画布、图表是图表。

# ===== 箱线图 =====

# 创建箱线图
定创建箱线图是函画布、数据列表、标题、Y轴标签：
  定图表是可视化，创建箱线图：
    画布是画布
    标题是标题
    Y轴标签是Y轴标签
    网格是真
    图例是真。
  
  对于数据于数据列表：
    图表，添加数据系列：
      名称是数据·名称
      值是数据·值
      颜色是数据·颜色。
  
  印"已创建箱线图："加标题。
  回图表。

# 示例：班级成绩箱线图
定班级成绩箱线图是函：
  定画布是800、500、"班级成绩分布"，创建画布。
  
  # 生成随机数据
  定一班成绩是列。
  定二班成绩是列。
  定三班成绩是列。
  
  对于i从1到30：
    一班成绩，添加随机数(60, 95)。
    二班成绩，添加随机数(55, 90)。
    三班成绩，添加随机数(65, 100)。
  。
  
  定数据列表是列：
    典名称是"一班"、值是一班成绩、颜色是"#0078D7"。
    典名称是"二班"、值是二班成绩、颜色是"#E81123"。
    典名称是"三班"、值是三班成绩、颜色是"#107C10"。
  。
  
  定图表是画布、数据列表、"班级成绩分布"、"分数"，创建箱线图。
  
  回典画布是画布、图表是图表。

# ===== 热力图 =====

# 创建热力图
定创建热力图是函画布、数据矩阵、X标签、Y标签、标题：
  定图表是可视化，创建热力图：
    画布是画布
    标题是标题
    X标签是X标签
    Y标签是Y标签
    颜色映射是"热度"。
  
  图表，设置数据(数据矩阵)。
  
  印"已创建热力图："加标题。
  回图表。

# 示例：相关性热力图
定相关性热力图是函：
  定画布是800、600、"特征相关性"，创建画布。
  
  # 相关性矩阵数据
  定特征是列"特征A"、"特征B"、"特征C"、"特征D"、"特征E"。
  
  定相关性矩阵是列：
    列1.0、0.8、0.3、负0.2、0.5。
    列0.8、1.0、0.4、负0.1、0.6。
    列0.3、0.4、1.0、0.7、0.2。
    列负0.2、负0.1、0.7、1.0、负0.3。
    列0.5、0.6、0.2、负0.3、1.0。
  。
  
  定图表是画布、相关性矩阵、特征、特征、"特征相关性"，创建热力图。
  
  回典画布是画布、图表是图表。

# ===== 雷达图 =====

# 创建雷达图
定创建雷达图是函画布、数据列表、标题：
  定图表是可视化，创建雷达图：
    画布是画布
    标题是标题
    图例是真
    填充是真。
  
  对于数据于数据列表：
    图表，添加数据系列：
      名称是数据·名称
      值是数据·值
      颜色是数据·颜色
      透明度是0.3。
  
  印"已创建雷达图："加标题。
  回图表。

# 示例：能力雷达图
定能力雷达图是函：
  定画布是800、600、"员工能力评估"，创建画布。
  
  # 数据
  定能力维度是列"技术能力"、"沟通能力"、"团队协作"、"创新能力"、"执行力"、"学习能力"。
  
  定员工A是列85、75、80、70、90、85。
  定员工B是列90、85、75、85、80、90。
  
  定数据列表是列：
    典名称是"员工A"、值是员工A、颜色是"#0078D7"。
    典名称是"员工B"、值是员工B、颜色是"#E81123"。
  。
  
  定图表是画布、数据列表、"员工能力评估"，创建雷达图。
  
  回典画布是画布、图表是图表。

# ===== 组合图表 =====

# 创建组合图表
定创建组合图表是函画布、标题：
  定图表是可视化，创建组合图表：
    画布是画布
    标题是标题。
  
  印"已创建组合图表："加标题。
  回图表。

# 示例：销售额与增长率组合图
定销售额增长率图是函：
  定画布是800、500、"销售额与增长率"，创建画布。
  
  # 数据
  定月份是列"1月"、"2月"、"3月"、"4月"、"5月"、"6月"。
  定销售额是列100、120、150、140、180、200。
  定增长率是列0、20、25、负6.7、28.6、11.1。
  
  定图表是画布、"销售额与增长率"，创建组合图表。
  
  # 添加柱状图（销售额）
  图表，添加柱状图系列：
    名称是"销售额"
    X值是月份
    Y值是销售额
    颜色是"#0078D7"
    Y轴是"左"。
  
  # 添加折线图（增长率）
  图表，添加折线图系列：
    名称是"增长率"
    X值是月份
    Y值是增长率
    颜色是"#E81123"
    Y轴是"右"。
  
  回典画布是画布、图表是图表。

# ===== 动态图表 =====

# 创建动画图表
定创建动画图表是函画布、数据列表、标题、动画类型：
  定图表是可视化，创建动画图表：
    画布是画布
    标题是标题
    动画类型是动画类型
    动画时长是1000。
  
  对于数据于数据列表：
    图表，添加数据系列：
      名称是数据·名称
      X值是数据·X值
      Y值是数据·Y值
      颜色是数据·颜色。
  
  印"已创建动画图表："加标题。
  回图表。

# 示例：动态增长图
定动态增长图是函：
  定画布是800、500、"动态增长演示"，创建画布。
  
  # 数据
  定年份是列"2019"、"2020"、"2021"、"2022"、"2023"、"2024"。
  定用户数是列100、200、350、500、750、1000。
  
  定数据列表是列：
    典名称是"用户数"、X值是年份、Y值是用户数、颜色是"#0078D7"。
  。
  
  定图表是画布、数据列表、"动态增长演示"、"增长"，创建动画图表。
  
  回典画布是画布、图表是图表。

# ===== 图表美化 =====

# 设置图表样式
定设置图表样式是函图表、样式配置：
  图表，设置标题样式：
    字体是样式配置·标题字体
    字号是样式配置·标题字号
    颜色是样式配置·标题颜色。
  
  图表，设置轴样式：
    字体是样式配置·轴字体
    字号是样式配置·轴字号
    颜色是样式配置·轴颜色。
  
  图表，设置网格样式：
    显示是样式配置·显示网格
    颜色是样式配置·网格颜色
    线型是样式配置·网格线型。
  
  图表，设置图例样式：
    位置是样式配置·图例位置
    字体是样式配置·图例字体
    字号是样式配置·图例字号。
  
  印"已设置图表样式"。

# 默认样式配置
定默认样式配置是典：
  标题字体是"微软雅黑"
  标题字号是16
  标题颜色是"#333333"
  轴字体是"微软雅黑"
  轴字号是12
  轴颜色是"#666666"
  显示网格是真
  网格颜色是"#E0E0E0"
  网格线型是"虚线"
  图例位置是"右上"
  图例字体是"微软雅黑"
  图例字号是11。

# ===== 图表导出 =====

# 导出为图片
定导出图片是函画布、文件路径、格式：
  若格式等于空就：
    格式是"PNG"。
  
  画布，导出图片：
    路径是文件路径
    格式是格式
    DPI是300。
  
  印"已导出图片："加文件路径。

# 导出为PDF
定导出PDF是函画布、文件路径：
  画布，导出PDF：
    路径是文件路径
    页面大小是"A4"。
  
  印"已导出PDF："加文件路径。

# 导出为SVG
定导出SVG是函画布、文件路径：
  画布，导出SVG(文件路径)。
  印"已导出SVG："加文件路径。

# ===== 主程序 =====

印"========================================"。
印"数据可视化示例"。
印"========================================"。
印""。

# 创建输出目录
定输出目录是"./charts"。
输出目录，创建目录。

# 折线图示例
印"【示例1：折线图】"。
定折线图是销售趋势图。
折线图·画布、输出目录，路径连接"line_chart.png"，导出图片。
印""。

# 柱状图示例
印"【示例2：柱状图】"。
定柱状图是产品销量图。
柱状图·画布、输出目录，路径连接"bar_chart.png"，导出图片。
印""。

# 分组柱状图示例
印"【示例3：分组柱状图】"。
定分组图是分组柱状图。
分组图·画布、输出目录，路径连接"grouped_bar_chart.png"，导出图片。
印""。

# 饼图示例
印"【示例4：饼图】"。
定饼图是市场份额图。
饼图·画布、输出目录，路径连接"pie_chart.png"，导出图片。
印""。

# 环形图示例
印"【示例5：环形图】"。
定环形是环形图。
环形·画布、输出目录，路径连接"donut_chart.png"，导出图片。
印""。

# 散点图示例
印"【示例6：散点图】"。
定散点图是身高体重图。
散点图·画布、输出目录，路径连接"scatter_chart.png"，导出图片。
印""。

# 面积图示例
印"【示例7：面积图】"。
定面积图是网站流量图。
面积图·画布、输出目录，路径连接"area_chart.png"，导出图片。
印""。

# 堆叠面积图示例
印"【示例8：堆叠面积图】"。
定堆叠图是堆叠面积图。
堆叠图·画布、输出目录，路径连接"stacked_area_chart.png"，导出图片。
印""。

# 直方图示例
印"【示例9：直方图】"。
定直方图是成绩分布图。
直方图·画布、输出目录，路径连接"histogram.png"，导出图片。
印""。

# 箱线图示例
印"【示例10：箱线图】"。
定箱线图是班级成绩箱线图。
箱线图·画布、输出目录，路径连接"box_plot.png"，导出图片。
印""。

# 热力图示例
印"【示例11：热力图】"。
定热力图是相关性热力图。
热力图·画布、输出目录，路径连接"heatmap.png"，导出图片。
印""。

# 雷达图示例
印"【示例12：雷达图】"。
定雷达图是能力雷达图。
雷达图·画布、输出目录，路径连接"radar_chart.png"，导出图片。
印""。

# 组合图表示例
印"【示例13：组合图表】"。
定组合图是销售额增长率图。
组合图·画布、输出目录，路径连接"combination_chart.png"，导出图片。
印""。

# 动态图表示例
印"【示例14：动态图表】"。
定动态图是动态增长图。
动态图·画布、输出目录，路径连接"animated_chart.png"，导出图片。
印""。

印"========================================"。
印"数据可视化完成"。
印"所有图表已保存到："加输出目录。
印"========================================"。
