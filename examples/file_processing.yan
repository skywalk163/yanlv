# 文件处理示例
# 展示如何用言律语言进行文件读写、批量处理、文件搜索、目录操作

# ===== 导入文件处理模块 =====

导入《文件系统》。
导入《路径》。
导入《编码》。

# ===== 文件读写操作 =====

# 读取文本文件
定读取文本文件是函文件路径：
  若文件路径，文件存在等于假就：
    印"文件不存在："加文件路径。
    回空。
  
  定文件内容是文件路径，读文件。
  印"已读取文件："加文件路径。
  印"文件大小："加文件内容，长度加"字节"。
  
  回文件内容。

# 写入文本文件
定写入文本文件是函文件路径、内容：
  文件路径、内容，写文件。
  印"已写入文件："加文件路径。
  印"写入大小："加内容，长度加"字节"。

# 追加文本到文件
定追加文本是函文件路径、内容：
  文件路径、内容，追加文件。
  印"已追加内容到文件："加文件路径。

# 读取文件行
定读取文件行是函文件路径：
  若文件路径，文件存在等于假就：
    回列。
  
  定内容是文件路径，读文件。
  定行列表是内容，用"\n"分割。
  
  印"文件行数："加行列表，长度。
  回行列表。

# 读取JSON文件
定读取JSON文件是函文件路径：
  定内容是文件路径，读文件。
  定数据是内容，解析JSON。
  
  印"已读取JSON文件："加文件路径。
  回数据。

# 写入JSON文件
定写入JSON文件是函文件路径、数据：
  定JSON文本是数据，生成JSON(缩进是2)。
  文件路径、JSON文本，写文件。
  
  印"已写入JSON文件："加文件路径。

# 读取CSV文件
定读取CSV文件是函文件路径、分隔符：
  若分隔符等于空就：
    分隔符是","。
  
  定内容是文件路径，读文件。
  定行列表是内容，用"\n"分割。
  
  定数据列表是列。
  定表头是空。
  
  对于i从0到行列表，长度减1：
    定行是行列表算i。
    
    若行等于""就：
      继续。
    
    定字段列表是行，用分隔符分割。
    
    若i等于0就：
      表头是字段列表。
    不然：
      定数据是典。
      对于j从0到字段列表，长度减1：
        定字段名是表头算j。
        定字段值是字段列表算j。
        数据算字段名是字段值。
      
      数据列表，添加数据。
  
  印"已读取CSV文件："加文件路径。
  印"数据行数："加数据列表，长度。
  
  回数据列表。

# 写入CSV文件
定写入CSV文件是函文件路径、数据列表、字段列表、分隔符：
  若分隔符等于空就：
    分隔符是","。
  
  定CSV内容是列。
  
  # 写入表头
  定表头是字段列表，用分隔符连接。
  CSV内容，添加表头。
  
  # 写入数据行
  对于数据于数据列表：
    定字段值列表是列。
    对于字段名于字段列表：
      定值是数据算字段名转字符串。
      字段值列表，添加值。
    
    定行是字段值列表，用分隔符连接。
    CSV内容，添加行。
  
  定CSV文本是CSV内容，用"\n"连接。
  文件路径、CSV文本，写文件。
  
  印"已写入CSV文件："加文件路径。
  印"数据行数："加数据列表，长度。

# ===== 目录操作 =====

# 创建目录
定创建目录是函目录路径：
  若目录路径，目录存在就：
    印"目录已存在："加目录路径。
    回。
  
  目录路径，创建目录。
  印"已创建目录："加目录路径。

# 删除目录
定删除目录是函目录路径、是否递归：
  若目录路径，目录存在等于假就：
    印"目录不存在："加目录路径。
    回。
  
  若是否递归就：
    目录路径，递归删除目录。
    印"已递归删除目录："加目录路径。
  不然：
    目录路径，删除目录。
    印"已删除目录："加目录路径。

# 列出目录内容
定列出目录是函目录路径：
  若目录路径，目录存在等于假就：
    印"目录不存在："加目录路径。
    回列。
  
  定内容列表是目录路径，列目录。
  
  印"目录内容："加目录路径。
  对于项于内容列表：
    定类型是若项·是目录就"[目录]"不然"[文件]"。
    印"  "加类型加" "加项·名称。
  
  回内容列表。

# 递归列出目录
定递归列出目录是函目录路径：
  定所有文件是列。
  
  定遍历目录是函路径、层级：
    定内容是路径，列目录。
    
    对于项于内容：
      定缩进是""。
      对于i从0到层级减1：
        缩加等于"  "。
      
      若项·是目录就：
        印缩加"[目录] "加项·名称。
        定子路径是路径，路径连接项·名称。
        子路径、层级加1，遍历目录。
      不然：
        印缩加"[文件] "加项·名称加" ("加项·大小加"字节)"。
        所有文件，添加典：
          路径是路径，路径连接项·名称
          名称是项·名称
          大小是项·大小。
  。
  
  目录路径、0，遍历目录。
  
  印"总文件数："加所有文件，长度。
  回所有文件。

# 复制目录
定复制目录是函源目录、目标目录：
  若源目录，目录存在等于假就：
    印"源目录不存在："加源目录。
    回假。
  
  # 创建目标目录
  目标目录，创建目录。
  
  # 复制文件和子目录
  定内容是源目录，列目录。
  
  对于项于内容：
    定源路径是源目录，路径连接项·名称。
    定目标路径是目标目录，路径连接项·名称。
    
    若项·是目录就：
      源路径、目标路径，复制目录。
    不然：
      源路径、目标路径，复制文件。
  
  印"已复制目录："加源目录加" → "加目标目录。
  回真。

# ===== 文件搜索 =====

# 按名称搜索文件
定搜索文件是函目录路径、文件名模式：
  定匹配文件是列。
  
  定递归搜索是函路径：
    若路径，目录存在等于假就：
      回。
    
    定内容是路径，列目录。
    
    对于项于内容：
      若项·是目录就：
        定子路径是路径，路径连接项·名称。
        子路径，递归搜索。
      不然：
        若项·名称，匹配模式(文件名模式)就：
          匹配文件，添加典：
            路径是路径，路径连接项·名称
            名称是项·名称
            大小是项·大小
            修改时间是项·修改时间。
  。
  
  目录路径，递归搜索。
  
  印"找到"加匹配文件，长度加"个匹配文件"。
  回匹配文件。

# 按内容搜索文件
定搜索文件内容是函目录路径、搜索文本、文件扩展名：
  定匹配文件是列。
  
  定递归搜索是函路径：
    若路径，目录存在等于假就：
      回。
    
    定内容是路径，列目录。
    
    对于项于内容：
      若项·是目录就：
        定子路径是路径，路径连接项·名称。
        子路径，递归搜索。
      不然：
        # 检查文件扩展名
        定扩展名是项·名称，获取扩展名。
        
        若文件扩展名等于空或文件扩展名，包含(扩展名)就：
          定文件路径是路径，路径连接项·名称。
          定内容是文件路径，读文件。
          
          若内容，包含(搜索文本)就：
            匹配文件，添加典：
              路径是文件路径
              名称是项·名称
              匹配行数是内容，统计匹配(搜索文本)。
  。
  
  目录路径，递归搜索。
  
  印"找到"加匹配文件，长度加"个包含'"加搜索文本加"'的文件"。
  回匹配文件。

# 按大小搜索文件
定搜索大文件是函目录路径、最小大小：
  定大文件是列。
  
  定递归搜索是函路径：
    若路径，目录存在等于假就：
      回。
    
    定内容是路径，列目录。
    
    对于项于内容：
      若项·是目录就：
        定子路径是路径，路径连接项·名称。
        子路径，递归搜索。
      不然：
        若项·大小大于等于最小大小就：
          大文件，添加典：
            路径是路径，路径连接项·名称
            名称是项·名称
            大小是项·大小。
  。
  
  目录路径，递归搜索。
  
  印"找到"加大文件，长度加"个大文件(≥"加最小大小加"字节)"。
  回大文件。

# 按修改时间搜索文件
定搜索最近文件是函目录路径、天数：
  定最近文件是列。
  定当前时间是当前时间。
  定时间阈值是当前时间减天数乘以86400。  # 86400秒/天
  
  定递归搜索是函路径：
    若路径，目录存在等于假就：
      回。
    
    定内容是路径，列目录。
    
    对于项于内容：
      若项·是目录就：
        定子路径是路径，路径连接项·名称。
        子路径，递归搜索。
      不然：
        若项·修改时间大于等于时间阈值就：
          最近文件，添加典：
            路径是路径，路径连接项·名称
            名称是项·名称
            修改时间是项·修改时间。
  。
  
  目录路径，递归搜索。
  
  印"找到"加最近文件，长度加"个最近"加天数加"天内修改的文件"。
  回最近文件。

# ===== 批量文件操作 =====

# 批量重命名文件
定批量重命名是函目录路径、旧模式、新模式：
  定重命名数是0。
  
  定内容是目录路径，列目录。
  
  对于项于内容：
    若项·是目录等于假就：
      定旧名称是项·名称。
      
      若旧名称，匹配模式(旧模式)就：
        定新名称是旧名称，替换模式(旧模式、新模式)。
        定旧路径是目录路径，路径连接旧名称。
        定新路径是目录路径，路径连接新名称。
        
        旧路径、新路径，重命名文件。
        重命名数加等于1。
        
        印"重命名："加旧名称加" → "加新名称。
  
  印"批量重命名完成，共"加重命名数加"个文件"。

# 批量复制文件
定批量复制是函源目录、目标目录、文件模式：
  定复制数是0。
  
  目标目录，创建目录。
  
  定内容是源目录，列目录。
  
  对于项于内容：
    若项·是目录等于假就：
      若项·名称，匹配模式(文件模式)就：
        定源路径是源目录，路径连接项·名称。
        定目标路径是目标目录，路径连接项·名称。
        
        源路径、目标路径，复制文件。
        复制数加等于1。
  
  印"批量复制完成，共"加复制数加"个文件"。

# 批量删除文件
定批量删除是函目录路径、文件模式：
  定删除数是0。
  
  定内容是目录路径，列目录。
  
  对于项于内容：
    若项·是目录等于假就：
      若项·名称，匹配模式(文件模式)就：
        定文件路径是目录路径，路径连接项·名称。
        文件路径，删除文件。
        删除数加等于1。
        
        印"已删除："加项·名称。
  
  印"批量删除完成，共"加删除数加"个文件"。

# 批量移动文件
定批量移动是函源目录、目标目录、文件模式：
  定移动数是0。
  
  目标目录，创建目录。
  
  定内容是源目录，列目录。
  
  对于项于内容：
    若项·是目录等于假就：
      若项·名称，匹配模式(文件模式)就：
        定源路径是源目录，路径连接项·名称。
        定目标路径是目标目录，路径连接项·名称。
        
        源路径、目标路径，移动文件。
        移动数加等于1。
  
  印"批量移动完成，共"加移动数加"个文件"。

# ===== 文件监控 =====

# 监控目录变化
定监控目录变化是函目录路径、回调函数、间隔秒数：
  定上次状态是典。
  
  # 获取初始状态
  定获取状态是函：
    定状态是典。
    定内容是目录路径，列目录。
    
    对于项于内容：
      状态算(项·名称)是典：
        大小是项·大小
        修改时间是项·修改时间
        是目录是项·是目录。
    
    回状态。
  。
  
  上次状态是获取状态。
  
  # 监控循环
  定监控中是真。
  
  当监控中：
    睡眠(间隔秒数)。
    
    定当前状态是获取状态。
    
    # 检查新增文件
    对于文件名于当前状态，键列表：
      若上次状态算文件名等于空就：
        目录路径、文件名、"新增"，回调函数。
    
    # 检查删除文件
    对于文件名于上次状态，键列表：
      若当前状态算文件名等于空就：
        目录路径、文件名、"删除"，回调函数。
    
    # 检查修改文件
    对于文件名于当前状态，键列表：
      若上次状态算文件名不等于空就：
        定旧信息是上次状态算文件名。
        定新信息是当前状态算文件名。
        
        若旧信息·大小不等于新信息·大小或旧信息·修改时间不等于新信息·修改时间就：
          目录路径、文件名、"修改"，回调函数。
    
    上次状态是当前状态。

# ===== 文件压缩 =====

# 压缩文件
定压缩文件是函源路径、目标路径：
  源路径、目标路径，创建压缩文件。
  印"已压缩文件："加源路径加" → "加目标路径。

# 解压文件
定解压文件是函源路径、目标目录：
  源路径、目标目录，解压文件。
  印"已解压文件："加源路径加" → "加目标目录。

# 批量压缩
定批量压缩是函文件列表、目标路径：
  文件列表、目标路径，创建压缩文件。
  印"已批量压缩"加文件列表，长度加"个文件到："加目标路径。

# ===== 文件编码转换 =====

# 转换文件编码
定转换文件编码是函文件路径、源编码、目标编码：
  定内容是文件路径，用源编码读文件。
  文件路径、内容，用目标编码写文件。
  
  印"已转换编码："加文件路径加" ("加源编码加" → "加目标编码加")"。

# 检测文件编码
定检测文件编码是函文件路径：
  定编码是文件路径，检测编码。
  印"文件编码："加文件路径加" = "加编码。
  回编码。

# ===== 文件比较 =====

# 比较两个文件
定比较文件是函文件路径1、文件路径2：
  定内容1是文件路径1，读文件。
  定内容2是文件路径2，读文件。
  
  若内容1等于内容2就：
    印"文件内容相同"。
    回真。
  不然：
    印"文件内容不同"。
    
    # 显示差异
    定行列表1是内容1，用"\n"分割。
    定行列表2是内容2，用"\n"分割。
    
    定最大行数是行列表1，长度和行列表2，长度的最大值。
    
    对于i从0到最大行数减1：
      定行1是若i小于行列表1，长度就行列表1算i不然""。
      定行2是若i小于行列表2，长度就行列表2算i不然""。
      
      若行1不等于行2就：
        印"第"加(i加1)加"行不同："。
        印"  文件1："加行1。
        印"  文件2："加行2。
    
    回假。

# 比较目录
定比较目录是函目录路径1、目录路径2：
  定差异列表是列。
  
  定内容1是目录路径1，列目录。
  定内容2是目录路径2，列目录。
  
  定文件集合1是典。
  定文件集合2是典。
  
  对于项于内容1：
    若项·是目录等于假就：
      文件集合1算(项·名称)是项。
  
  对于项于内容2：
    若项·是目录等于假就：
      文件集合2算(项·名称)是项。
  
  # 检查新增文件
  对于文件名于文件集合2，键列表：
    若文件集合1算文件名等于空就：
      差异列表，添加典：
        类型是"新增"
        文件名是文件名
        路径是目录路径2，路径连接文件名。
  
  # 检查删除文件
  对于文件名于文件集合1，键列表：
    若文件集合2算文件名等于空就：
      差异列表，添加典：
        类型是"删除"
        文件名是文件名
        路径是目录路径1，路径连接文件名。
  
  # 检查修改文件
  对于文件名于文件集合1，键列表：
    若文件集合2算文件名不等于空就：
      定文件路径1是目录路径1，路径连接文件名。
      定文件路径2是目录路径2，路径连接文件名。
      
      若文件路径1、文件路径2，比较文件等于假就：
        差异列表，添加典：
          类型是"修改"
          文件名是文件名
          路径1是文件路径1
          路径2是文件路径2。
  
  印"目录比较完成，发现"加差异列表，长度加"处差异"。
  回差异列表。

# ===== 主程序 =====

印"========================================"。
印"文件处理示例"。
印"========================================"。
印""。

# 创建测试目录
印"【第一步：创建测试目录和文件】"。
定测试目录是"./test_files"。
测试目录，创建目录。

# 创建测试文件
定测试文件1是测试目录，路径连接"test1.txt"。
定测试文件2是测试目录，路径连接"test2.txt"。
定测试JSON是测试目录，路径连接"data.json"。
定测试CSV是测试目录，路径连接"data.csv"。

"这是测试文件1的内容\n第二行\n第三行"，写入文本文件(测试文件1)。
"这是测试文件2的内容\n第二行\n第三行"，写入文本文件(测试文件2)。

# 写入JSON文件
定测试数据是典：
  名称是"测试项目"
  数值是100
  列表是列1、2、3、4、5。

测试JSON、测试数据，写入JSON文件。

# 写入CSV文件
定CSV数据是列：
  典名称是"张三"、年龄是25、城市是"北京"。
  典名称是"李四"、年龄是30、城市是"上海"。
  典名称是"王五"、年龄是28、城市是"广州"。

测试CSV、CSV数据、列"名称"、"年龄"、"城市"，写入CSV文件。

印""。

# 读取文件
印"【第二步：读取文件】"。
定内容1是测试文件1，读取文本文件。
印"文件内容："加内容1。

定JSON数据是测试JSON，读取JSON文件。
印"JSON数据："加JSON数据·名称。

定CSV数据是测试CSV，读取CSV文件(分隔符是",")。
印"CSV数据行数："加CSV数据，长度。

印""。

# 目录操作
印"【第三步：目录操作】"。
测试目录，列出目录。

定子目录是测试目录，路径连接"subdir"。
子目录，创建目录。
"子目录文件内容"，写入文本文件(子目录，路径连接"file.txt")。

测试目录，递归列出目录。

印""。

# 文件搜索
印"【第四步：文件搜索】"。
定搜索结果是测试目录、"*.txt"，搜索文件。
对于文件于搜索结果：
  印"找到文件："加文件·路径。

印""。

# 批量操作
印"【第五步：批量操作】"。
定备份目录是测试目录，路径连接"backup"。
测试目录、备份目录、"*.txt"，批量复制。

印""。

# 文件比较
印"【第六步：文件比较】"。
测试文件1、测试文件2，比较文件。

印""。

# 清理
印"【第七步：清理测试文件】"。
测试目录、真，删除目录。
印"已删除测试目录"。

印""。
印"========================================"。
印"文件处理完成"。
印"========================================"。
