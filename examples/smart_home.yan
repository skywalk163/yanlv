# 智能家居控制系统

输出 "=== 智能家居控制 ==="

# 传感器数据
定 温度 是 28
定 湿度 是 65
定 光线 是 "昏暗"
定 有人 是 1

# 温度控制
输出 "温度控制："
温度 大于 28，输出 "开启空调制冷"。
温度 小于 20，输出 "开启空调制热"。
温度 等于 28，输出 "温度适宜"。

# 湿度控制
输出 "湿度控制："
湿度 大于 70，输出 "开启除湿机"。
湿度 小于 40，输出 "开启加湿器"。
湿度 等于 65，输出 "湿度正常"。

# 光线控制
输出 "光线控制："
光线 等于 "昏暗"，输出 "开启灯光"。
光线 等于 "明亮"，输出 "关闭灯光"。

# 综合判断
输出 "综合状态："
温度 大于 25，输出 "环境偏热"。
湿度 大于 60，输出 "环境偏湿"。

输出 "=== 控制完成 ==="
