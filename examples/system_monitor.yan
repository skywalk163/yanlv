# 系统监控示例
# 展示如何用言律语言进行系统资源监控、进程管理、日志处理、自动化任务

# ===== 导入系统监控模块 =====

导入《系统》。
导入《进程》。
导入《网络》。
导入《时间》。

# ===== 系统信息获取 =====

# 获取系统基本信息
定获取系统信息是函：
  定系统信息是典：
    操作系统是系统，获取操作系统
    版本是系统，获取系统版本
    主机名是系统，获取主机名
    用户名是系统，获取用户名
    CPU核心数是系统，获取CPU核心数
    总内存是系统，获取总内存
    总磁盘空间是系统，获取总磁盘空间。
  
  印"===== 系统信息 ====="。
  印"操作系统："加系统信息·操作系统。
  印"版本："加系统信息·版本。
  印"主机名："加系统信息·主机名。
  印"用户名："加系统信息·用户名。
  印"CPU核心数："加系统信息·CPU核心数。
  印"总内存："加(系统信息·总内存除以1024除以1024除以1024)转字符串(保留2位小数)加" GB"。
  印"总磁盘空间："加(系统信息·总磁盘空间除以1024除以1024除以1024)转字符串(保留2位小数)加" GB"。
  印""。
  
  回系统信息。

# 获取CPU使用率
定获取CPU使用率是函间隔秒数：
  若间隔秒数等于空就：
    间隔秒数是1。
  
  定使用率是系统，获取CPU使用率(间隔秒数)。
  印"CPU使用率："加使用率转字符串(保留2位小数)加"%"。
  回使用率。

# 获取内存使用情况
定获取内存使用情况是函：
  定内存信息是系统，获取内存信息。
  
  定总内存是内存信息·总内存。
  定可用内存是内存信息·可用内存。
  定已用内存是总内存减可用内存。
  定使用率是已用内存除以总内存乘以100。
  
  印"===== 内存使用情况 ====="。
  印"总内存："加(总内存除以1024除以1024除以1024)转字符串(保留2位小数)加" GB"。
  印"已用内存："加(已用内存除以1024除以1024除以1024)转字符串(保留2位小数)加" GB"。
  印"可用内存："加(可用内存除以1024除以1024除以1024)转字符串(保留2位小数)加" GB"。
  印"使用率："加使用率转字符串(保留2位小数)加"%"。
  印""。
  
  回典：
    总内存是总内存
    已用内存是已用内存
    可用内存是可用内存
    使用率是使用率。

# 获取磁盘使用情况
定获取磁盘使用情况是函：
  定磁盘列表是系统，获取磁盘信息。
  
  印"===== 磁盘使用情况 ====="。
  
  对于磁盘于磁盘列表：
    定总空间是磁盘·总空间。
    定已用空间是磁盘·已用空间。
    定可用空间是磁盘·可用空间。
    定使用率是已用空间除以总空间乘以100。
    
    印"磁盘 "加磁盘·挂载点加"："。
    印"  总空间："加(总空间除以1024除以1024除以1024)转字符串(保留2位小数)加" GB"。
    印"  已用空间："加(已用空间除以1024除以1024除以1024)转字符串(保留2位小数)加" GB"。
    印"  可用空间："加(可用空间除以1024除以1024除以1024)转字符串(保留2位小数)加" GB"。
    印"  使用率："加使用率转字符串(保留2位小数)加"%"。
  
  印""。
  回磁盘列表。

# 获取网络信息
定获取网络信息是函：
  定网络接口列表是网络，获取网络接口。
  
  印"===== 网络信息 ====="。
  
  对于接口于网络接口列表：
    印"接口 "加接口·名称加"："。
    印"  IP地址："加接口·IP地址。
    印"  MAC地址："加接口·MAC地址。
    印"  发送字节："加(接口·发送字节除以1024除以1024)转字符串(保留2位小数)加" MB"。
    印"  接收字节："加(接口·接收字节除以1024除以1024)转字符串(保留2位小数)加" MB"。
  
  印""。
  回网络接口列表。

# ===== 进程管理 =====

# 获取进程列表
定获取进程列表是函：
  定进程列表是进程，获取所有进程。
  
  印"===== 进程列表（前20个）====="。
  印"PID      名称                    CPU%    内存%"。
  印"----------------------------------------"。
  
  定计数是0。
  对于进程于进程列表：
    若计数小于20就：
      定PID是进程·PID转字符串，左填充(8)。
      定名称是进程·名称，左填充(20)。
      定CPU是进程·CPU使用率转字符串(保留1位小数)，左填充(8)。
      定内存是进程·内存使用率转字符串(保留1位小数)，左填充(8)。
      
      印PID加名称加CPU加内存。
      计数加等于1。
  
  印""。
  回进程列表。

# 查找进程
定查找进程是函进程名：
  定进程列表是进程，获取所有进程。
  定匹配进程是列。
  
  对于进程于进程列表：
    若进程·名称，包含(进程名)就：
      匹配进程，添加进程。
  
  印"找到"加匹配进程，长度加"个匹配'"加进程名加"'的进程"。
  回匹配进程。

# 启动进程
定启动进程是函命令、参数列表：
  定进程ID是进程，启动进程(命令、参数列表)。
  
  印"已启动进程："加命令。
  印"进程ID："加进程ID。
  
  回进程ID。

# 停止进程
定停止进程是函进程ID：
  进程，停止进程(进程ID)。
  印"已停止进程："加进程ID。

# 监控进程
定监控进程是函进程ID、间隔秒数、持续时间秒数：
  定开始时间是当前时间。
  定监控数据是列。
  
  当(当前时间减开始时间)小于持续时间秒数：
    定进程信息是进程，获取进程信息(进程ID)。
    
    若进程信息等于空就：
      印"进程已结束"。
      回监控数据。
    
    监控数据，添加典：
      时间是当前时间
      CPU是进程信息·CPU使用率
      内存是进程信息·内存使用率
      线程数是进程信息·线程数。
    
    印"CPU: "加进程信息·CPU使用率转字符串(保留1位小数)加"% | 内存: "加进程信息·内存使用率转字符串(保留1位小数)加"%"。
    睡眠(间隔秒数)。
  
  回监控数据。

# ===== 系统监控 =====

# 持续监控系统资源
定监控系统资源是函间隔秒数、持续时间秒数：
  定开始时间是当前时间。
  定监控记录是列。
  
  印"===== 开始监控系统资源 ====="。
  印"监控间隔："加间隔秒数加"秒"。
  印"持续时间："加持续时间秒数加"秒"。
  印""。
  
  当(当前时间减开始时间)小于持续时间秒数：
    定CPU是系统，获取CPU使用率(间隔秒数)。
    定内存是系统，获取内存信息。
    定内存使用率是(内存·总内存减内存·可用内存)除以内存·总内存乘以100。
    
    定时间戳是当前时间，格式化时间是"%H:%M:%S"。
    印时间戳加" | CPU: "加CPU转字符串(保留1位小数)加"% | 内存: "加内存使用率转字符串(保留1位小数)加"%"。
    
    监控记录，添加典：
      时间是当前时间
      CPU是CPU
      内存是内存使用率。
  
  印""。
  印"监控完成"。
  回监控记录。

# 检测系统异常
定检测系统异常是函CPU阈值、内存阈值、磁盘阈值：
  定异常列表是列。
  
  # 检测CPU使用率
  定CPU使用率是系统，获取CPU使用率(1)。
  若CPU使用率大于CPU阈值就：
    异常列表，添加典：
      类型是"CPU"
      消息是"CPU使用率过高"
      当前值是CPU使用率
      阈值是CPU阈值。
  
  # 检测内存使用率
  定内存信息是系统，获取内存信息。
  定内存使用率是(内存信息·总内存减内存信息·可用内存)除以内存信息·总内存乘以100。
  
  若内存使用率大于内存阈值就：
    异常列表，添加典：
      类型是"内存"
      消息是"内存使用率过高"
      当前值是内存使用率
      阈值是内存阈值。
  
  # 检测磁盘使用率
  定磁盘列表是系统，获取磁盘信息。
  
  对于磁盘于磁盘列表：
    定磁盘使用率是磁盘·已用空间除以磁盘·总空间乘以100。
    
    若磁盘使用率大于磁盘阈值就：
      异常列表，添加典：
        类型是"磁盘"
        消息是"磁盘使用率过高："加磁盘·挂载点
        当前值是磁盘使用率
        阈值是磁盘阈值。
  
  # 输出异常信息
  若异常列表，长度大于0就：
    印"===== 检测到系统异常 ====="。
    对于异常于异常列表：
      印"类型："加异常·类型。
      印"消息："加异常·消息。
      印"当前值："加异常·当前值转字符串(保留2位小数)加"%"。
      印"阈值："加异常·阈值转字符串(保留2位小数)加"%"。
      印""。
  不然：
    印"系统状态正常"。
  
  回异常列表。

# ===== 日志处理 =====

# 读取日志文件
定读取日志文件是函文件路径、行数：
  若文件路径，文件存在等于假就：
    印"日志文件不存在："加文件路径。
    回列。
  
  定内容是文件路径，读文件。
  定行列表是内容，用"\n"分割。
  
  定开始索引是行列表，长度减行数。
  若开始索引小于0就：
    开始索引是0。
  
  定日志列表是列。
  对于i从开始索引到行列表，长度减1：
    日志列表，添加行列表算i。
  
  印"已读取最后"加日志列表，长度加"行日志"。
  回日志列表。

# 过滤日志
定过滤日志是函日志列表、关键词：
  定过滤后是列。
  
  对于日志于日志列表：
    若日志，包含(关键词)就：
      过滤后，添加日志。
  
  印"过滤关键词'"加关键词加"'，找到"加过滤后，长度加"条日志"。
  回过滤后。

# 统计日志级别
定统计日志级别是函日志列表：
  定统计结果是典：
    DEBUG是0
    INFO是0
    WARNING是0
    ERROR是0
    CRITICAL是0。
  
  对于日志于日志列表：
    若日志，包含("DEBUG")就：
      统计结果·DEBUG加等于1。
    不然若日志，包含("INFO")就：
      统计结果·INFO加等于1。
    不然若日志，包含("WARNING")就：
      统计结果·WARNING加等于1。
    不然若日志，包含("ERROR")就：
      统计结果·ERROR加等于1。
    不然若日志，包含("CRITICAL")就：
      统计结果·CRITICAL加等于1。
  
  印"===== 日志级别统计 ====="。
  印"DEBUG: "加统计结果·DEBUG。
  印"INFO: "加统计结果·INFO。
  印"WARNING: "加统计结果·WARNING。
  印"ERROR: "加统计结果·ERROR。
  印"CRITICAL: "加统计结果·CRITICAL。
  印""。
  
  回统计结果。

# 实时监控日志
定实时监控日志是函文件路径、关键词列表、持续时间秒数：
  定开始时间是当前时间。
  定文件大小是文件路径，获取文件大小。
  
  印"===== 实时监控日志 ====="。
  印"日志文件："加文件路径。
  印"监控关键词："加关键词列表，用"、"连接。
  印""。
  
  当(当前时间减开始时间)小于持续时间秒数：
    定新大小是文件路径，获取文件大小。
    
    若新大小大于文件大小就：
      # 读取新增内容
      定内容是文件路径，读文件(偏移是文件大小)。
      定行列表是内容，用"\n"分割。
      
      对于行于行列表：
        若行等于""就：
          继续。
        
        对于关键词于关键词列表：
          若行，包含(关键词)就：
            定时间戳是当前时间，格式化时间是"%H:%M:%S"。
            印时间戳加" | "加行。
      
      文件大小是新大小。
    
    睡眠(1)。
  
  印"监控结束"。

# ===== 自动化任务 =====

# 定时执行任务
定定时执行任务是函任务函数、间隔秒数、执行次数：
  定执行记录是列。
  
  印"===== 开始定时任务 ====="。
  印"执行间隔："加间隔秒数加"秒"。
  印"执行次数："加执行次数。
  印""。
  
  对于i从1到执行次数：
    定开始时间是当前时间。
    
    印"第"加i加"次执行："。
    任务函数。
    
    定结束时间是当前时间。
    定耗时是结束时间减开始时间。
    
    执行记录，添加典：
      次数是i
      开始时间是开始时间
      结束时间是结束时间
      耗时是耗时。
    
    印"耗时："加耗时转字符串(保留3位小数)加"秒"。
    印""。
    
    若i小于执行次数就：
      睡眠(间隔秒数)。
  
  印"定时任务完成"。
  回执行记录。

# 条件触发任务
定条件触发任务是函条件函数、任务函数、检查间隔秒数、超时秒数：
  定开始时间是当前时间。
  
  印"===== 等待条件触发 ====="。
  
  当(当前时间减开始时间)小于超时秒数：
    若条件函数就：
      印"条件满足，执行任务"。
      任务函数。
      回真。
    
    睡眠(检查间隔秒数)。
  
  印"超时，条件未满足"。
  回假。

# 系统健康检查
定系统健康检查是函：
  印"===== 系统健康检查 ====="。
  
  定检查结果是列。
  
  # 检查CPU
  定CPU是系统，获取CPU使用率(1)。
  若CPU小于80就：
    检查结果，添加典项目是"CPU"、状态是"正常"、值是CPU。
  不然：
    检查结果，添加典项目是"CPU"、状态是"警告"、值是CPU。
  
  # 检查内存
  定内存是系统，获取内存信息。
  定内存使用率是(内存·总内存减内存·可用内存)除以内存·总内存乘以100。
  
  若内存使用率小于80就：
    检查结果，添加典项目是"内存"、状态是"正常"、值是内存使用率。
  不然：
    检查结果，添加典项目是"内存"、状态是"警告"、值是内存使用率。
  
  # 检查磁盘
  定磁盘列表是系统，获取磁盘信息。
  
  对于磁盘于磁盘列表：
    定磁盘使用率是磁盘·已用空间除以磁盘·总空间乘以100。
    
    若磁盘使用率小于80就：
      检查结果，添加典项目是"磁盘"加磁盘·挂载点、状态是"正常"、值是磁盘使用率。
    不然：
      检查结果，添加典项目是"磁盘"加磁盘·挂载点、状态是"警告"、值是磁盘使用率。
  
  # 检查网络
  定网络状态是网络，测试连接("8.8.8.8")。
  
  若网络状态就：
    检查结果，添加典项目是"网络"、状态是"正常"、值是"连接正常"。
  不然：
    检查结果，添加典项目是"网络"、状态是"异常"、值是"连接失败"。
  
  # 输出结果
  对于结果于检查结果：
    定状态标记是若结果·状态等于"正常"就"✓"不然"✗"。
    印状态标记加" "加结果·项目加"："加结果·值转字符串(保留1位小数)加"%"。
  
  印""。
  回检查结果。

# 生成系统报告
定生成系统报告是函报告路径：
  定报告内容是列。
  
  报告内容，添加("===== 系统监控报告 =====")。
  报告内容，添加("生成时间："加当前时间，格式化时间是"%Y-%m-%d %H:%M:%S")。
  报告内容，添加("")。
  
  # 系统信息
  定系统信息是获取系统信息。
  报告内容，添加("【系统信息】")。
  报告内容，添加("操作系统："加系统信息·操作系统)。
  报告内容，添加("主机名："加系统信息·主机名)。
  报告内容，添加("")。
  
  # CPU信息
  定CPU使用率是系统，获取CPU使用率(1)。
  报告内容，添加("【CPU使用率】")。
  报告内容，添加("当前使用率："加CPU使用率转字符串(保留2位小数)加"%")。
  报告内容，添加("")。
  
  # 内存信息
  定内存信息是获取内存使用情况。
  报告内容，添加("【内存使用情况】")。
  报告内容，添加("使用率："加内存信息·使用率转字符串(保留2位小数)加"%")。
  报告内容，添加("")。
  
  # 磁盘信息
  定磁盘列表是获取磁盘使用情况。
  报告内容，添加("【磁盘使用情况】")。
  对于磁盘于磁盘列表：
    定使用率是磁盘·已用空间除以磁盘·总空间乘以100。
    报告内容，添加(磁盘·挂载点加"："加使用率转字符串(保留2位小数)加"%")。
  报告内容，添加("")。
  
  # 健康检查
  定健康检查是系统健康检查。
  报告内容，添加("【健康检查】")。
  对于结果于健康检查：
    报告内容，添加(结果·项目加"："加结果·状态)。
  
  # 写入文件
  定报告文本是报告内容，用"\n"连接。
  报告路径、报告文本，写文件。
  
  印"系统报告已生成："加报告路径。

# ===== 主程序 =====

印"========================================"。
印"系统监控示例"。
印"========================================"。
印""。

# 系统信息
印"【第一步：获取系统信息】"。
定系统信息是获取系统信息。

# 资源监控
印"【第二步：资源监控】"。
定CPU使用率是获取CPU使用率(间隔秒数是1)。
定内存信息是获取内存使用情况。
定磁盘信息是获取磁盘使用情况。
定网络信息是获取网络信息。

# 进程管理
印"【第三步：进程管理】"。
定进程列表是获取进程列表。
定匹配进程是"python"，查找进程。

# 系统健康检查
印"【第四步：系统健康检查】"。
定健康检查是系统健康检查。

# 异常检测
印"【第五步：异常检测】"。
定异常列表是80、85、90，检测系统异常。

# 生成报告
印"【第六步：生成系统报告】"。
"./system_report.txt"，生成系统报告。

印""。
印"========================================"。
印"系统监控完成"。
印"========================================"。
