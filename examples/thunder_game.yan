# ============================================
# 雷电射击游戏
# ============================================

# 游戏状态
定义游戏：
  初始状态：菜单。
  初始分数：0。
  初始生命：3。
  初始等级：1。
  
  状态为菜单：
    按下开始键，状态变为游戏中。
    按下设置键，状态变为设置。
  
  状态为游戏中：
    玩家被击中，生命减1。
    生命等0，状态变为游戏结束。
    分数达到1000，等级加1、敌人速度加快。
    按下暂停键，状态变为暂停。
  
  状态为暂停：
    按下继续键，状态变为游戏中。
    按下退出键，状态变为菜单。
  
  状态为游戏结束：
    按下重新开始键，分数重置、生命重置、状态变为游戏中。
    按下退出键，状态变为菜单。

# ============================================
# 玩家控制
# ============================================

定义玩家：
  初始位置：屏幕中央。
  初始速度：5。
  初始生命：3。
  
  按下左键，位置左移速度。
  按下右键，位置右移速度。
  按下上键，位置上移速度。
  按下下键，位置下移速度。
  按下空格键，发射子弹。
  
  位置超出左边界，位置等于左边界。
  位置超出右边界，位置等于右边界。
  位置超出上边界，位置等于上边界。
  位置超出下边界，位置等于下边界。

# ============================================
# 子弹系统
# ============================================

定义子弹：
  初始位置：玩家位置。
  初始速度：10。
  初始状态：活跃。
  
  状态为活跃：
    位置上移速度。
    位置超出屏幕，状态变为销毁。
    碰撞敌人，敌人受伤、状态变为销毁。

# ============================================
# 敌人系统
# ============================================

定义敌人：
  初始位置：随机顶部。
  初始速度：2。
  初始生命：1。
  初始类型：普通。
  
  状态为活跃：
    位置下移速度。
    位置超出屏幕，状态变为销毁。
    碰撞玩家，玩家受伤、状态变为销毁。
    碰撞子弹，生命减1。
    生命等0，爆炸、加分、状态变为销毁。

# ============================================
# 碰撞检测
# ============================================

定检测碰撞是函对象1、对象2：
  定距离是算对象1位置减对象2位置，绝对。
  若距离小于对象1半径加对象2半径就：
    返回真。
  
  不然：
    返回假。

# ============================================
# 渲染系统
# ============================================

定渲染游戏是函游戏状态：
  清空屏幕。
  
  游戏状态为游戏中：
    渲染玩家。
    渲染所有子弹。
    渲染所有敌人。
    渲染分数、生命、等级。
  
  游戏状态为菜单：
    渲染菜单界面。
  
  游戏状态为暂停：
    渲染暂停界面。
  
  游戏状态为游戏结束：
    渲染游戏结束界面、最终分数。
  
  刷新屏幕。

# ============================================
# 主循环
# ============================================

定主循环是函：
  定游戏实例是创建游戏。
  定玩家实例是创建玩家。
  定子弹列表是空列表。
  定敌人列表是空列表。
  
  一直：
    # 处理输入
    处理按键事件。
    
    # 更新游戏状态
    游戏实例，更新。
    玩家实例，更新。
    子弹列表，更新所有。
    敌人列表，更新所有。
    
    # 碰撞检测
    子弹列表、敌人列表，检测所有碰撞。
    玩家实例、敌人列表，检测碰撞。
    
    # 生成敌人
    每隔2秒，生成新敌人。
    
    # 渲染
    游戏实例状态，渲染游戏。
    
    # 帧率控制
    等待16毫秒。

# 启动游戏
主循环。
