# 实用工具示例
# 展示言律语言在实际应用中的能力

# ===== 文件处理工具 =====

# 读取文件内容
定读取文件是函文件路径：
定内容是文件路径，读文件。
回内容。

# 写入文件内容
定写入文件是函文件路径、内容：
文件路径、内容，写文件。
印"文件已保存"。

# 复制文件
定复制文件是函源路径、目标路径：
定内容是源路径，读文件。
目标路径、内容，写文件。
印"文件已复制"。

# 删除文件
定删除文件是函文件路径：
文件路径，删除。
印"文件已删除"。

# 列出目录内容
定列出目录是函目录路径：
定文件列表是目录路径，列目录。
对于文件于文件列表：
印文件。

# 查找文件
定查找文件是函目录路径、文件名：
定文件列表是目录路径，列目录。
定结果是文件列表，筛选包含文件名。
回结果。

# ===== 字符串处理工具 =====

# 分割字符串
定分割字符串是函字符串、分隔符：
定结果是字符串，按分隔符分割。
回结果。

# 连接字符串
定连接字符串是函字符串列表、连接符：
定结果是字符串列表，用连接符连接。
回结果。

# 替换字符串
定替换字符串是函字符串、旧内容、新内容：
定结果是字符串，将旧内容替换为新内容。
回结果。

# 提取子串
定提取子串是函字符串、开始位置、结束位置：
定结果是字符串，从开始位置到结束位置。
回结果。

# 统计字符
定统计字符是函字符串、字符：
定计数是0。
对于字符于字符串：
若字符等于字符就：
计数加等于1。
回计数。

# ===== 列表处理工具 =====

# 列表排序
定排序列表是函列表、排序方式：
若排序方式等于"升序"就：
回列表，升序排序。
不然：
回列表，降序排序。

# 列表去重
定去重是函列表：
定结果是列表，去除重复元素。
回结果。

# 列表分组
定分组是函列表、分组大小：
定结果是列。
定当前组是列。
定计数是0。

对于元素于列表：
当前组，添加元素。
    计数加等于1。

    若计数等于分组大小就：
    结果，添加当前组。
    当前组是列。
        计数是0。

        若当前组，长度大于0就：
        结果，添加当前组。

        回结果。

        # 列表扁平化
        定扁平化是函嵌套列表：
        定结果是列。

        对于元素于嵌套列表：
        若元素是列表就：
        定子列表是元素，扁平化。
        结果，合并子列表。
        不然：
        结果，添加元素。

        回结果。

        # ===== 字典处理工具 =====

        # 合并字典
        定合并字典是函字典1、字典2：
        定结果是字典1，复制。
        对于键于字典2：
        结果·键是字典2·键。
        回结果。

        # 过滤字典
        定过滤字典是函字典、键列表：
        定结果是典。
        对于键于键列表：
        若字典包含键就：
        结果·键是字典·键。
        回结果。

        # 字典键值互换
        定键值互换是函字典：
        定结果是典。
        对于键于字典：
        定值是字典·键。
        结果·值是键。
        回结果。

        # ===== 数学计算工具 =====

        # 计算平均值
        定平均值是函列表：
        定总和是列表，求和。
        定数量是列表，长度。
        回总和除以数量。

        # 计算中位数
        定中位数是函列表：
        定排序后是列表，升序排序。
        定长度是排序后，长度。
        定中间是长度除以2。

        若长度是偶数就：
        定值1是排序后算中间减1。
        定值2是排序后算中间。
        回值1加值2除以2。
        不然：
        回排序后算中间。

        # 计算标准差
        定标准差是函列表：
        定均值是列表，平均值。
        定平方和是0。

        对于值于列表：
        定差是值减均值。
        平方和加等于差乘以差。

        定方差是平方和除以列表，长度。
        回方差，开方。

        # 计算阶乘
        定阶乘是函数：
        若数等于0或数等于1就：
        回1。
        不然：
        回数乘以数减1的阶乘。

        # 判断素数
        定是素数是函数：
        若数小于2就：
        回假。

        对于i从2到数减1：
        若数除以i的余数等于0就：
        回假。

        回真。

        # ===== 时间处理工具 =====

        # 获取当前时间
        定当前时间是函：
        回当前时间。

        # 格式化时间
        定格式化时间是函时间、格式：
        定结果是时间，按格式格式化。
        回结果。

        # 计算时间差
        定时间差是函开始时间、结束时间：
        定差是结束时间减开始时间。
        回差。

        # 延迟执行
        定延迟是函毫秒数：
        等待毫秒数毫秒。

        # ===== 网络请求工具 =====

        # HTTP GET请求
        定获取请求是函网址：
        定响应是网址，HTTP获取。
        回响应。

        # HTTP POST请求
        定提交请求是函网址、数据：
        定响应是网址、数据，HTTP提交。
        回响应。

        # 下载文件
        定下载文件是函网址、保存路径：
        网址、保存路径，下载。
        印"文件已下载"。

        # ===== JSON处理工具 =====

        # 解析JSON
        定解析JSON是函JSON字符串：
        定结果是JSON字符串，解析。
        回结果。

        # 生成JSON
        定生成JSON是函对象：
        定结果是对象，序列化。
        回结果。

        # 格式化JSON
        定格式化JSON是函JSON字符串：
        定对象是JSON字符串，解析。
        定结果是对象，格式化序列化。
        回结果。

        # ===== 实用工具组合 =====

        # 文件分析工具
        定分析文件是函文件路径：
        # 读取文件
        定内容是文件路径，读文件。

        # 统计信息
        定行数是内容，按换行符分割，长度。
        定字符数是内容，长度。
        定单词数是内容，按空格分割，长度。

        # 返回结果
        回典：
        行数是行数
        字符数是字符数
        单词数是单词数

        # 批量文件处理
        定批量处理是函目录路径、处理函数：
        定文件列表是目录路径，列目录。
        定结果是列。

        对于文件于文件列表：
        定文件路径是目录路径连接文件。
        定处理结果是文件路径，处理函数。
        结果，添加处理结果。

        回结果。

        # 数据清洗工具
        定清洗数据是函数据：
        # 去除空值
        定无空值是数据，过滤非空。

        # 去除重复
        定无重复是无空值，去重。

        # 标准化
        定标准化是无重复，标准化。

        回标准化。

        # ===== 测试 =====

        # 测试文件处理
        "test.txt"、"你好，言律语言！"，写入文件。
        "test.txt"，读取文件，印。

        # 测试字符串处理
        "苹果、香蕉、橙子"、"、"，分割字符串，印。
        列"苹果"、"香蕉"、"橙子"、"、"，连接字符串，印。

        # 测试列表处理
        列3、1、4、1、5、9、2、6、"升序"，排序列表，印。
        列1、2、2、3、3、3、4，去重，印。

        # 测试数学计算
        列1、2、3、4、5，平均值，印。
        列1、2、3、4、5，中位数，印。
        列1、2、3、4、5，标准差，印。
        5，阶乘，印。
        17，是素数，印。

        # 测试时间处理
        当前时间，印。
            当前时间、"YYYY年MM月DD日 HH:mm:ss"，格式化时间，印。

                # 测试JSON处理
                定测试对象是典姓名是"张三"、年龄是25、城市是"北京"。
                测试对象，生成JSON，印。
                '{"name":"李四","age":30}'，解析JSON，印。

                # 测试文件分析
                "test.txt"，分析文件，印。
