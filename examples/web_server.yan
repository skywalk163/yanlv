# Web服务器示例
# 展示如何用言律语言构建RESTful API服务器

# ===== 导入HTTP模块 =====

导入《HTTP》。

# ===== 定义数据模型 =====

# 用户模型
定用户模型是典：
  编号是空
  名称是空
  年龄是空
  邮箱是空
  创建时间是空。

# 模拟数据库
定用户数据库是列。

# ===== 路由定义 =====

# 主页路由
定主页处理器是函请求：
  回典：
    状态是200
    体是"欢迎使用言律语言Web服务器"
    类型是"text/plain"。

# 获取所有用户
定获取所有用户是函请求：
  回典：
    状态是200
    体是用户数据库
    类型是"application/json"。

# 获取单个用户
定获取用户是函请求：
  定用户ID是请求·参数·编号。
  
  # 查找用户
  定用户是空。
  对于项于用户数据库：
    若项·编号等于用户ID就：
      用户是项。
  
  # 返回结果
  若用户不等于空就：
    回典：
      状态是200
      体是用户
      类型是"application/json"。
  不然：
    回典：
      状态是404
      体是典错误是"用户不存在"。
      类型是"application/json"。

# 创建用户
定创建用户是函请求：
  定用户数据是请求·体。
  
  # 验证数据
  若用户数据·名称等于空就：
    回典：
      状态是400
      体是典错误是"名称不能为空"。
      类型是"application/json"。
  
  # 创建新用户
  定新用户是典：
    编号是用户数据库，长度加1
    名称是用户数据·名称
    年龄是用户数据·年龄
    邮箱是用户数据·邮箱
    创建时间是当前时间。
  
  # 保存到数据库
  用户数据库，添加新用户。
  
  # 返回结果
  回典：
    状态是201
    体是新用户
    类型是"application/json"。

# 更新用户
定更新用户是函请求：
  定用户ID是请求·参数·编号。
  定用户数据是请求·体。
  
  # 查找用户
  定用户索引是负1。
  对于i从0到用户数据库，长度减1：
    若用户数据库算i·编号等于用户ID就：
      用户索引是i。
  
  # 更新用户
  若用户索引不等于负1就：
    定旧用户是用户数据库算用户索引。
    定新用户是典：
      编号是旧用户·编号
      名称是用户数据·名称
      年龄是用户数据·年龄
      邮箱是用户数据·邮箱
      创建时间是旧用户·创建时间。
    
    用户数据库算用户索引是新用户。
    
    回典：
      状态是200
      体是新用户
      类型是"application/json"。
  不然：
    回典：
      状态是404
      体是典错误是"用户不存在"。
      类型是"application/json"。

# 删除用户
定删除用户是函请求：
  定用户ID是请求·参数·编号。
  
  # 查找用户
  定用户索引是负1。
  对于i从0到用户数据库，长度减1：
    若用户数据库算i·编号等于用户ID就：
      用户索引是i。
  
  # 删除用户
  若用户索引不等于负1就：
    用户数据库，删除用户索引。
    
    回典：
      状态是200
      体是典消息是"用户已删除"。
      类型是"application/json"。
  不然：
    回典：
      状态是404
      体是典错误是"用户不存在"。
      类型是"application/json"。

# ===== 路由表 =====

定路由是典：
  获取是典：
    "/"是主页处理器
    "/用户"是获取所有用户
    "/用户/:编号"是获取用户。
  
  提交是典：
    "/用户"是创建用户。
  
  更新是典：
    "/用户/:编号"是更新用户。
  
  删除是典：
    "/用户/:编号"是删除用户。

# ===== 中间件 =====

# 日志中间件
定日志中间件是函请求、响应、下一步：
  定时间戳是当前时间。
  定方法是请求·方法。
  定路径是请求·路径。
  
  印时间戳加" - "加方法加" "加路径。
  
  请求、响应，下一步。

# 错误处理中间件
定错误处理中间件是函请求、响应、下一步：
  试：
    请求、响应，下一步。
  捕错误：
    印"错误："加错误。
    
    回典：
      状态是500
      体是典错误是"服务器内部错误"。
      类型是"application/json"。

# CORS中间件
定CORS中间件是函请求、响应、下一步：
  响应·头部是典：
    "Access-Control-Allow-Origin"是"*"
    "Access-Control-Allow-Methods"是"GET, POST, PUT, DELETE"
    "Access-Control-Allow-Headers"是"Content-Type"。
  
  请求、响应，下一步。

# ===== 服务器配置 =====

定服务器配置是典：
  主机是"localhost"
  端口是8080
  路由是路由
  中间件是列日志中间件、错误处理中间件、CORS中间件。

# ===== 启动服务器 =====

定启动服务器是函配置：
  印"========================================"。
  印"言律语言Web服务器"。
  印"========================================"。
  印""。
  印"服务器地址：http://"加配置·主机加":"加配置·端口。
  印""。
  印"可用路由："。
  印"  GET    /              - 主页"。
  印"  GET    /用户          - 获取所有用户"。
  印"  GET    /用户/:编号    - 获取单个用户"。
  印"  POST   /用户          - 创建用户"。
  印"  PUT    /用户/:编号    - 更新用户"。
  印"  DELETE /用户/:编号    - 删除用户"。
  印""。
  印"按 Ctrl+C 停止服务器"。
  印"========================================"。
  
  # 启动HTTP服务器
  配置，启动HTTP服务器。

# ===== 测试数据 =====

# 添加测试用户
定添加测试数据是函：
  定用户1是典：
    编号是1
    名称是"张三"
    年龄是25
    邮箱是"zhangsan@example.com"
    创建时间是当前时间。
  
  定用户2是典：
    编号是2
    名称是"李四"
    年龄是30
    邮箱是"lisi@example.com"
    创建时间是当前时间。
  
  定用户3是典：
    编号是3
    名称是"王五"
    年龄是28
    邮箱是"wangwu@example.com"
    创建时间是当前时间。
  
  用户数据库，添加用户1。
  用户数据库，添加用户2。
  用户数据库，添加用户3。
  
  印"已添加3个测试用户"。

# ===== 主程序 =====

# 添加测试数据
添加测试数据。

# 启动服务器
服务器配置，启动服务器。
