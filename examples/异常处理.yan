# 言律语言示例程序 - 异常处理

# 基本异常抛出
输出 "测试1：抛出异常"
抛出 "这是一个测试异常"

# try-catch捕获异常
输出 "测试2：捕获异常"
尝试
    抛出 "捕获我！"
捕获 "所有" 为 错误
    输出 "捕获到异常："
    输出 错误
结束

# 无异常情况
输出 "测试3：无异常情况"
尝试
    定义变量x为 100
    输出 "正常执行"
捕获 "所有" 为 错误
    输出 "不应该执行这里"
结束

# 异常类型匹配
输出 "测试4：异常类型匹配"
尝试
    抛出 "自定义错误"
捕获 "自定义错误" 为 错误
    输出 "匹配到自定义错误"
捕获 "所有" 为 错误
    输出 "匹配到其他错误"
结束

# 函数中的异常
输出 "测试5：函数中的异常"
定义函数 除法 参数 a, b
    如果 b 等于 0 则
        抛出 "除零错误"
    结束
    返回 a 除以 b
结束

尝试
    定义变量结果为 调用 除法 参数 10, 0
捕获 "所有" 为 错误
    输出 "捕获到除零错误"
结束
