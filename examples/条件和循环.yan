# 言律语言示例程序 - 条件和循环

# 条件语句示例
定义变量分数为 85

如果 分数 大于等于 90 则
    输出 "优秀"
    否则
        如果 分数 大于等于 80 则
            输出 "良好"
            否则
                如果 分数 大于等于 60 则
                    输出 "及格"
                    否则
                        输出 "不及格"

            # 循环语句示例
            输出 "循环1到5："
            定义变量i为 1
            循环 5 次执行
                输出 i
                定义变量i为 i 加 1

            # 嵌套循环示例
            输出 "九九乘法表："
            定义变量行为 1
            循环 9 次执行
                定义变量列为 1
                循环 9 次执行
                    定义变量积为 行 乘以 列
                    输出 积
                    定义变量列为 列 加 1
                定义变量行为 行 加 1

            # 条件和循环结合
            输出 "1到10中的偶数："
            定义变量n为 1
            循环 10 次执行
                定义变量余数为 n 模 2
                如果 余数 等于 0 则
                    输出 n
                定义变量n为 n 加 1
