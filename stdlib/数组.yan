# 言律语言标准库 - 数组模块

定义模块 数组
    # 基本操作
    定义函数 排序 参数 数组
        返回 排序 数组
    结束
    
    定义函数 反转 参数 数组
        返回 反转 数组
    结束
    
    # 统计函数
    定义函数 最大值 参数 数组
        返回 最大值 数组
    结束
    
    定义函数 最小值 参数 数组
        返回 最小值 数组
    结束
    
    定义函数 求和 参数 数组
        返回 求和 数组
    结束
    
    定义函数 平均值 参数 数组
        定义变量总和为 求和 数组
        定义变量长度为 数组的长度
        返回 总和 除以 长度
    结束
    
    # 查找
    定义函数 查找元素 参数 数组, 元素
        定义变量索引为 0
        循环 数组的长度 次执行
            如果 数组[索引] 等于 元素 则
                返回 索引
            结束
            定义变量索引为 索引 加 1
        结束
        返回 -1
    结束
    
    定义函数 包含 参数 数组, 元素
        定义变量索引为 查找元素 数组, 元素
        返回 索引 不等于 -1
    结束
    
    # 导出所有函数
    导出 排序
    导出 反转
    导出 最大值
    导出 最小值
    导出 求和
    导出 平均值
    导出 查找元素
    导出 包含
结束模块
