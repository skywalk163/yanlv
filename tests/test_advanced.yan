# 言律语言高级语法测试

# ========================================
# 测试1: 循环语法
# ========================================
输出 "=== 测试1: 循环语法 ==="

# 从1到5
对于 i 从 1 到 5 ：
  输出 i

# 计算1到10的和
定 总和 是 0。
对于 j 从 1 到 10 ：
  定 总和 是 总和 加 j。

输出 "1到10的和："
输出 总和

# ========================================
# 测试2: 数组操作
# ========================================
输出 "=== 测试2: 数组操作 ==="

# 定义数组
定 数据 是 列 5 3 8 4 2 1 9 6。
输出 "数组："
输出 数据

# 数组长度
输出 "数组长度："
输出 长度 数据

# ========================================
# 测试3: 因果链
# ========================================
输出 "=== 测试3: 因果链 ==="

定 温度 是 30。
温度大于28，输出"开启空调制冷"。
温度小于20，输出"开启空调制热"。
温度在20到28之间，输出"温度适宜"。

# ========================================
# 测试4: 复合条件
# ========================================
输出 "=== 测试4: 复合条件 ==="

定 湿度 是 65。
温度大于25且湿度大于60，输出"高温高湿"。
温度小于15或湿度小于30，输出"低温或低湿"。

# ========================================
# 测试5: 算术运算
# ========================================
输出 "=== 测试5: 算术运算 ==="

定 a 是 10。
定 b 是 3。
输出 "加法："
输出 a 加 b
输出 "减法："
输出 a 减 b
输出 "乘法："
输出 a 乘 b
输出 "除法："
输出 a 除 b

# ========================================
# 测试6: 智能家居场景
# ========================================
输出 "=== 测试6: 智能家居 ==="

温度大于28，输出"智能控制：开启空调制冷"。
温度小于20，输出"智能控制：开启空调制热"。
湿度大于70，输出"智能控制：开启除湿机"。
湿度小于40，输出"智能控制：开启加湿器"。

# ========================================
# 总结
# ========================================
输出 "=== 所有测试完成 ==="
