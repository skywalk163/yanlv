# 言律语言完整测试套件

# ========================================
# 测试1: 因果链语法
# ========================================
温度大于28，输出"开启空调制冷"。
温度小于20，输出"开启空调制热"。
温度在20到28之间，输出"温度适宜"。

湿度大于70，输出"开启除湿机"。
湿度小于40，输出"开启加湿器"。

# ========================================
# 测试2: 变量定义
# ========================================
定温度是30。
定湿度是65。
定分数是85。
定姓名是"张三"。

# ========================================
# 测试3: 输出语句
# ========================================
输出"你好，言律语言！"
输出温度
输出湿度
输出姓名

# ========================================
# 测试4: 条件判断
# ========================================
分数大于等于90，输出"优秀"。
分数大于等于80，输出"良好"。
分数大于等于60，输出"及格"。
分数小于60，输出"不及格"。

# ========================================
# 测试5: 复合条件
# ========================================
温度大于25且湿度大于60，输出"高温高湿"。
温度小于15或湿度小于30，输出"低温或低湿"。

# ========================================
# 测试6: 算术运算
# ========================================
定a是10。
定b是3。
定和是a加b。
定差是a减b。
定积是a乘b。
定商是a除b。

输出"算术运算测试："
输出和
输出差
输出积
输出商

# ========================================
# 测试7: 智能家居场景
# ========================================
温度大于28，输出"智能控制：开启空调制冷"。
温度小于20，输出"智能控制：开启空调制热"。
湿度大于70，输出"智能控制：开启除湿机"。
湿度小于40，输出"智能控制：开启加湿器"。

# ========================================
# 测试8: 业务流程
# ========================================
定订单状态是"已付款"。
订单状态等于"已付款"，输出"准备发货"。
订单状态等于"已发货"，输出"通知用户"。
订单状态等于"已签收"，输出"更新库存"。

# ========================================
# 总结
# ========================================
输出"=== 所有测试完成 ==="
