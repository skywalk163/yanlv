# 言律语言综合测试套件

# ========================================
# 测试1: 基础输出功能
# ========================================
输出 "=== 测试1: 基础输出 ==="
输出 "字符串输出测试"
输出 123
输出 45.67

# ========================================
# 测试2: 变量定义
# ========================================
输出 "=== 测试2: 变量定义 ==="
定 整数 是 100
定 小数 是 3.14
定 文本 是 "言律语言"

输出 整数
输出 小数
输出 文本

# ========================================
# 测试3: 算术运算
# ========================================
输出 "=== 测试3: 算术运算 ==="
定 a 是 20
定 b 是 8

输出 "加法："
输出 a 加 b

输出 "减法："
输出 a 减 b

输出 "乘法："
输出 a 乘 b

输出 "除法："
输出 a 除 b

# ========================================
# 测试4: 条件判断
# ========================================
输出 "=== 测试4: 条件判断 ==="
定 分数 是 85

分数 大于等于 90，输出 "优秀"。
分数 大于等于 80，输出 "良好"。
分数 大于等于 70，输出 "中等"。
分数 大于等于 60，输出 "及格"。
分数 小于 60，输出 "不及格"。

# ========================================
# 测试5: 复合条件
# ========================================
输出 "=== 测试5: 复合条件 ==="
定 温度 是 28
定 湿度 是 65

温度 大于 25 且 湿度 大于 60，输出 "高温高湿"。
温度 小于 15 或 湿度 小于 30，输出 "低温或低湿"。

# ========================================
# 测试6: 范围判断
# ========================================
输出 "=== 测试6: 范围判断 ==="
定 数值 是 50

数值 在 1 到 100 之间，输出 "在范围内"。
数值 在 60 到 80 之间，输出 "在子范围内"。

# ========================================
# 测试7: 因果链
# ========================================
输出 "=== 测试7: 因果链 ==="
定 状态 是 "运行"

状态 等于 "运行"，输出 "系统正在运行"。
状态 等于 "停止"，输出 "系统已停止"。
状态 等于 "错误"，输出 "系统出错"。

# ========================================
# 测试8: 嵌套表达式
# ========================================
输出 "=== 测试8: 嵌套表达式 ==="
定 x 是 10
定 y 是 5
定 z 是 3

输出 "复杂运算："
输出 x 加 y 乘 z

# ========================================
# 测试9: 边界情况
# ========================================
输出 "=== 测试9: 边界情况 ==="
定 零 是 0
定 负数 是 -10
定 大数 是 1000000

输出 零
输出 负数
输出 大数

零 等于 0，输出 "零值测试通过"。
负数 小于 0，输出 "负数测试通过"。
大数 大于 100000，输出 "大数测试通过"。

# ========================================
# 测试10: 实际应用场景
# ========================================
输出 "=== 测试10: 实际应用 ==="

# 成绩等级判断
定 学生分数 是 78
输出 "学生成绩等级："
学生分数 大于等于 90，输出 "A"。
学生分数 大于等于 80，输出 "B"。
学生分数 大于等于 70，输出 "C"。
学生分数 大于等于 60，输出 "D"。
学生分数 小于 60，输出 "F"。

# 温度状态
定 当前温度 是 35
输出 "温度状态："
当前温度 大于 30，输出 "炎热"。
当前温度 在 20 到 30 之间，输出 "舒适"。
当前温度 小于 10，输出 "寒冷"。

# ========================================
# 测试完成
# ========================================
输出 "=== 所有测试完成 ==="
输出 "✅ 基础功能测试通过"
输出 "✅ 算术运算测试通过"
输出 "✅ 条件判断测试通过"
输出 "✅ 因果链测试通过"
输出 "✅ 实际应用测试通过"
